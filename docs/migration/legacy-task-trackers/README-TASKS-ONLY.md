# FormuLab — TASKS-Only Tracking Migration Record

This directory preserves historical FormuLab task trackers and control-plane artifacts for auditability and migration provenance.

## Current authority

Current project task state is defined **only** by the repository-root `TASKS.md` on the H!veAI configured tracked branch `feature/laboratory-stability`.

GitHub repository metadata plus root `TASKS.md` are the only inputs H!veAI needs for current project tracking.

Everything under this directory is historical evidence only. It must not be parsed as a current task ledger, used to override task status, or treated as an alternative source of current milestone/sprint/task truth.

The archived material may contain older status vocabularies, tables, percentages, branch references, handoffs, or control-plane conventions that were correct at the time but are now superseded by the root TASKS-only architecture.
