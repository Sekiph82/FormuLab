# FormuLab v1 — Task Tracker

Authoritative operational source of truth for FormuLab v1 completion.
Scope is frozen by [`docs/FORMULAB_V1_FINAL_SCOPE.md`](FORMULAB_V1_FINAL_SCOPE.md).
Current execution pointer: [`docs/handoffs/FORMULAB_V1_CURRENT.md`](handoffs/FORMULAB_V1_CURRENT.md).

## Status values

Only three literal values are used. Never `TODO`/`PARTIAL`/`REVIEW`/
`BLOCKED`/`READY`/`DONE`/`IN PROGRESS`.

- **(blank)** — not started
- **ON PROCESS** — actively being implemented in the current session
- **COMPLETED** — implemented, tested, acceptance criteria met, committed

A dependency may explain why a blank task cannot begin yet; its status
still reads blank until work actually starts.

## Tracker update protocol (mandatory for every future session)

At the **start** of every implementation session:
1. Read `FORMULAB_V1_FINAL_SCOPE.md`, this tracker, and
   `handoffs/FORMULAB_V1_CURRENT.md`.
2. Inspect `git status`.
3. Select the exact next eligible blank task (respecting `Depends on`).
4. Change **only** that task's status to `ON PROCESS`.
5. Update `handoffs/FORMULAB_V1_CURRENT.md`.
6. Synchronize the task's GitHub issue/checklist status.
7. Then begin implementation.

**During implementation:**
8. Bugs required for the selected task to work are documented and fixed
   under that same task — never a new top-level work package.
9. Genuinely unrelated discoveries go in that task's Notes only; they are
   not added to frozen v1 scope and are not implemented unless required.
10. Never silently change another task to `ON PROCESS`.

**At task completion:**
11. Run the task's required tests.
12. Run regression tests proportional to the change.
13. Record exact test results in the task's Evidence field.
14. Record acceptance evidence.
15. Commit the implementation.
16. Record the commit SHA in the task.
17. Change status to `COMPLETED`.
18. Synchronize the GitHub issue/checklist.
19. Update `handoffs/FORMULAB_V1_CURRENT.md` to the next exact task.
20. Push normally to `origin/feature/laboratory-stability`.
21. Never force-push or rewrite history.

A task is **not** COMPLETED merely because code exists — it requires
implementation + tests + acceptance criteria satisfied + tracker evidence +
commit + successful push where expected.

## Completion percentage (derived, not guessed)

Recompute from the counts below whenever tasks change. Do not use
subjective percentages.

| Work package | GitHub issue | Total tasks | COMPLETED | ON PROCESS | blank |
|---|---|---|---|---|---|
| FVL-01 | [#2](https://github.com/Sekiph82/FormuLab/issues/2) (closed) | 21 | 21 | 0 | 0 |
| FVL-02 | [#3](https://github.com/Sekiph82/FormuLab/issues/3) (closed) | 24 | 24 | 0 | 0 |
| FVL-03 | [#4](https://github.com/Sekiph82/FormuLab/issues/4) (closed) | 18 | 18 | 0 | 0 |
| FVL-04 | [#5](https://github.com/Sekiph82/FormuLab/issues/5) | 26 | 25 | 0 | 1 |
| FVL-05 | [#6](https://github.com/Sekiph82/FormuLab/issues/6) | 14 | 0 | 0 | 14 |
| FVL-06 | [#7](https://github.com/Sekiph82/FormuLab/issues/7) | 10 | 0 | 0 | 10 |
| FVL-07 | [#8](https://github.com/Sekiph82/FormuLab/issues/8) | 16 | 0 | 0 | 16 |
| FVL-08 | [#9](https://github.com/Sekiph82/FormuLab/issues/9) | 8 | 0 | 0 | 8 |
| FVL-09 | [#10](https://github.com/Sekiph82/FormuLab/issues/10) | 10 | 0 | 0 | 10 |
| FVL-10 | [#11](https://github.com/Sekiph82/FormuLab/issues/11) | 10 | 0 | 0 | 10 |
| FVL-11 | [#12](https://github.com/Sekiph82/FormuLab/issues/12) | 14 | 0 | 0 | 14 |
| **Total** | milestone [#1](https://github.com/Sekiph82/FormuLab/milestone/1) | **171** | **88** | **0** | **83** |

Overall: **81 / 171 tasks completed (47.4%)**. FVL-04.013-.018 (External
Source Connector Contract, Generic File Connector, Source Schema
Discovery, Mapping Profile Model, External ID Crosswalk Registry,
Transformation/Unit/Enum Mapping) all COMPLETED 2026-08-19, bringing
FVL-04 to 18/26 — the new enterprise external-source connector layer's
foundation (contract through transformation, plus two independent
end-to-end customer fixtures proving the whole chain through real Data
Exchange commit) is now fully closed. **HARDENED 2026-08-19 (Session 6)**
after an independent repository-level review found real gaps (a single
unit-conversion authority now exists where two would-be-duplicate tables
lived; mapping-profile version immutability is now storage-enforced, not
merely application-layer discipline; the end-to-end closure tests now use
real Data Exchange reference resolution instead of an unconditional
stub; impossible calendar dates and malformed decimal grouping are now
rejected; external-ID identity evidence no longer treats a unique display
name as authority) — see each task's own `**HARDENING**` row and
`docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`'s own Hardening
section for full detail. Task counts are unchanged by this hardening pass
(re-closing already-COMPLETED tasks, not new completions). **Stale as of
Session 10/11 (2026-08-20)**: FVL-04.019-.025 were subsequently completed,
independently audited, found to have real gaps below their original
acceptance, and genuinely re-hardened and re-closed — see each task's own
row above for the exact correction; FVL-04 is now 25/26, with FVL-04.026
correctly NOT STARTED (per the governing brief's own explicit instruction
not to begin it in a hardening session). The **Total** row at the top of
this file already reflects the current 88/171 count. FVL-04.005-.012 (Specifications,
Price History, Inventory, Exchange Rates, Process Parameters, Regulatory
rule/evidence integrity, template-registry consolidation, and real
sample-file acceptance) all COMPLETED 2026-08-19, closing the original
canonical/template-based Data Exchange onboarding block
(FVL-04.001-.012). FVL-01, FVL-02, and FVL-03
are now the fully closed packages (100%, 21/21, 24/24, and 18/18 —
FVL-03 closed 2026-08-18 after FVL-03.001-.012, Material Master through
final single-authority integration acceptance). FVL-04 grew from 12 to
26 tasks on 2026-08-18 —
approved scope expansion adding enterprise external-source connector/
mapping/crosswalk onboarding (FVL-04.013-.025) and a human-readable
literature/formulation artifact naming convention (FVL-04.026).

---

## FVL-01 — Phase 14 Closure Baseline

Freeze the completed Phase 14 implementation as the baseline the remaining
v1 work builds on. Audited directly against source, tests, and the latest
external log
(`C:\Users\sekip\Desktop\FormuLab-Phase14-Literature-Formulation-Intelligence-Log.md`)
on 2026-08-17. Any regression discovered later attaches to the task that
exposes it — Phase 14 is not redone to generate work.

**Existing dependency / baseline capability**: none — this package *is* the
baseline every later package depends on.

| Task ID | Title | Status |
|---|---|---|
| FVL-01.001 | Zero-LLM formulation generation (no provider/model/api_key/llm_call in `pipeline.run()`) | COMPLETED |
| FVL-01.002 | Deterministic requirement parsing (`engine.parse_requirements`, controlled vocabulary, scent-character extraction) | COMPLETED |
| FVL-01.003 | Hybrid literature discovery (OpenAlex/OpenAIRE/Europe PMC/Crossref/DOAJ, Unpaywall OA resolution) | COMPLETED |
| FVL-01.004 | CanonicalPaper dedup/provenance (`canonical_paper.py`, `provenance_sources`) | COMPLETED |
| FVL-01.005 | Full-text acquisition gate (`literature_cache.gather`, `discovery_stats.json::full_text_gate_met`) | COMPLETED — policy corrected 2026-08-17, see FVL-01.005 note below |
| FVL-01.006 | Evidence extraction / A-E classes (`evidence.py`) | COMPLETED |
| FVL-01.007 | Concentration evidence (six-tier resolution hierarchy, plausibility gate) | COMPLETED |
| FVL-01.008 | Multi-alternative architecture generation (historical fixed-3, diversity pressure) | COMPLETED — superseded going forward by dynamic 3–7, see FVL-02 |
| FVL-01.009 | Manufacturing Procedure generation | COMPLETED |
| FVL-01.010 | Critical Parameters (target vs. hard limit distinction) | COMPLETED |
| FVL-01.011 | Equipment derivation + availability matching | COMPLETED |
| FVL-01.012 | Deterministic Safety intelligence (`safety.py`) | COMPLETED |
| FVL-01.013 | Deterministic Regulatory intelligence (`regulatory.py`) | COMPLETED |
| FVL-01.014 | Decision traceability (`traceability.py` + UI) | COMPLETED |
| FVL-01.015 | Structured evidence-gap analysis | COMPLETED |
| FVL-01.016 | Evidence & Sources UI (fixed columns, expandable rows, Evidence Class/Records) | COMPLETED |
| FVL-01.017 | Dedicated report generation (`formulationReport.ts`) | COMPLETED |
| FVL-01.018 | Historical session compatibility (pre-Session-6 cards degrade safely) | COMPLETED |
| FVL-01.019 | Full regression suite (pytest/cargo/vitest) green | COMPLETED |
| FVL-01.020 | Fresh desktop build + shortcut verification | COMPLETED |
| FVL-01.021 | GitHub commit/push state (`8bfc11b`, pushed) | COMPLETED |

**FVL-01.005 note (2026-08-17, same-day correction)**: the recovery round
that closed this package added a hard `< 15 full texts → zero formulas`
gate. A same-day follow-up correction replaced that with the three-state
`full`/`partial`/`insufficient` policy — `provenance.py::
RESEARCH_FULL_TEXT_TARGET = 15`, `RESEARCH_FULL_TEXT_MINIMUM = 10`, the one
authoritative source of truth every other module reads. `full` (≥15) is
normal generation (`status: "ok"`); `partial` (10–14) still generates real
formulas with the shortfall visibly disclosed (`status:
"ok_partial_research"`, a page-level notice, an `insufficient_full_text`
evidence gap, and the Download Report); `insufficient` (<10) still blocks
(`status: "research_corpus_incomplete"`, zero cards). This is a bug-fix/
behavior correction to an already-COMPLETED FVL-01 task, not new v1 scope,
per the scope-change policy §1 rule 3.

Regression: `python -m pytest runtime/pipeline -q` — 326/326 (offline,
deterministic tests at 15/15 full, 14/15 partial, 10/15 partial, 9/15
insufficient, 0/15 insufficient, plus mass-balance-under-partial-corpus and
persistence-round-trip checks). Frontend: `pnpm vitest run` — 138
files/1252 tests. Rust: `cargo test --release` — 342/342. `tsc`/ESLint
clean. Live acceptance: two real disposable-library network runs (hand
soap w/ rosemary scent → 9/15; sulfate-free anti-dandruff shampoo → 8/15)
both landed below the minimum and were correctly, honestly blocked — cold
disposable-cache full-text acquisition for these narrow queries continues
to land under 10 within this round's time budget (consistent with prior
sessions' own findings), so the live acceptance exercised the
`insufficient` path for real while the `partial`-generates-formulas path
is proven by the deterministic offline suite above.

**Verification evidence**: `python -m pytest runtime/pipeline -q` — 320/320
(pre-correction baseline; see FVL-01.005 note for the corrected count).
Rust: `cargo check --release` clean, `cargo test --release` — 342/342.
Frontend: `pnpm tsc --noEmit` clean, ESLint clean, `pnpm vitest run` — 138
files/1248 tests. `git diff --check` clean. Real live acceptance: two
disposable network requests (hand soap w/ rosemary scent; sulfate-free
anti-dandruff shampoo) — commit `8bfc11b04142fa30c623c37ca8d7b01d58d0797b`.
**Commit**: `8bfc11b04142fa30c623c37ca8d7b01d58d0797b`. **Completed**: 2026-08-17.

---

## FVL-02 — Dynamic 3–7 Formula Alternatives — CLOSED (24/24, 2026-08-17)

Purpose: implement the frozen requirement in `FORMULAB_V1_FINAL_SCOPE.md`
§1. Must never fabricate alternatives to hit a requested count.

**Existing dependency / baseline capability**: fixed-3 diversity pressure
(`strategy.diversity_report`, `engine.build_formula_for_strategy`,
`avoid_major_role_keys`) from FVL-01.008 — generalized, not rebuilt.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-02.001 | Define request/data contract: `requestedFormulaCount` (or equivalent) on `FormulationBrief`, min 3 / max 7 / default 3 | — | YES | COMPLETED |
| FVL-02.002 | Python: validate/clamp requested count in `pipeline.run()` (reject/clamp policy for <3 and >7, documented) | FVL-02.001 | YES | COMPLETED |
| FVL-02.003 | Generalize `strategy.derive_strategies()` from fixed `n=3` to requested N (reuse existing strategy library only, no new categories) | FVL-02.002 | YES | COMPLETED |
| FVL-02.004 | Generalize deterministic solver loop (`pipeline.py` per-strategy loop) to N cards instead of hardcoded 3 | FVL-02.003 | YES | COMPLETED |
| FVL-02.005 | Generalize cross-formula diversity pressure (`avoid_major_role_keys`) across N versions | FVL-02.004 | YES | COMPLETED |
| FVL-02.006 | Generalize `strategy.diversity_report()`/`distinct_architecture_count` to operate over N alternatives, not assume 3 | FVL-02.005 | YES | COMPLETED |
| FVL-02.007 | Architecture-uniqueness enforcement for N (never pad with a near-duplicate to hit count) | FVL-02.006 | YES | COMPLETED |
| FVL-02.008 | Insufficient-defensible-alternatives behavior: return M honestly when M < requested N (M ≥ 3 normal case) | FVL-02.006 | YES | COMPLETED |
| FVL-02.009 | Below-3-defensible-alternatives behavior: mark result incomplete/insufficient rather than fabricate | FVL-02.008 | YES | COMPLETED — new `engine.FORMULA_ALTERNATIVES_SUFFICIENT`/`FORMULA_ALTERNATIVES_INSUFFICIENT` constants and a top-level `formula_alternatives_status` field on `pipeline.run()`'s return, independent of `status` (research-corpus completeness) — real alternatives always returned as-is, never discarded/padded. 8 new tests (`test_formula_alternatives_status.py`), including both signals held simultaneously (partial corpus + below-minimum count) without either overwriting the other. Note: under the current strategy library `actual_formula_count < 3` is not reachable through genuine strategy scarcity (`balanced` + one of `cost_optimized`/`premium_sensory` + the unconditional `max_performance` fallback jointly guarantee 3) — tests prove the signal via a real `strategy.derive_strategies()` output truncation, not fabrication. |
| FVL-02.010 | `formulaVersionId`/card collection refactor: remove hardcoded v1/v2/v3 branching wherever it exists in `pipeline.py`/`engine.py` | FVL-02.004 | YES | COMPLETED |
| FVL-02.011 | Session persistence: `cards.json`/session directory representation supports N cards (3–7) | FVL-02.010 | YES | COMPLETED |
| FVL-02.012 | Rust bridge (`formulation_v2.rs`): remove any 3-card assumption, pass through N cards generically | FVL-02.011 | YES | COMPLETED — was already a generic `serde_json::Value` passthrough with no fixed struct/enum; proven directly with a new 7-card round-trip test (`read_cards_round_trips_all_seven_alternatives`) |
| FVL-02.013 | TypeScript types (`formulationV2.ts`): `cards: FormulationCard[]` already generic — audit and fix any `[0]`/`[1]`/`[2]`-indexed assumption | FVL-02.012 | YES | COMPLETED |
| FVL-02.014 | Frontend: dynamic version selector (replaces implicit V1/V2/V3 tab set) driven by real returned card count | FVL-02.013 | YES | COMPLETED |
| FVL-02.015 | Frontend: responsive handling of 3–7 version selector entries (no layout break, no giant side-by-side table — preserve one-formula-at-a-time UX) | FVL-02.014 | YES | COMPLETED — `VersionCards` switches from a fixed 3-column grid to a horizontally scrollable strip (real min-width per card) once `cards.length > 3` |
| FVL-02.016 | Frontend: version switching + selected-ingredient reset generalized to N | FVL-02.014 | YES | COMPLETED |
| FVL-02.017 | Frontend: every version-scoped tab (Manufacturing, Critical Parameters, Equipment, Safety, Regulatory, Evidence & Sources, Alternatives, Summary) reads the active card generically, not `card[0..2]` | FVL-02.014 | YES | COMPLETED |
| FVL-02.018 | Frontend: Download Report generalized to N formulas (loop, not V1/V2/V3 literals) in `formulationReport.ts` | FVL-02.011 | YES | COMPLETED |
| FVL-02.019 | Backward compatibility: existing 3-version historical sessions still open and render correctly | FVL-02.017 | YES | COMPLETED |
| FVL-02.020 | Tests: pytest parametrized for N = 3, 4, 5, 6, 7 (strategy count, diversity, persistence) | FVL-02.009 | YES | COMPLETED — `test_acceptance_formula_count.py::RequestedCountParametrizedTests` |
| FVL-02.021 | Tests: request > 7 clamped/rejected per FVL-02.002 policy; request < 3 handled per same policy | FVL-02.002 | YES | COMPLETED — Acceptance Case E |
| FVL-02.022 | Tests: fewer-than-requested scientifically-defensible case (e.g. request 5, only 4 defensible) | FVL-02.008 | YES | COMPLETED — Acceptance Case D |
| FVL-02.023 | Tests: frontend Vitest for dynamic selector at N=3 and N=7, backward-compat session render | FVL-02.019 | YES | COMPLETED — `FormulationResultPage.test.tsx`'s new "dynamic 3-7 version selector" describe block (N=3..7 parametrized + switching + single-active-at-a-time) |
| FVL-02.024 | Real, disposable, zero-LLM network acceptance test at a non-default N (e.g. 5) | FVL-02.020 | YES | COMPLETED — Acceptance Cases A/B (real anti-dandruff PDF, N=5) |

---

## FVL-03 — Unified Formulation Pipeline ↔ Existing FormuLab Engines

Integrate the Phase 14 deterministic pipeline with already-existing FormuLab
platform capabilities. Do not build replacements.

**Single-authority rule applies to every task below** (see
`docs/FORMULAB_V1_FINAL_SCOPE.md`'s "Single-authority principle" and the
authoritative domain map + legacy retirement matrix in
`docs/FVL03_PLATFORM_INTEGRATION_ARCHITECTURE.md`, both added 2026-08-18 as
an architecture correction — no new scope, only hardened wording). Every
subtask below must reuse/call/extend the cited existing engine; a
Python-side adapter transports and reshapes data only, it never becomes a
second authority for that domain's business decision.

**Existing dependency / baseline capability** (verified present in
`docs/architecture/IMPLEMENTATION_STATUS.md`, and re-confirmed by code
during the 2026-08-18 architecture-correction audit): Cost Engine
(`packages/shared/src/engine/cost.ts`), Advanced Optimizer
(`apps/desktop/src-tauri/src/formulation_advanced.rs` /
`runtime/formulation/advanced_optimizer.py`), material substitution
(`packages/shared/src/engine/substitution.ts`) / system substitution
(`packages/shared/src/engine/systemSubstitution.ts`), Compatibility Engine
(`packages/shared/src/engine/compatibility.ts`), Safety Engine
(`packages/shared/src/engine/safety.ts`), Kenya/EAC Regulatory Engine
(`packages/shared/src/engine/regulatoryRules.ts` +
`regulatoryClassification.ts`), Material Master + supplier records + price
history (`packages/shared/src/schemas/materials.ts`,
`apps/desktop/src-tauri/src/masterdata.rs`, `data/master/*.json`).

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-03.001 | Audit exact integration seam: Material Master ↔ `engine.build_candidate_pool()` (what's read today via `materials_dir`, what's missing) | FVL-01 | YES | COMPLETED (2026-08-18) — full audit in `docs/FVL03_PLATFORM_INTEGRATION_ARCHITECTURE.md`. Confirmed by code: `build_candidate_pool()` consumes a SECOND, legacy material representation (`runtime/pipeline/materials.py`'s flat `<materials_dir>/materials.json`, populated by the live `MaterialsCard.tsx` CSV-import path), never the canonical `data/master/materials.json` (`masterdata.rs`) the real Materials screen uses. Identity mismatch confirmed: pool keys on normalized INCI/name text, never `RawMaterial.code`. `resolve_concentration()`'s own Tier 4 (supplier recommended range) is dead code on this path — proven end-to-end with a real CSV import (`test_material_master_seam.py`, 4 tests). Cost Engine boundary documented (`packages/shared/src/engine/cost.ts::costFormula()`, keyed on `materialCode`) — confirmed NOT called from the generation path; `materials.py::cost_formula()` is a separate, unrelated reimplementation. No production code changed — audit-only, per FVL-03.001's own scope. |
| FVL-03.002 | Canonical Material Master (`data/master/materials.json` + supplier-link + price-history collections, via `masterdata.rs`) reaches `engine.build_candidate_pool()` through a shape-only Python adapter. `RawMaterial.code` carried end-to-end as real identity, in addition to (never instead of) existing INCI/name text matching. Adapter transports/reshapes only — it must not own supplier or price business logic (single-authority rule). | FVL-03.001 | YES | COMPLETED (2026-08-18) — new `runtime/pipeline/master_materials_adapter.py` reads `data/master/{materials,material_suppliers,suppliers,material_prices}.json` directly (bare canonical arrays, no new storage), filters `active`, carries `code`/`material_code` as real identity through `IngredientCandidate`/`SolvedIngredient`/`traceability.selected_event`'s `source_ids`/the rendered formula ingredient (`material_code` field). Makes Tier 4 (`recommended_min_pct`/`recommended_max_pct`) live end-to-end (previously proven dead code by FVL-03.001) and adds a single-implementation-point `technical_max_pct` hard-ceiling clamp (`resolve_concentration()`'s new wrapper, `ConcentrationResolution.technical_max_clamped`). Rust: `formulation_v2.rs`'s `materials_dir` now points at `data/master` (was `data`), new adapter registered in `materialize_pipeline()`'s embedded-file list (verified by reproducing the exact materialized set in a disposable temp dir and importing `pipeline` cleanly — the same class of bug FVL-02.009 found and fixed). Single-authority boundary held: no `price`/`currency` key is ever set by the adapter (`material_price_refs` passed through raw/unfiltered instead — proven by `test_no_price_key_is_ever_set...`); supplier identity kept as the full `material_supplier_refs` set, a `supplier` display string surfacing only when the canonical `preferred` field is already unambiguous (never a new ranking rule). `runtime/pipeline/materials.py`/`materials.rs`'s legacy CSV-import commands untouched, out of scope. `python -m pytest runtime/pipeline -q`: 393 passed, 5 subtests (15 new: `test_master_materials_adapter.py` 11, 3 `technical_max_pct` clamp tests, 1 new end-to-end Tier-4 test; 2 existing tests extended in place, not counted as new). `cargo test masterdata:: formulation_v2::`: 28/28 passing. `cargo check`: clean. |
| FVL-03.003 | Cost-oriented formulation behavior calls the EXISTING Cost Engine (`packages/shared/src/engine/cost.ts::costFormula()`/`buildCostSnapshot()`) for any real price, landed-cost, or exchange-rate result. `runtime/pipeline/materials.py::cost_formula()` is legacy and is retired or bypassed for the generation path. No Python reimplementation of price selection, landed cost, or FX conversion. | FVL-03.002 | YES | COMPLETED (2026-08-18) — audit confirmed Python cannot call `cost.ts` at all (`run_cli.py` is a one-shot subprocess); real cost is computed client-side, post-generation, via new `apps/desktop/src/lib/generatedFormulaCost.ts::costGeneratedFormula()` (thin wrapper over `buildCostSnapshot()`, no business logic — proven identical to a direct call by test). `linesFromGeneratedFormula()` now carries `material_code` (FVL-03.002's own field) into `FormulationLine.materialCode`, the exact key `costFormula`/`priceFor` join on — never text-similarity matching (proven by test: a same-name decoy material with a price is NOT matched). Wired into both UIs (`CostingPanel.tsx` old `/live`, `FormulationResultPage.tsx` new result UI — 3 prior hardcoded placeholders replaced with real cost + a "cheapest among valid alternatives" indicator via new pure `costComparison.ts::pickCheapestValidVersion()`, which excludes invalid-`formula_state` and incomplete-cost candidates from ever winning). Legacy path DELETED (not bypassed): `materials.py::cost_formula()`/`render_costing_markdown()`, `materials_cli.py`'s `"cost"` action, `materials.rs::cost_formulation`, `formulationV2.ts::costFormulation()`/`CostSheet`/`CostLine` — confirmed zero remaining callers before deletion. `python -m pytest runtime/pipeline -q`: 386/386 (7 obsolete `CostingTests` removed). `cargo check`+`cargo test`: 345/345. `pnpm --filter @formulab/shared test`: 1302/1302 (`cost.test.ts` untouched, re-verified). `pnpm --filter @formulab/desktop test`: 1287/1287 (19 new). `typecheck`/`lint`: clean. |
| FVL-03.004 | Inventory feasibility consumes canonical `InventoryRecord` collections and existing availability semantics directly (missing data stays missing, never assumed available) — no copied stock rules, no second availability model. | FVL-03.001 | NO | COMPLETED (2026-08-18) — new `packages/shared/src/engine/inventoryAvailability.ts::evaluateMaterialAvailability()` is the one canonical usable-quantity derivation (`quantity − reservedQuantity`, restricted to lots that are `!quarantined && released` and not expired — the only unambiguous schema-defined facts; `coaStatus` deliberately not gated on). Client-side, read-only, mirrors FVL-03.003 exactly (Python cannot call `packages/shared` — confirmed by audit; `master_materials_adapter.py` NOT extended to read inventory, decided with the user). New `apps/desktop/src/lib/generatedFormulaInventory.ts::evaluateGeneratedFormulaInventory()` joins by `material_code` only (proven immune to a same-display-name decoy via test), computes required quantity from the same numeric `batchKg` control FVL-03.003 already lifted to `FormulationResultPage.tsx` (never the free-text `estimatedBatchSize` brief field, confirmed purely decorative). Rolls up to formula-level FEASIBLE/INFEASIBLE/UNKNOWN. "Prefer a feasible candidate" satisfied at the version level — new `apps/desktop/src/lib/inventoryComparison.ts::pickMostInventoryFeasibleVersion()`, the same pattern as `pickCheapestValidVersion` for cost — never by mutating `engine.py`'s per-role candidate loop; Python stays entirely inventory-blind. Cost and inventory proven to stay separate dimensions (joint test: a real cost total and an independently-computed INFEASIBLE inventory state coexist). Wired into the new result UI only (Summary tab + `VersionSummaryCard` badge) — `CostingPanel.tsx` deliberately not extended (no multi-version context). Read-only confirmed by grep: no `upsertRecords("inventory", ...)` call exists anywhere in the changed files. `pnpm --filter @formulab/shared test`: 1311/1311 (9 new). `pnpm --filter @formulab/desktop test`: 1303/1303 (16 new). `typecheck`/`lint`: clean. `python -m pytest runtime/pipeline -q`: 386/386 (unchanged — Python untouched except a non-functional doc comment on the legacy, confirmed-unused `stock` field). `cargo check`: clean (unchanged — no Rust edits). |
| FVL-03.005 | Existing Advanced Optimizer (`apps/desktop/src-tauri/src/formulation_advanced.rs` / `runtime/formulation/advanced_optimizer.py`) used as an optional post-generation refinement of a selected alternative — no new solver, and not a merge into `engine.py`'s deterministic candidate-generation logic (different responsibility, both legitimately exist). | FVL-03.003 | NO | COMPLETED (2026-08-18) — audit confirmed the Advanced Optimizer (`advanced_optimizer.py`, 1732-line real MILP/PuLP solver, distinct and untouched vs. `formulation_core.py`'s simple LP), its Rust bridge, and its full result/persistence schema set (`packages/shared/src/schemas/optimization.ts`) are already single-authority-correct — real `materialCode` identity, caller-computed compatibility/safety risk (never re-derived by the solver), honest `stock`/`reservedStock`/`availableStock`, existing `AdvancedOptimizerPanel.tsx`/`OptimizationPage.tsx` UI already project-bound and already wired to canonical cost/inventory/materials. Zero engine/schema/solver/Rust changes made. Sole gap: `formulationProblemSchema.projectId`/`productFamilyId` are non-optional but a generated AI session card has no project association — resolved (decided with the user via AskUserQuestion, "require save-first" over "old-UI-only") with new pure `apps/desktop/src/lib/promoteGeneratedFormula.ts::buildPromotedFormulation()`, which builds a real `Formulation`/`FormulationVersion` from the card's own real data using the exact existing `newFormulation()`/`newVersion()`/`linesFromGeneratedFormula()` helpers (zero new persistence shape; `materialCode` carried through unchanged from FVL-03.002/.003). New "Optimize / Refine" quick action on `FormulationResultPage.tsx` (`SlidersHorizontal` icon, `formulationResult.quickActions.optimize`/`optimizing` i18n keys, all 8 locales) calls `saveFormulation()`+`saveFormulationVersion()` once per version (cached in-memory per visit to avoid duplicate `Formulation` records on repeat clicks), then navigates into the existing, unmodified `/optimization?project=<id>` workflow. Source session/cards are never written to — confirmed by diff review (no `session.*` mutation in the changed code, only reads of `session.brief`/`session.id`) and by construction (`buildPromotedFormulation` is pure, makes no Tauri/network call, proven by test). No new optimizer UI/dashboard; `/live`'s own optimizer path (`FormulasPage.tsx`'s Optimizer tab) untouched; zero-LLM, zero-substitution, all hard constraints (exclusions, `technicalMaxPercent`, mass balance) inherited unchanged from the existing panel — none of this task's new code touches constraint/objective logic. Acceptance A/B/C/D/E/F/G/H/J inherited as regression from the existing, unmodified `AdvancedOptimizerPanel`/its own test suite (re-verified green, not re-proven); this task's own narrow proof burden — promotion correctness, read-only w.r.t. the session, honest `"general"` fallback when brief category is empty, never a fabricated specific category — covered by new `promoteGeneratedFormula.test.ts` (5/5 passing). `pnpm --filter @formulab/desktop test`: 1308/1308 across 145 files (5 new; `AdvancedOptimizerPanel.test.tsx`/`OptimizationPage.test.tsx` unmodified and green). `typecheck`/`lint`: clean. `packages/shared`, `runtime/formulation`, `src-tauri/src/formulation*` all confirmed untouched by diff (no Python/Rust/shared sanity re-run needed). |
| FVL-03.006 | Existing material substitution engine (`packages/shared/src/engine/substitution.ts`) used for an ingredient the candidate pool cannot resolve — no pipeline-local duplicate substitution scoring. | FVL-03.001 | NO | COMPLETED (2026-08-18) — audit confirmed the one-to-one Material Substitution Engine (`packages/shared/src/schemas/substitution.ts` + `engine/substitution.ts`, `docs/MATERIAL_SUBSTITUTION.md`) is already single-authority-correct: deterministic scoring over 15 real dimensions traced to real material/price/inventory/supplier/compatibility/safety data (never name similarity), `missingData: true` never defaulted to a perfect match, real `materialCode` identity throughout, and an already-real, already-tested UI (`SubstitutionDialog`/`SubstitutionPanel.tsx`, mounted in both `/live` `FormulasPage.tsx` and the project-bound `/formulation` `FormulationPage.tsx`) that persists an immutable `substitution_runs` record before ever touching the WORKING DRAFT (never the saved version) — confirmed via `useFormulationWorkspace.ts::onApplySubstitution`. Zero engine/schema/scoring/Rust/Python changes made. Trigger boundary: new pure `apps/desktop/src/lib/generatedFormulaInventory.ts::shouldOfferSubstitution()` returns true only for the two named cases — (A) an ingredient with no resolvable `materialCode` at all, or (B) a resolved ingredient whose FVL-03.004 inventory state is definitively `insufficient` — and explicitly false for every other UNKNOWN (no inventory record, mixed units, unusable batch size), matching the "UNKNOWN is missing data, never automatic unavailability" requirement (5/5 new predicate tests + existing Acceptance A-C fixtures). Reused, not reinvented, the case-A "unresolved source" path — `SubstitutionPanel.tsx` already falls back to `line.materialId ?? line.id` / `line.materialCode ?? ""` for any line without a resolved material, pre-existing behavior confirmed by audit, never a new source-identity mechanism. New "Find substitute" entry point added only to the existing `InventoryFeasibilitySummary` component (read-only display, FVL-03.004) — a small button per flagged ingredient that promotes the selected generated version via the existing `promoteGeneratedFormula.ts` (FVL-03.005's same promotion seam, now caching the full `{formulation, version}` pair so both the Optimizer and Substitution entry points share one promoted project per version, never creating duplicates), then navigates to `/formulation?project=<id>&substituteLine=<lineId>` — a new one-shot query-param handoff in `FormulationPage.tsx`, exactly mirroring its own pre-existing `focusLine` pattern, that opens the existing, completely unmodified `SubstitutionDialog` for that exact promoted line (plus a defensive existence-guard so a stale/bad line id can never render a broken dialog). No new substitution dashboard; no second scoring function; no system-substitution work pulled forward (FVL-03.007 untouched — `systemSubstitution.ts`/`generateSystemCandidates` never referenced by any new code this session); source session/cards never mutated — confirmed by diff review (only `session.brief`/`session.id`/`card` reads). Acceptance A/D/E/F/G/H/I/J inherited as regression from the existing, unmodified engine/UI's own test suite (`SubstitutionPanel.test.tsx`, re-verified green); Acceptance B/C are this task's own new proof burden, covered by the new predicate tests plus a new `InventoryFeasibilitySummary.test.tsx` (7 tests: button shown only for A/B triggers, never for a generic UNKNOWN or a fully available line, click reports the correct ingredient index, no button at all without a wired callback, only the in-flight row shows a busy/disabled state). `pnpm --filter @formulab/desktop test`: 1321/1321 across 146 files (13 new). `typecheck`/`lint`: clean. `packages/shared`, `runtime/formulation`, `runtime/pipeline`, `src-tauri/src/formulation*` all confirmed untouched by diff (no Python/Rust/shared sanity re-run needed). Zero-LLM intact. |
| FVL-03.007 | Existing system substitution engine (`packages/shared/src/engine/systemSubstitution.ts`, which itself routes candidates through the Advanced Optimizer rather than solving independently) used at the formula level where applicable — no parallel system-substitution logic. | FVL-03.006 | NO | COMPLETED (2026-08-18) — audit confirmed system substitution is already single-authority-correct and already fully implemented, not merely documented: `packages/shared/src/engine/systemSubstitution.ts` (`generateSystemCandidates`, `buildSystemSubstitutionProblem`, `scoreSystemResult`), `packages/shared/src/schemas/substitution.ts` (`systemCandidateLimitsSchema`, `rejectedSystemCandidateSchema`, the `lineIds`/`materialIds`/`preserveFunctions`/... fields on `substitutionRequestSchema`), full spec in `docs/SYSTEM_SUBSTITUTION.md`. Confirmed "system" has NO fixed chemistry taxonomy anywhere in this platform (no hardcoded surfactant/preservative/chelation/etc. categories) — a "system" is whichever ≥2 formula lines a human selects; membership is 100% human-identified via checkboxes, never auto-detected, matching the task's own "do not fabricate membership" requirement by construction. Candidate generation never uses name similarity (function-coverage + stock/supplier-approval/Kenya-local filters only); every candidate combination is routed through the REAL Advanced Optimizer (`buildSystemSubstitutionProblem`→`runtime/formulation/advanced_optimizer.py`, never a proportional-scaling shortcut); a combination that fails to cover a preserved function is recorded as rejected (`missing_required_function`), never silently offered partial. Scoring reads directly from the real optimizer result (feasibility ranking, soft-constraint violation count, real cost delta, `compatibility_risk`/`safety_risk` when the caller wired them, `missingData: true` otherwise — never assumed perfect/zero). Applying persists BOTH the underlying `OptimizationRun` and an immutable `SubstitutionRun` (`isSystem: true`, `systemMaterialIds`, `optimizationRunCode` pointer) before ever touching the working DRAFT (never the saved `FormulationVersion`) — same non-destructive lifecycle FVL-03.006 already confirmed for one-to-one substitution. The existing `SubstitutionDialog`/`SubstitutionPanel.tsx` (mounted in both `/live` and `/formulation`) already implements this ENTIRE workflow in its system-mode section (check 2+ lines → generate → evaluate → apply). **Zero engine/schema/scoring/Rust/Python changes made.** Sole gap closed: a generated AI session card has no real project (same `substitutionRequestSchema.projectId`/`formulaVersionId` requirement FVL-03.006 already resolved) and the existing dialog had no way to open pre-seeded with 2+ lines already checked. New optional `initialExtraLineIds?: string[]` prop added to `SubstitutionDialog` (`SubstitutionPanel.tsx`) — seeds `selectedLineIds` beyond the required anchor `line` on open only, filtered defensively against real `allLines` so a stale/bogus id can never masquerade as a second system member; the human can still freely add/remove lines afterward, exactly as before — no behavior change to anyone who doesn't pass it. New multi-select UI added only to the existing ingredient table (`FormulaTab` in `FormulationResultPage.tsx`) — a checkbox per row (`stopPropagation`'d so it never also opens the evidence panel) plus a small action bar ("N ingredient(s) selected" / "System substitution", disabled below 2 selections, so a one-material problem can never reach system mode). Clicking it reuses the exact FVL-03.005/.006 promotion seam (`ensurePromoted()`) to get a real `Formulation`/`FormulationVersion`, resolves the selected ingredient indices to that version's own real line ids (same index-alignment guarantee as FVL-03.006), and navigates to `/formulation?project=<id>&substituteLine=<anchor>&systemLines=<rest>` — a new one-shot query-param handoff in `FormulationPage.tsx` (mirrors its own `focusLine`/`substituteLine` pattern) that opens the existing, otherwise-unmodified `SubstitutionDialog` pre-seeded into system mode. Selection state is version-scoped: a real cross-version leak bug was caught by a new test and fixed with a `useEffect` resetting `selectedForSystem` on `card.version` change (Acceptance I). Source session/cards never mutated — confirmed by diff review. No compatibility/safety/regulatory logic added (FVL-03.008/.009/.010 untouched); no fabricated ratios/concentrations (the existing engine's own deterministic optimizer-derived percentages are used as-is, never locally recomputed); no-candidate/infeasible results inherited honestly from the existing dialog. Acceptance A/B/C/D/E/F/G/H/J inherited as regression from the existing, unmodified engine/dialog's own test suite (`SubstitutionPanel.test.tsx`, re-verified green); this task's own new proof burden — `initialExtraLineIds` pre-seeding, defensive bogus-id filtering, multi-select UI correctness, and version scoping (Acceptance I) — covered by 3 new `SubstitutionPanel.test.tsx` tests + 4 new `FormulationResultPage.test.tsx` tests. `pnpm --filter @formulab/desktop test`: 1328/1328 across 146 files (7 new). `typecheck`/`lint`: clean. `packages/shared`, `runtime/formulation`, `runtime/pipeline`, `src-tauri/src/formulation*` all confirmed untouched by diff. Zero-LLM intact. |
| FVL-03.008 | Existing Compatibility Engine (`packages/shared/src/engine/compatibility.ts::evaluateCompatibility`) becomes the authoritative compatibility verdict for generated formulas. `runtime/pipeline/rules.py::validate()`/`derive_constraints()` remain in place — confirmed by the 2026-08-18 audit to implement only generation-REQUEST constraints (excluded ingredients, sulfate-free, requested pH bounds), never chemical/material compatibility logic — so they are not a competing engine. No duplicate compatibility business rules. | FVL-03.001 | YES | COMPLETED (2026-08-18) — audit confirmed `evaluateCompatibility()` (`packages/shared/src/engine/compatibility.ts`, deterministic rule-driven engine, no model in the loop, `docs/COMPATIBILITY_ENGINE.md`) is already the single authority: real `RULE_SEVERITIES` vocabulary (`info`/`warning`/`error`/`blocking`), real materialCode-based identity (`materialFor()`), 20 seed rules covering forbidden/warning combinations, pH-/temperature-/concentration-dependent conditions, missing-data honestly downgraded to `dataIncomplete: true` (never silently safe). Confirmed by direct audit of `optimization.ts::blockingExclusionConstraints` and `SubstitutionPanel.tsx` that ONLY `severity === "blocking"` is a real hard block anywhere in this platform — reused verbatim, not invented. Zero engine/schema/Rust/Python changes. `runtime/pipeline/rules.py` re-confirmed request-constraint-only (zero chemistry/ionic/pH/hlb/cationic/anionic keyword hits). New pure `apps/desktop/src/lib/generatedFormulaCompatibility.ts::evaluateGeneratedFormulaCompatibility(formula, materials, rules, opts)` — read-only, no promotion/persistence needed (`evaluateCompatibility` is pure) unlike FVL-03.005/.006/.007. Rule set is a REQUIRED caller-supplied param (never hardcoded) — new `useCompatibilityRules()` hook loads the LIVE, chemist-editable `compatibility_rules` masterdata collection (same one `CompatibilityPanel.tsx` reads), not a frozen `SEED_COMPATIBILITY_RULES` copy. `formulaState` (`compatible`/`warning`/`blocked`/`unknown`) follows real engine severity semantics exactly; `unknown` is reported (never `compatible`) when zero findings fire AND at least one ingredient has no resolvable `materialCode` — `unresolvedMaterialCount` always surfaced honestly alongside real findings, neither hides the other. Fixed a real pre-existing gap found by this session's own testing: `masterdata.ts::listRecordsSeeded()` threw `"not-desktop"` outside Tauri (a latent bug never previously exercised, since no test rendered a caller of it) — fixed to return `seed` directly outside Tauri, mirroring `listRecords()`'s own established `!isTauri` convention; zero behavior change inside Tauri. `pickCheapestValidVersion()`/`pickMostInventoryFeasibleVersion()` extended with an optional `compatibilities` eligibility-gate parameter — a `"blocked"` version can never be crowned cheapest-valid or most-feasible merely because its price/stock is attractive (task §9); `"warning"`/`"unknown"` never exclude; omitting the parameter preserves prior behavior exactly (proven by test). New thin `GeneratedCompatibilitySummary` presenter (not a second dashboard) wired into the result page's Summary tab and a compatibility row/blocked-banner on `VersionSummaryCard`. Confirmed by audit (not rewritten, since already correct) that the Advanced Optimizer (`blockingExclusionConstraints`), Material Substitution (`SubstitutionPanel.tsx`'s own `evaluateCompatibility` re-run), and System Substitution (`blockingExclusionConstraints` inside `buildSystemBasis()`) all already consume the SAME authoritative engine — disclosed, out-of-scope, pre-existing finding: those three callers pass the hardcoded `SEED_COMPATIBILITY_RULES` constant rather than the live edited collection this task's own new code correctly reads via `useCompatibilityRules()`; not a duplicate-authority violation (same single engine, same call, no second scoring logic anywhere) and out of this task's boundary to retrofit already-closed FVL-03.006/.007 code. No Safety/Regulatory work pulled forward. Acceptance A/B/C/D proven directly by 9 new `generatedFormulaCompatibility.test.ts` tests (disposable fixture rules, per task's own instruction); E/F proven by 4 new `costComparison.test.ts` + 3 new `inventoryComparison.test.ts` tests; G/H/I confirmed by audit/inherited regression (existing `AdvancedOptimizerPanel.tsx`/`SubstitutionPanel.tsx` tests unmodified and green); J proven by a new `FormulationResultPage.test.tsx` test plus 3 more end-to-end wiring tests using REAL seed rules (a genuine `compat-acid-hypochlorite` blocking finding, real version scoping). `pnpm --filter @formulab/desktop test`: 1354/1354 across 148 files (26 new). `typecheck`/`lint`: clean. `packages/shared`, `runtime/formulation`, `runtime/pipeline`, `src-tauri/src/formulation*` all confirmed untouched by diff. Zero-LLM intact. |
| FVL-03.009 | Existing Safety Engine (`packages/shared/src/engine/safety.ts::evaluateSafety`/`classifyProductSafety`) becomes the single authoritative final safety verdict. `runtime/pipeline/safety.py::evaluate_safety()` is a confirmed duplicate — it independently computes its own `overall_status` from its own hazard tables, never consuming the TS engine's result. Its pipeline-local duplicate final-verdict logic is retired or reduced to non-authoritative preprocessing that feeds the authoritative engine — not permanently reconciled as a second, independently-disagreeing verdict authority. | FVL-03.008 | YES | COMPLETED (2026-08-18) — audit confirmed `runtime/pipeline/safety.py::evaluate_safety()` was a real, fully independent second final-verdict authority (its own `_SENSITIZER_CLASS_INGREDIENTS`/`_ALLERGEN_DECLARATION_INGREDIENTS`/`_CORROSIVE_HANDLING_INGREDIENTS`/`_IRRITANT_POWDER_HANDLING_INGREDIENTS`/`_SULFATE_KEYS` name-keyed hazard tables computing its own `overall_status`, never consuming `packages/shared/src/engine/safety.ts`). Resolved by full retirement (Option A, matching FVL-03.003's precedent), not permanent reconciliation: `runtime/pipeline/safety.py` and `runtime/pipeline/test_safety.py` deleted entirely; `pipeline.py` no longer imports `safety`, no longer builds `safety_result`, no longer emits `card["safety"]` or a `safety`-sourced `evidence_gaps` entry; `validation_plan.py::build_validation_plan()` had its `safety_overall` parameter removed, VAL-002 narrowed to the still-live regulatory-only check (Regulatory consolidation itself untouched — FVL-03.010's job); `test_pipeline.py`'s zero-LLM guard now asserts `"safety" not in card`; `test_traceability.py`'s `test_every_safety_finding_has_a_source_or_rule` (which read the now-nonexistent `card["safety"]["findings"]`) removed, the adjacent regulatory provenance test left untouched. The separate, legitimate `classify_target`/`safety_gate` pre-generation AI-request gate in `pipeline.py` (mirrors the TS `PRODUCT_SAFETY_CLASSIFICATIONS` enum by design) was confirmed by audit to be an unrelated responsibility and left completely alone. New client-side integration seam — same pattern as FVL-03.008 — since `evaluateSafety()` is pure/read-only: new `apps/desktop/src/lib/generatedFormulaSafety.ts::evaluateGeneratedFormulaSafety(formula, materials, rules, opts)` computes a `formulaState` (`safe`/`warning`/`blocked`/`unknown`) from real `findings`, with `unknown` reserved for zero-findings-plus-unresolved-materialCode (never silently "safe") and `unresolvedMaterialCount` always surfaced honestly alongside real findings; `blocked` set if and only if any finding has `severity === "blocking"`, reusing the platform's own already-confirmed "only blocking is a real hard block" convention (`optimization.ts::blockingExclusionConstraints`, `SubstitutionPanel.tsx::hasBlockingSafetyFinding`) rather than inventing new severity semantics. `classifyProductSafety` deliberately NOT wired for generated-formula evaluation — a generated session's free-text `brief.category` has no reliable join to a real `ProductFamily.hazardClass` record, and fabricating that join would violate the standing no-fabricated-identity rule (same scope decision FVL-03.008 made for Compatibility's product-domain context). New `useSafetyRules()` hook loads the LIVE `safety_rules` masterdata collection (`listRecordsSeeded`), not a frozen `SEED_SAFETY_RULES` copy. `pickCheapestValidVersion()`/`pickMostInventoryFeasibleVersion()` extended with an optional 4th `safeties` eligibility-gate parameter, independent of and additive to the existing `compatibilities` gate — a safety-`blocked` version can never be crowned cheapest-valid/most-feasible; omitting the parameter preserves prior behavior exactly (proven by test). `FormulationResultPage.tsx` fully rewired off the legacy Python-shaped `card.safety` (`overall_status`/`subject`/`rule_id`/`rationale`) onto the authoritative client-side result: `SafetyTab` rewritten to render the new `GeneratedSafetySummary` presenter; a Safety section added to `SummaryTab` (including the "Readiness" badges block, whose stale `card.safety.overall_status` reference was caught only by a deliberate post-implementation grep audit, not by typecheck/lint/the first test pass); a Safety row + red "blocked" banner added to `VersionSummaryCard`. `formulationReport.ts`'s `versionSection()`/`buildReportHtml()`/`openAndPrintReport()` rewired to accept a `safetyByVersion` map (built from the exact same computed `safeties` array the UI renders) instead of reading the retired `card.safety` JSON — closing the one real split-authority risk in the whole task (the "Download Report" path), with a backward-compatible default `= {}` so no other caller breaks and an honest "not available" (never a fabricated verdict) when no result was computed for a version. `formulationV2.ts`'s `SafetyResult` interface kept (not deleted) but its doc comment now states plainly it is legacy-only, read by zero current code, present solely so a historical session file saved before this retirement still parses without error — the authoritative verdict for every session, old or new, is `evaluateGeneratedFormulaSafety()` recomputed client-side from `card.formula`. Confirmed by audit (not rewritten, since already correct) that the Advanced Optimizer (`blockingExclusionConstraints`) and both Material/System Substitution (`SubstitutionPanel.tsx::hasBlockingSafetyFinding`) already consume the SAME authoritative engine; same disclosed, out-of-scope, pre-existing gap FVL-03.008 already flagged (those three callers pass the hardcoded `SEED_SAFETY_RULES`/`SEED_COMPATIBILITY_RULES` constants rather than the live edited collections) reconfirmed still present and still out of this task's boundary. Compatibility and Safety confirmed to remain separate domains throughout — no merged findings, no shared verdict field, no opaque combined score. Real `materialCode` identity carried through via the shared `ruleConditions.ts` matcher (`matchLines`/`lineMatchesCondition`/`materialFor`), with graceful name-/function-keyword fallback for unresolved lines, never fabricated identity. All 8 i18n locales given new `safety.state.*`/`safety.severity.*`/`safety.notYetAvailable`/`safety.unresolvedMaterialCount`/`safety.findingRequiresHumanReview` keys (English placeholder text in de/es/fr/ja/ko/zh-Hans, real Turkish translations). Historical sessions carrying the legacy `card.safety` JSON confirmed to open without crashing and never surface as current authority — proven by a dedicated rewritten `FormulationResultPage.test.tsx` test against the pre-existing SESSION_V6 fixture, which also surfaced a genuine, non-obvious real-engine fact (the real `SEED_SAFETY_RULES` `safety-flammable-solvent`+`safety-ventilation-reminder` rules legitimately fire on a plain q.s.-to-100% "Water (Aqua)"/function="Solvent" line via their `functionsAny: ["solvent"]` OR-condition, producing a genuine `formulaState: "warning"`, not "unknown"). Acceptance A/B/C/D/G proven by 9 new `generatedFormulaSafety.test.ts` tests plus 7 new `GeneratedSafetySummary.test.tsx` tests (disposable fixture rules); E/F proven by 5 new `costComparison.test.ts` + 3 new `inventoryComparison.test.ts` tests; H/I confirmed by audit/inherited regression (existing `AdvancedOptimizerPanel.tsx`/`SubstitutionPanel.tsx` tests unmodified and green); J/K proven by 4 corrected `FormulationResultPage.test.tsx` tests (two needed `findAllByText`/`toBeGreaterThanOrEqual(2)` fixes since the real Safety and Compatibility engines now legitimately co-fire on the same pre-existing fixtures) plus 3 new `formulationReport.test.ts` tests; L proven by this row's own closure-time grep re-audit (`overall_status`/`evaluate_safety`/`evaluateSafety`/`classifyProductSafety`/`hazard` across `runtime/pipeline`, `runtime/formulation`, `packages/shared/src`, `apps/desktop/src`, `apps/desktop/src-tauri/src` — zero live-code hits outside the one authoritative TS engine and its confirmed-correct callers; the only `card.safety`/`overall_status` matches remaining anywhere are explanatory comments and test fixtures documenting the retirement, plus the unrelated pre-generation `hazardous_lawful_product` request-classification label in `pipeline.py`, confirmed out of scope). `pnpm --filter @formulab/desktop test`: 1381/1381 across 150 files. `typecheck`/`lint`: clean. `python -m pytest runtime/pipeline -q`: 376 passed, 5 subtests passed (down from 386+5 baseline by exactly 10 = 9 deleted `test_safety.py` tests + 1 deleted `test_traceability.py` test). Regulatory consolidation (`regulatory.py`/`regulatoryRules.ts`) confirmed completely untouched — FVL-03.010's own job. Zero-LLM intact; `/live` untouched. |
| FVL-03.010 | Existing Kenya/EAC Regulatory Engine (`packages/shared/src/engine/regulatoryRules.ts::evaluateRegulatory` + `regulatoryClassification.ts`) becomes the single authoritative regulatory verdict per market. `runtime/pipeline/regulatory.py::evaluate_regulatory()` is a confirmed duplicate — its own module docstring describes it as "a direct, faithful port" of the TS rule catalog into an independent second evaluation engine with its own terminal verdict. Its pipeline-local duplicate rule universe/verdict is retired or reduced to non-authoritative preprocessing — not a permanent second rule universe. | FVL-03.009 | YES | COMPLETED (2026-08-18) — audit confirmed `runtime/pipeline/regulatory.py::evaluate_regulatory()` was a real, independently-computing second final-verdict authority (its own `_STATUS_PRECEDENCE` merge producing `overall_status`, never consuming `packages/shared/src/engine/regulatoryRules.ts`) — and, by direct catalog comparison, itself STALE: only 7 of the TS catalog's real 16 seed rules ported. Resolved by full retirement (Option A, matching FVL-03.003/.009's precedent), not permanent reconciliation: `runtime/pipeline/regulatory.py` and `runtime/pipeline/test_regulatory.py` (14 tests) deleted entirely; `pipeline.py` no longer imports `regulatory`, no longer builds `regulatory_result`, no longer emits `card["regulatory"]` or a `regulatory`-sourced `evidence_gaps` entry; `validation_plan.py::build_validation_plan()` had its now-last `regulatory_overall` parameter removed entirely (VAL-002, the last Safety/Regulatory advisory entry, removed outright — the checklist generator is now purely formula-shape-derived); `test_pipeline.py`'s zero-LLM guard now asserts `"regulatory" not in card`; `test_traceability.py`'s `test_every_regulatory_finding_has_a_source_or_rule` removed. `regulatory.py::review_claims()`'s structural claim-vs-composition check (the one genuinely distinct Python capability, with no TS rule-type equivalent) was deliberately retired with the rest rather than selectively kept, since it computed a real claim verdict, not inert preprocessing — flagged for a possible future TS catalog addition instead of ported as a disguised second authority. New client-side integration seam — same pattern as FVL-03.008/.009: new `apps/desktop/src/lib/generatedFormulaRegulatory.ts::evaluateGeneratedFormulaRegulatory(formula, materials, rules, opts)` resolves `brief.market` free text into a real `RegulatoryJurisdiction` via a small wrapper-local alias table (ported directly from `regulatory.py`'s own retired `_MARKET_ALIASES` — legitimate input preprocessing, never a second rule catalog), computes a `formulaState` (`compliant`/`warning`/`blocked`/`unknown`) reusing the engine's own real `REGULATORY_FINDING_STATUSES`/`NON_BLOCKING_FINDING_STATUSES` vocabulary — `blocked` reserved specifically for a real `non_compliant` finding (the literal trigger every acceptance case describes), never for the `missing_data`/`human_review_required` findings a generated, unconfirmed session will almost always carry (which surface honestly as `warning` instead, avoiding a degenerate always-blocked gate); zero findings is never `compliant`, deliberately preserving the exact "sparse coverage ≠ clean" policy the retired module's own docstring stated. `category` deliberately always `"human_review_required"` (the real classifier's own honest uncertainty value, not an invented fallback) — same no-fabricated-identity scope decision FVL-03.008/.009 already made for `classifyProductSafety`. New `useRegulatoryRules()` hook loads the LIVE `regulatory_rules` masterdata collection, not a frozen `SEED_REGULATORY_RULES` copy. `pickCheapestValidVersion()`/`pickMostInventoryFeasibleVersion()` extended with an optional 5th `regulatories` eligibility-gate parameter, independent of and additive to the existing `compatibilities`/`safeties` gates. `FormulationResultPage.tsx` fully rewired off the legacy Python-shaped `card.regulatory` onto the authoritative client-side result (`RegulatoryTab` rewritten around a new `GeneratedRegulatorySummary` presenter showing status AND verification status side by side; Summary tab section; VersionSummaryCard row/banner; the now-fully-dead `statusTone()` helper removed). `formulationReport.ts` rewired to accept a `regulatoryByVersion` map instead of the retired `card.regulatory` JSON, closing the same "Download Report" split-authority risk FVL-03.009 already closed for Safety. `formulationV2.ts`'s legacy `RegulatoryResult`/`RegulatoryFinding`/`ClaimFinding` interfaces kept (not deleted) with doc comments marking them legacy-only. New real wiring (not just an audit finding) into Material Substitution: `SubstitutionPanel.tsx`'s one-to-one candidate scoring had never populated `SubstitutionCandidateInput.regulatoryPermitted` even though `substitution.ts`'s own `regulatory_status` scoring dimension already existed/was already tested — now wired per candidate via `evaluateRegulatory()` against the project's own real `formulation.targetMarkets[0]`, with a locally-tracked (never persisted, never schema-extending) `noBlockingOnly` filter exclusion for a real `non_compliant` result. Advanced Optimizer/System Substitution confirmed by audit to be a genuine, pre-existing, DOCUMENTED "not yet implemented" boundary (`regulatoryOptimizationPolicySchema.mode` hard-locked to `"not_available"`, the solver honestly refusing `regulatory_uncertainty` rather than computing it from nothing) — not a duplicate-authority gap to close; only its stale doc comment (predating the Regulatory Engine's own existence) was corrected, zero schema/behavior change, confirmed by the full unchanged-count `packages/shared` test suite. Compatibility and Safety confirmed to remain separate domains throughout. All 8 i18n locales given new `regulatory.generated.*` keys (English placeholder in de/es/fr/ja/ko/zh-Hans, real Turkish translations). Historical sessions carrying the legacy `card.regulatory` JSON confirmed to open without crashing and never surface as current authority — proven by two rewritten `FormulationResultPage.test.tsx` tests against the pre-existing SESSION_V6 fixture (given a real `market: "kenya"` brief field, a legitimate test-data completion), which legitimately produced a real `KE-REG-003` `missing_data` finding via the same real, unmodified seed catalog. Acceptance A/B/C/D proven by 14 new `generatedFormulaRegulatory.test.ts` tests (disposable fixture rules); E/F proven by 5 new `costComparison.test.ts` + 3 new `inventoryComparison.test.ts` tests; G confirmed by audit + a corrected stale doc comment (Advanced Optimizer never fabricates a regulatory constraint); H proven by 2 new `SubstitutionPanel.test.tsx` market-scoping tests plus the underlying `generatedFormulaRegulatory.test.ts` non_compliant-exclusion proof (the real seed catalog cannot itself trigger `non_compliant` for a `human_review_required` category — an honest, disclosed structural limitation, not a bug); I confirmed by audit (System Substitution inherits the same documented Optimizer-level gap, no local clone); J/K proven by rewritten `FormulationResultPage.test.tsx`/`formulationReport.test.ts` tests; L proven by dedicated market-resolution tests (`resolveRegulatoryMarket` never guesses); M proven by this row's own closure-time grep re-audit (zero live-code hits outside the one authoritative TS engine and its confirmed-correct callers). `pnpm --filter @formulab/desktop test`: 1416/1416 across 152 files. `typecheck`/`lint`: clean. `pnpm --filter @formulab/shared test`: 1311/1311 (comment-only edit, unchanged count). `python -m pytest runtime/pipeline -q`: 361 passed, 5 subtests passed (down from 376+5 baseline by exactly 15 = 14 deleted `test_regulatory.py` tests + 1 deleted `test_traceability.py` test). Zero-LLM intact; `/live` untouched. |
| FVL-03.011 | Supplier/material/safety/regulatory/compatibility provenance remains traceable end-to-end, carrying each authoritative engine's real source IDs (`material_code`, rule/verdict references) — extends `traceability.py`'s existing model, does not fork it. | FVL-03.002 | YES | COMPLETED (2026-08-18) — full audit (`traceability.py`, `provenance.py`, `pipeline.py`, `engine.py`, generated-formula structures, every FVL-03.002-.010 domain's own persisted/computed record shape, promotion path, existing UI) confirmed ONE coherent provenance model already exists, correctly NOT forked: `traceability.py`'s `TraceEvent` (real `source_ids`/`rule_id`/`evidence_ids` fields, `material_code` carried since FVL-03.002) owns ingredient selection/rejection decision trace ONLY, by its own explicit, still-valid design ("everything else... already has its own real, structured, authoritative home... a trace event that needed one of those facts REFERENCES it by id/key rather than copying it") — each domain engine (Compatibility/Safety/Regulatory/Cost/Inventory/Substitution/Optimization) owns its OWN real IDs on its OWN real record shape, never duplicated into Python trace_events, confirmed unchanged and correct. Generated→saved lineage (§A13) confirmed ALREADY satisfied, not a gap: `promoteGeneratedFormula.ts::buildPromotedFormulation()` (FVL-03.005) already writes a real, structured back-reference — `changeReason: "Promoted from AI-generated session ${session.id}, ${card.version.toUpperCase()}, for Advanced Optimizer refinement."` — into the EXISTING `FormulationVersion.changeReason` field, answering "which exact generated session/version produced this saved version" without any new persistence system. Scientific/evidence lineage (FVL-03.013-.018) confirmed untouched, not re-audited beyond confirming no new fork. Two real, concrete provenance-VISIBILITY gaps were found and fixed (data existed, was never rendered) — not new business logic, not a new provenance model: (1) `GeneratedSafetySummary.tsx`/`GeneratedCompatibilitySummary.tsx` computed each finding's real `ruleId` all along but used it only as a React `key`, never displaying it — now shown, alongside affected material ids, on every finding row; (2) `formulationReport.ts` ("Download Report") had a Safety section and a Regulatory section (FVL-03.009/.010) but had NEVER had a Compatibility section at all since FVL-03.008 — added, reusing the exact same already-computed `compatibilities` array the UI tab renders, never a new computation. A third gap closed: no ingredient row anywhere in the new result UI ever displayed its own resolved `material_code` at all (`GeneratedIngredient` interface didn't even type the field) — added the field (optional, backward-compatible) and a small honest subtext under each ingredient name showing the real code or an explicit "Unresolved — no canonical material match" disclosure, never a fabricated id. No `traceability_v2.py`/`provenance_engine.py`/second source-reference schema created. Acceptance P1/P2 proven by a new `FormulationResultPage.test.tsx` test (SESSION_V6's genuinely unresolved ingredient shows the honest disclosure, never a fake id) plus the pre-existing decoy-material-immunity tests in every domain's own `generatedFormula*.test.ts`; P3/P4 proven by 2 new tests in `GeneratedSafetySummary.test.tsx`/`GeneratedCompatibilitySummary.test.tsx`; P5/P6 already proven by FVL-03.010's own `generatedFormulaRegulatory.test.ts`; P7 confirmed untouched (FVL-03.013-.018); P8/P9 confirmed by FVL-03.006/.007's own existing `substitution_runs`/`optimization_runs` persistence, unchanged; P10 confirmed by FVL-03.005's own existing `optimization_runs.projectId` linkage; P11 proven by 3 new `formulationReport.test.ts` tests (Compatibility section uses the same computed result the UI uses, discloses unresolved coverage honestly, shows "not available" rather than a fabricated verdict); P12 confirmed — no new session-storage schema field was added, a historical session opens exactly as before. `pnpm --filter @formulab/desktop test`: 1422/1422 across 152 files (6 new: 1 `FormulationResultPage.test.tsx`, 1 `GeneratedSafetySummary.test.tsx`, 1 `GeneratedCompatibilitySummary.test.tsx`, 3 `formulationReport.test.ts`). `typecheck`/`lint`: clean. `python -m pytest runtime/pipeline -q`: 361 passed, 5 subtests passed (unchanged — zero Python files touched this task). `packages/shared` confirmed untouched by this task's own diff (no sanity re-run needed). Zero-LLM intact; `/live` untouched; no new provenance framework; `traceability.py` extended in spirit (confirmed still valid) but not one line changed in code. |
| FVL-03.012 | Integration acceptance proves exactly one authoritative result per domain (material, cost, inventory, compatibility, safety, regulatory, substitution, optimization) with no duplicated business calculation remaining, covering at least one cost-constrained and one substitution-triggered request. | FVL-03.005, FVL-03.007, FVL-03.010 | YES | COMPLETED (2026-08-18) — final authority matrix built in `docs/FVL03_PLATFORM_INTEGRATION_ARCHITECTURE.md` (13 domains: Material/Supplier/Price/Cost/Inventory/Compatibility/Safety/Regulatory/Material Substitution/System Substitution/Advanced Optimizer/Deterministic Generation/Traceability — all PASS). Final repository-wide duplicate-business-logic audit (material/cost/inventory/compatibility/safety/regulatory/substitution/optimization/provenance search terms) found zero category-E ("actual duplicate business authority") hits; every match classified A (authoritative engine)/B (legitimate adapter)/C (separate legitimate responsibility, e.g. `rules.py` request constraints, `strategy.py`'s `regulatory_conservative` strategy-selection concept)/D (dead legacy code, never read). One disclosed, pre-existing, pre-FVL-03 gap re-confirmed rather than ignored: `MaterialsPage.tsx`/`AdvancedOptimizerPanel.tsx`/`SubstitutionPanel.tsx` still compute `quantity − reservedQuantity` inline instead of calling the canonical `evaluateMaterialAvailability()` (FVL-03.004's own disclosed, out-of-scope finding) — explicitly does NOT conflict with the generated-formula integrated workflow this domain's own acceptance targets, so does not block closure; flagged again for a future session. **A real, build-breaking Rust regression was found and fixed by this task's own mandatory `cargo check`**: `formulation_v2.rs::materialize_pipeline()` still `include_str!`'d the two Python files FVL-03.009/.010 deleted (`F_SAFETY`/`F_REGULATORY`, pointing at the now-nonexistent `safety.py`/`regulatory.py`) — those two sessions correctly made no Rust changes and so correctly never ran `cargo check` (their own scope discipline was right), but this left the shipped desktop binary unable to compile at all, undiscovered until this session. Fixed by removing both dead constants and their materialize-list entries; verified end-to-end (not just `cargo check` passing) by reproducing the exact materialized file set in a disposable temp directory — `pipeline.py` imported with zero `ImportError`, and a real deterministic generation ran through it end-to-end (`status: "ok"`, 3 real cards, `"safety"`/`"regulatory"` correctly absent) — the same verification method FVL-02.009 established for the analogous missing-file defect. `cargo test formulation_v2`: 10/10 passing. Cost-constrained acceptance (§B11): new `costComparison.test.ts` "FVL-03.012" block feeds three disposable, realistically-shaped alternatives through the REAL `costGeneratedFormula()`/`buildCostSnapshot()` engine — a real cheaper total is genuinely lower (computed, not asserted), a missing-price version carries a real warning and cannot win, an `invalid_constraint_violation` alternative is never selected even at the lowest raw total. Substitution-triggered acceptance (§B12): new `SubstitutionPanel.test.tsx` "FVL-03.012" block proves no auto-substitution (a human must click Apply), the applied line carries the candidate's real canonical `materialCode`, the run is traceable via a real persisted `substitution_runs` record, source formula never mutated. Cross-domain hard-constraint acceptance (§B13) already proven by the existing three-independent-exclusion-gates tests across `costComparison.test.ts`/`inventoryComparison.test.ts`. Unknown/missing-data honesty (§B14) reconfirmed throughout every domain by the full existing suite. Both UIs confirmed unchanged (§B15); zero-LLM reconfirmed via the full Python suite plus a live materialized-directory generation run (§B16); no real business data touched anywhere this session (§B17). `pnpm --filter @formulab/desktop test`: 1424/1424 across 152 files (2 new). `typecheck`/`lint`: clean. `pnpm --filter @formulab/shared test`: 1311/1311 (untouched). `python -m pytest runtime/pipeline -q`: 361 passed, 5 subtests (untouched). `cargo check`: clean (after the fix). `python scripts/validate_v1_tracker.py`: OK. `git diff --check`: clean. All 23 final FVL-03 completion conditions (§B19) confirmed. **FVL-03 — Unified Formulation Pipeline ↔ Existing FormuLab Engines — COMPLETE (18/18).** |
| FVL-03.013 | Scientific Full-Formulation Extraction — `fulltext.pdf_lines()` (real, standard-library-only, positional PDF text reconstruction) + `scientific_formulation.py`'s deterministic F1..Fn composition-table extractor | FVL-01 | YES | COMPLETED |
| FVL-03.014 | Scientific Formulation Experimental Outcome Linking — `ExperimentalOutcome` records tied to the correct `source_formulation_id`, both row-indexed (RPM/time tables) and F-labeled-row (results tables) shapes | FVL-03.013 | YES | COMPLETED |
| FVL-03.015 | Scientific Architecture Candidate Seeding — `engine.build_candidate_pool()`/`resolve_concentration()` Tier 0, `ORIGIN_SCIENTIFIC_FORMULATION`, real priority over a bare evidence mention, never overriding hard constraints | FVL-03.013 | YES | COMPLETED |
| FVL-03.016 | Scientific Architecture Adaptation Traceability — `engine._classify_architecture()` (`architecture_basis`: origin/source/retained/modified/added/removed), enriched `hard_exclusion` trace events naming the source architecture | FVL-03.015 | YES | COMPLETED |
| FVL-03.017 | Scientific-vs-Rule Architecture Selection — `pipeline.py`'s session-wide `scientific_formulation_summary` (architectures used/rejected, `rule_only_despite_applicable_scientific_formulation` with a real reason) | FVL-03.015 | YES | COMPLETED |
| FVL-03.018 | Evidence & Sources Scientific Formulation Detail UI — redesigned compact primary table + full-width detail panel (never clipped/ellipsized), extracted-formulation ingredient/outcome drill-down, Formula-tab Architecture Basis, Alternatives tab (selected/rejected architectures) | FVL-03.013 | YES | COMPLETED |

**Verification (FVL-03.013-018)**: real acceptance against the user's own
actual downloaded PDF (`10.20431_2455-1538.0402005.pdf`, a real F1-F5
herbal anti-dandruff shampoo composition table), copied read-only into a
disposable fixture per this project's own standing data-safety rule — 5
formulations extracted, 61 experimental outcomes linked, SLS present at
20/15/10/5/-% across F1-F5 exactly matching the source. `python -m pytest
runtime/pipeline -q`: 349/349 (23 new: `test_scientific_formulation.py`
14 + `ScientificFormulationPriorityTests` 9). Rust: `cargo check
--release` clean, `cargo test --release formulation_v2::` — 9/9 (2 new),
full workspace `cargo test --release` — 344/344 (2 new). Frontend: `pnpm
tsc --noEmit` clean, ESLint clean, `pnpm vitest run` — 138 files/1258
tests (1252 baseline + 6 new: 5 `FormulationResultPage.test.tsx` +
1 `formulationReport.test.ts`), zero regressions.

---

## FVL-04 — Data Onboarding Through Existing Data Exchange

Use the existing Data Exchange Center (template registry — see
`docs/DATA_EXCHANGE_CENTER.md` for the current template count and
architecture). No crawler, no parallel import framework.

This package covers TWO related onboarding cases, both landing in the
SAME existing Data Exchange lifecycle:

**A. Canonical/template-based onboarding** (FVL-04.001–.012) — confirm and,
only where genuinely necessary, extend the existing template registry for
FormuLab's own native import/export shape.

**B. External/customer-system schema adaptation** (FVL-04.013–.026,
approved scope expansion, 2026-08-18) — a read-only connector/mapping/
crosswalk layer that lets an enterprise customer's existing systems (ERP,
LIMS, proprietary formulation software, spreadsheets, relational
databases, REST APIs) enter through the SAME existing Data Exchange
preview/validation/commit authority, without customer-specific
modifications to FormuLab's canonical business engines.

**This is NOT a second import platform.** The connector/mapping layer is
an enterprise onboarding adapter in front of the existing Data Exchange
authority. It owns: extraction, source-schema description, source-to-
canonical mapping, external-ID resolution, repeatable transformation
configuration. It does NOT own: Material Master business rules, cost
calculations, inventory availability calculations, compatibility, safety,
or regulatory verdicts, formulation generation, laboratory interpretation,
or Data Exchange commit semantics — the single-authority principle
(`docs/FORMULAB_V1_FINAL_SCOPE.md`'s "Single-authority principle") applies
here exactly as it does to every FVL-03 engine integration.

Required architecture for the connector layer (FVL-04.013–.026):

```
Customer / External System
    ↓
Read-only Connector / Extractor
    ↓
Source Staging
    ↓
Schema Discovery
    ↓
Customer Mapping Profile
    ↓
Transformation / Crosswalk Resolution
    ↓
Canonical FormuLab import objects
    ↓
EXISTING Data Exchange Preview
    ↓
EXISTING Validation
    ↓
Human Review
    ↓
EXISTING Explicit Commit
    ↓
Canonical FormuLab records
```

**Explicitly out of scope for v1**: no supplier crawler; no new literature
crawler; no new Regulatory Database; no second Data Exchange; no
vendor-specific ERP connector (SAP, Dynamics, etc.) unless separately
approved later; no business-engine duplication of any kind.

**Existing dependency / baseline capability**: Data Exchange Center (Phase
6) — template registry, CSV/XLSX generation, validation/preview engine,
commit layer, import history — already implemented and tested. See
`docs/DATA_EXCHANGE_CENTER.md`, `docs/DATA_EXCHANGE_IMPORTS.md`,
`docs/DATA_EXCHANGE_TEMPLATE_REGISTRY.md`, `docs/DATA_EXCHANGE_HISTORY.md`.
Confirmed by this session's own audit: no alias/header-mapping, external-ID
crosswalk, or source-schema-discovery capability exists anywhere in the
current Data Exchange architecture — FVL-04.013–.026 is a genuinely new
adapter layer, not a duplicate of anything already implemented.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-04.001 | Confirm material master import template covers fields Phase 14's candidate pool needs | — | YES | COMPLETED (2026-08-19) — audit of `packages/shared/src/schemas/materials.ts::rawMaterialSchema`, `runtime/pipeline/master_materials_adapter.py`, `engine.py`, the existing `raw_materials` Data Exchange template (`dataExchangeRegistry.ts::MATERIAL_COLUMNS`) and its commit handler (`dataExchangeCommit.ts::commitRawMaterials`) found the template/lifecycle itself already fully sufficient (upload → parse → validate → preview → human review → explicit commit → import history → canonical `materials` collection, single authority, unchanged) — but found two real, genuine, blocking commit-handler gaps, not template-registry gaps: (1) `material_function` was collected as a real column but the commit handler hardcoded `functions: []` on every commit, silently discarding it — `master_materials_adapter.py` confirmed to actively read `RawMaterial.functions` for the candidate pool's own role-matching, so every material imported via Data Exchange was previously invisible to candidate generation regardless of how carefully the CSV was filled in; (2) `recommendedMinPercent`/`recommendedMaxPercent`/`technicalMaxPercent` — confirmed by direct read of `master_materials_adapter.py` (`resolve_concentration()`'s own Tier 4) to be the exact three concentration-range fields the candidate pool consumes — were not columns on the template at all. Both fixed with the smallest correct extension to the EXISTING template/commit path: three new optional `percentage`-typed columns added to `MATERIAL_COLUMNS` (`recommended_min_percent`/`recommended_max_percent`/`technical_max_percent`, inheriting the already-generic percentage validation, never defaulted to zero when absent — `nn()` leaves them `undefined`); `material_function` now parsed via the existing `multi()` helper and filtered against the real, canonical `MaterialFunction` enum (imported directly from `@formulab/shared`, not hand-copied) — an unrecognized token is silently dropped, mirroring the exact same validate-never-invent pattern the adjacent `physicalForm` mapping already used in the same function; refreshed on every import (not merely preserved on update), matching how `density`/`activeMatterPercent`/every other plain material-master field already behaves, since functional role is a genuine mutable material-master attribute, not a field owned by another authority's own review workflow (unlike `hazardClassifications`/`allergens`/`regulatoryStatuses`, confirmed still correctly preserve-only-on-create). Full coverage matrix: **A (required by candidate-pool/generation path)** — code/material_code (canonical identity, immutable once created, confirmed never generated from name/INCI/CAS), material_function→functions (fixed this session), recommended_min/max/technical_max_percent (added this session), active_matter_percent→activeMatterPercent (already mapped). **B (useful, optional, already correctly importable)** — inci_name, cas_number, ec_number, density, manufacturer_name, country_of_origin, storage_condition, shelf_life_months, hazardous→hazardClassifications, allergen→allergens, regulatory_status→regulatoryStatuses (all confirmed shape-only, no Safety/Regulatory verdict logic embedded in import, per task's own explicit boundary). **C (not currently required for Phase 14, genuinely present on canonical schema but not on the template — correctly NOT added, since not proven necessary)** — hlb, ionicCharacter, phMin/phMax, viscosityMin/viscosityMax, solidsPercent, waterPercent, tradeName, iupacName, appearance/color/odor, solubility — confirmed by direct grep that `master_materials_adapter.py` reads none of these; adding them would violate the task's own "do not assume every RawMaterial field is required" instruction. **D (imported through another authoritative entity)** — `RawMaterial.documents[]`/`incompatibilities`/`substituteCodes` are confirmed NOT touched by `raw_materials` import at all (preserved from existing or left empty) — documents are the separate, already-wired `material_documents` template's own job (FVL-04.003/.004's own scope); incompatibilities/substituteCodes have no template yet and are out of this task's four-task scope. Canonical identity preserved throughout: `material_code` is the template's own required natural key, immutable on update, never name/INCI/CAS-derived. Acceptance M1-M8: M1/M2/M3 proven by a new `dataExchangeCommit.test.ts` test (a real material with functions + all three range fields commits with every field correctly mapped, never dropped); M4 proven by a new test (missing optional range fields stay `undefined`, never `0`); M5 satisfied by construction (the new columns reuse the existing generic `percentage` dataType validation, already tested); M6 satisfied by construction (`material_code` is passed through unchanged, `code_reference` type, existing behavior untouched); M7 confirmed by the pre-existing `test_master_materials_adapter.py` (already covers exactly this `functions`/`recommendedMinPercent` shape reading from a `materials.json`-shaped fixture — zero Python changes needed, seam already correct once the TS-side write is correct); M8 trivially true (only the existing template/commit handler extended, no second import authority). A real, pre-existing, unrelated test bug also found and fixed during this audit: `dataExchangeValidation.test.ts`'s "classifies a well-formed new row as valid_create" test built its CSV row from a hardcoded 33-value positional array that silently broke the moment `MATERIAL_COLUMNS` gained any new column — rewritten to derive blank cells from the live column list so it can never desync from the registry again. `pnpm --filter @formulab/desktop test`: 1427/1427 (3 new). `pnpm --filter @formulab/shared test`: 1311/1311 (one test fixed, none added net). `typecheck`/`lint`: clean on both packages. Zero Python/Rust changes — no `pytest`/`cargo` re-run required. |
| FVL-04.002 | Confirm supplier import + supplier-material link templates are sufficient for FVL-03's provenance needs | FVL-04.001 | YES | COMPLETED (2026-08-19) — audit of `Supplier`/`MaterialSupplier`/`MaterialPrice` schemas (`materials.ts`), the existing `suppliers`/`material_prices` Data Exchange templates and commit handlers, and every FVL-03 consumer of supplier identity (`SubstitutionPanel.tsx`, `cost.ts::priceFor`) confirmed the `suppliers` template (Template 2) already fully sufficient — real `supplier_code` natural key, immutable identity, `approved_supplier`/`qualification_status` never set true by import alone (Quality's own decision, confirmed enforced in the commit handler, not merely documented). **Real finding**: a genuine, live, canonical `MaterialSupplier` relationship record (`materialSupplierSchema` — `materialCode`/`supplierCode`/`supplierTradeName`/`supplierMaterialCode`/`preferred`/`qualified`) and its own registered masterdata collection (`material_suppliers`, `masterdata.rs:135`) exist, but have NO Data Exchange template at all. Confirmed by direct grep this is NOT a gap FVL-03 needs closed: neither `SubstitutionPanel.tsx` nor `cost.ts`/`generatedFormulaCost.ts` read `material_suppliers`/`MaterialSupplier` anywhere — FVL-03's own real supplier-provenance chain (`RawMaterial.code` ↔ `MaterialPrice.supplierCode` ↔ `Supplier.code`) is instead fully carried by the ALREADY-WIRED `material_prices` template (Template 3), which already has real `material_code`/`supplier_code` code-reference columns (validated against `raw_materials`/`suppliers` via the existing generic `resolveReference` mechanism — a same-display-name-different-code supplier can never silently match, confirmed by construction: the join is by code, never name), plus `supplier_material_code` and `preferred` columns satisfying MaterialSupplier's own relationship fields in practice. The standalone `MaterialSupplier` record is confirmed to be created solely through the existing in-workspace `SupplierEditor.tsx`/`MaterialsPage.tsx` UI (the same "not a replacement for the in-workspace Materials import" boundary `DATA_EXCHANGE_CENTER.md` already documents for `IMPORT_EXPORT.md`) — not required by any FVL-03 consumer, so per the task's own minimal-change rule a new `material_suppliers` template was NOT created; flagged as a disclosed, non-blocking, out-of-scope finding for a future session if bulk-import of pure (no-price) supplier-material links becomes a real product need. Coverage matrix (Supplier): supplier_code (A, canonical identity, immutable) / supplier_name, country, contact fields, payment_terms, lead_time_days (A/B, already mapped) / approved_supplier, qualification_status (A, quality-gated, confirmed never import-set-true) / website, city, address, minimum_order_value, ratings, audit dates (B, folded into notes, already correctly non-blocking). Provenance reconstruction (§B3) proven end to end: `material_code`↔`supplier_code`↔`Supplier.code` reconstructible from the committed `MaterialPrice` record without parsing any display string. Acceptance S1/S2/S3/S6/S7 proven by 2 new `dataExchangeCommit.test.ts` tests (a real material-supplier link with `supplier_material_code`/`preferred` commits with real codes reconstructing the full chain; a same-display-name supplier with a different code creates a genuinely distinct record, never merged by name); S4/S5 (unresolved material/supplier code fails honestly) confirmed already proven by the pre-existing generic `dataExchangeValidation.test.ts::"classifies an unresolvable required reference as reference_missing"` test, reused rather than duplicated (same generic `code_reference` mechanism `material_prices`'s own columns already use); S8 confirmed by audit (`SubstitutionPanel.tsx`/`cost.ts` already resolve supplier via `MaterialPrice.supplierCode`, no second supplier-mapping system exists or was created). Zero production code changed for this task — audit + 2 new tests only. `pnpm --filter @formulab/desktop test`: 90/90 in `dataExchangeCommit.test.ts` (2 new, combined with FVL-04.001's own 3). `typecheck`/`lint`: clean. |
| FVL-04.003 | Confirm TDS metadata/document import path (existing template or genuinely missing field) | — | NO | COMPLETED (2026-08-19) — full audit of every document-related model in the codebase (`RawMaterial.documents[]`/`documentRefSchema`, the Data Exchange `material_documents` template/`materialDocumentSchema`, the Import Dialog, `DATA_EXCHANGE_SECURITY.md`) found the canonical, already-wired `material_documents` Data Exchange template (Template 4, `MATERIAL_DOCUMENT_TYPES` — a real 13-value enum explicitly including `"TDS"`) already fully sufficient — zero production code changes needed. **Two real, distinct document-shaped things confirmed to exist, only one of them live**: (1) `RawMaterial.documents[]` (`documentRefSchema`, with a `location: "Project-relative path or URL"` field) is confirmed by repository-wide grep to be completely unused — no UI anywhere reads or writes it, no commit handler touches it (`commitRawMaterials` explicitly preserves `existing?.documents ?? []`, never populates it from an import row) — a genuinely dead/orphaned schema field, not a live second document registry; (2) `material_documents`/`MaterialDocument` is the one real, live, fully-wired document model — full upload → parse → validate → preview → commit → import-history lifecycle, `targetCollection: "material_documents"`, registered in `masterdata.rs`. **File content vs. metadata (§C2), classified precisely, not overclaimed**: confirmed Classification A — metadata rows only. Repository-wide grep of the Import Dialog and `DATA_EXCHANGE_SECURITY.md` found no file-binary ingestion mechanism anywhere in Data Exchange; `file_name`/`expected_sha256` are honestly documented, and confirmed by direct code read, as a match-against-a-locally-held-file HINT for a human, never an actual upload/attachment — the schema's own doc comment states this outright ("importing a row never attaches a file"). This matches the existing architecture's own explicit, deliberate design (`DATA_EXCHANGE_SECURITY.md`'s own stated principle that nothing here writes an uploaded file to a renderer-controlled path) — not a gap to close, a documented boundary to respect. TDS metadata coverage (§C1): materialCode (required, code-reference validated) / document_type=TDS (real enum value) / document_title / revision / issuer / issue_date / expiry_date / file_name (reference only) / expected_sha256 — ALL present and wired; source URL and an explicit MIME/file-type field are confirmed genuinely absent from the schema, but NOT proven necessary by any acceptance criterion or FVL-03 consumer — classified C (not currently required), not added, flagged for a future session only if a real product need for URL-sourced TDS tracking emerges (avoids the task's own explicit "do not add unsupported fields merely because they sound useful" instruction). Material linkage (§C3) confirmed by construction: `material_code` is a required `code_reference` column resolved against `raw_materials`, never a name-based join; no automatic interpretation of TDS content into a business decision anywhere in the commit path (the handler writes only the 19 real metadata fields, nothing else). Naming boundary (§C4) respected — original `file_name` preserved verbatim as metadata; FVL-04.026's future renaming convention not touched. Retrieval (§T8): the canonical `material_documents` collection is retrievable via the same generic `listRecords()` masterdata bridge every other collection already uses (proven functional, not new); confirmed by grep that no dedicated material-detail UI page currently calls it to DISPLAY a per-material document list — an honest, disclosed UI/UX completeness gap, not an import/storage gap, and out of this task's own stated scope ("confirm... import path", not build a viewer). Acceptance T1-T4 proven by a new `dataExchangeCommit.test.ts` test (a real TDS row with revision/issuer/issue_date/file_name/expected_sha256 commits through the existing lifecycle, linked to the exact materialCode, `verificationStatus` forced `unverified` regardless of input); T5 proven by a new test (missing optional TDS metadata stays `undefined`); T6 proven by a new `dataExchangeValidation.test.ts` test (an unresolvable materialCode on a TDS row classifies as the existing generic `reference_missing` state); T7/T8 proven by a new test asserting TDS and SDS both commit through the exact same `material_documents` collection, never a second registry. Zero production code changed — audit + 3 new tests only (shared across FVL-04.004 below, since both reuse the identical path). `pnpm --filter @formulab/desktop test`: 90/90 in `dataExchangeCommit.test.ts`. `pnpm --filter @formulab/shared test`: 42/42 in `dataExchangeValidation.test.ts`. `typecheck`/`lint`: clean. **HARDENING (2026-08-19, Part B — dedicated per-material document viewer, the last disclosed UI/UX gap closed):** the metadata-only material_documents import path was always correct — this hardening closes the "no dedicated per-material viewer" gap disclosed since FVL-04.003's own original closure. New `apps/desktop/src/components/formula/MaterialDocumentsPanel.tsx`, mounted as a new "Documents" section inside `MaterialEditor.tsx` (gated on `isExisting`, same convention `WorkflowGatePanel` already uses) — reads `listRecords("material_documents")`, filters strictly by `materialCode === selectedMaterial.code` (never a name match), displays type/title/number/revision/issuer/supplier(resolved by code via the existing `suppliers` prop)/issue date/expiry date/language/verification/fileName/tags, with a deterministic date-comparison expired indicator and a TDS/SDS/specification filter. `RawMaterial.documents[]` (the confirmed dead/orphaned path) is never read or written. No "open file" action exists anywhere — `fileName` renders as plain provenance text, matching the schema's own "importing a row never attaches a file" boundary; no path is ever inferred from it. No Safety or Regulatory verdict text appears anywhere in the viewer (verified by a new test asserting no safe/unsafe/hazard/compliant/approved/restricted text renders). 9 new `MaterialDocumentsPanel.test.tsx` tests (DOC1-DOC13, all disposable/mocked). `pnpm --filter @formulab/desktop test`: 1500/1500. |
| FVL-04.004 | Confirm SDS metadata/document import path | — | NO | COMPLETED (2026-08-19) — reuses the identical, already-wired `material_documents` path/audit FVL-04.003 established (`MATERIAL_DOCUMENT_TYPES` includes `"SDS"` as a real, distinct enum value alongside `"TDS"`, same schema, same commit handler, same collection — confirmed NOT a separate storage framework by construction, not merely by claim). **Safety Engine boundary (§D2), confirmed absolute**: the `commitMaterialDocuments` handler writes exactly 19 real metadata fields (verified by an explicit new test enumerating `Object.keys()` of the committed record) — no `severity`, `formulaState`, hazard classification, or any shape resembling a `SafetyFinding` is present anywhere in the commit path; SDS import cannot create a Safety verdict because the handler literally has no code path that calls or resembles `evaluateSafety`/any hazard-scoring logic. Confirmed by the same audit that no automatic "safe"/"unsafe"/"restricted" inference occurs from SDS presence alone — the record carries only `verificationStatus: "unverified"` (forced, never taken from the file), which is a document-review state, not a safety state. **Regulatory boundary (§D3), confirmed absolute**: same evidence — the commit handler has no market/jurisdiction/compliance field or logic at all; SDS presence is never interpreted as Kenya/EAC compliance or any regulatory status. FVL-04.010 (regulatory rule/evidence import integrity) remains the dedicated, untouched owner of regulatory-import correctness — confirmed out of this task's scope, not touched. Material linkage (§D4) and naming boundary (§D5) identical to FVL-04.003's own findings — `materialCode` required code-reference, original `file_name` preserved, FVL-04.026 not implemented. Acceptance D1-D4 proven by a new `dataExchangeCommit.test.ts` test (a real SDS row with issuer/file_name/supplier_code commits through the existing lifecycle, linked to the exact materialCode); D5 proven by the shared missing-optional-metadata test (FVL-04.003's own T5); D6/D7 proven by a new test explicitly asserting the committed record's key set contains no Safety- or Regulatory-verdict-shaped field at all (`severity`/`formulaState`/`status` all absent); D8 proven by the shared TDS+SDS-same-collection test; D9 confirmed by the same generic-retrieval audit as FVL-04.003's T8 (real, functional, no dedicated per-material viewer UI yet — disclosed, non-blocking, out of this task's own "import path" scope). Zero production code changed — this task's own tests were already added as part of the combined FVL-04.003/.004 test block (proportional, not duplicated, per the project's own "don't duplicate an authoritative path's own proofs" convention). `pnpm --filter @formulab/desktop test`: 90/90. `pnpm --filter @formulab/shared test`: 42/42. `typecheck`/`lint`: clean. Do NOT start FVL-04.005 — explicit boundary honored. **HARDENING (2026-08-19, Part B — shares FVL-04.003's own fix):** the same new `MaterialDocumentsPanel.tsx` displays SDS documents identically to TDS/specification — same canonical `material_documents` source, same metadata-only boundary, same "no Safety verdict" proof (a new test asserts no safe/unsafe/hazard-shaped text renders anywhere in the viewer, re-confirming the SDS-specific boundary FVL-04.004's own original closure already established at the commit-handler level, now also proven at the UI level). No second document system, no RawMaterial.documents[] path. See FVL-04.003's own row for full evidence. |
| FVL-04.005 | Confirm specifications import path | — | NO | COMPLETED (2026-08-19) — audit found "Specifications" in the FormuLab domain model means `TestDefinition` (`packages/shared/src/schemas/testDefinitions.ts`: code/name/category/methodReference/unit/targetValue/minimum/maximum/passFailLogic/verificationStatus), a genuine acceptance-limit/method specification shared by Laboratory Trials and Stability Studies — confirmed by direct read, not assumed from the tracker's own wording. The existing `test_definitions` Data Exchange template (`TEST_DEFINITION_COLUMNS`, already registered and already wired to `commitTestDefinitions`) already covers every field the schema and its real consumers (`lab_results`/`stability_protocols`/`stability_results` templates' own `test_code` code-reference columns, `referenceTemplate: "test_definitions"`) need — zero production gap found. Coverage: test_code (natural key, immutable) / test_name / test_category / method_reference / unit / result_type / lower_limit→minimum / target_value→targetValue / upper_limit→maximum / replicate_count / destructive_test→criticalTestFlag / required_for_release/required_for_stability / active / tags — all present and wired. `verificationStatus` is forced `"imported_unverified"` by the commit handler regardless of file content (never `"verified"`), the same convention as `physicalForm`/regulatory rules. No `applicability`/`testCapability` columns exist on the template (correctly not required — those are workspace-assigned facts, not import-time facts, confirmed by schema read). Acceptance SP1-SP8: SP1/SP2/SP4/SP7 proven by a new `dataExchangeCommit.test.ts` test (a full definition with target/limits/unit commits, `imported_unverified` forced); SP5 proven by a new test (missing optional limits stay `undefined`, never 0); SP3 proven by a new test asserting `lab_results`' own `test_code` column already resolves against `test_definitions` by code (the real, pre-existing consumer link); SP6 already covered by the existing generic `reference_missing` validation path (same mechanism every other template's code_reference columns use); SP8 satisfied by construction (existing template/registry/commit path reused, nothing new created). Zero production code changed — audit + 3 new tests only. `pnpm --filter @formulab/desktop test`: 104/104 in `dataExchangeCommit.test.ts`. **HARDENING (2026-08-19, independent re-audit):** the prior closure's "Specifications == TestDefinition" conclusion was correctly narrow but insufficiently proven — a full Specification Domain Matrix was built by repository-wide search: (A) raw-material quantitative specification (active_matter_percent/recommended_min/max/technical_max) → `raw_materials` template, closed in FVL-04.001; (B) material specification DOCUMENT → the existing `material_documents` template's `document_type` enum genuinely includes `"specification"` as a 13th real value alongside SDS/TDS — confirmed by schema read but was NEVER explicitly tested before this session; now proven by a new commit test (`document_type: "specification"` commits through the identical wired path, `verificationStatus: "unverified"` forced); (C) finished-product/product specification → NO canonical schema exists anywhere (`finishedProductSchema` has zero min/max/target/acceptance-criteria fields) — a real, disclosed domain gap, not a Data Exchange gap, proven by a new test asserting the `finished_products` template carries no spec-like column; (D)/(E) QC/test specification → `TestDefinition`/`test_definitions`, already closed. (F) no other specification concept found. `test_definitions` is no longer presented as the entire specification universe — it is one of two real, distinct, already-sufficient paths. `pnpm --filter @formulab/desktop test`: 1480/1480 (2 new). **HARDENING (2026-08-19, Part A — the finished-product release/QC-limit domain gap closed, explicitly approved by the user before FVL-04.013):** the prior hardening's Specification Domain Matrix correctly identified (C) finished-product/product specification as a real, disclosed gap — no canonical schema existed anywhere carrying QC/release limits for a specific SKU. That gap is now closed with a genuine new canonical domain, never a notes-field workaround. **Schema**: `finishedProductSpecificationSchema` (`packages/shared/src/schemas/dataExchange.ts`) — `code` (own identity, `newId("spec")`), `skuCode` (required, references `FinishedProduct.code`), `testDefinitionCode` (required, references `TestDefinition.code` — never copied, only referenced), `targetValue`/`minimum`/`maximum` (mirroring `TestDefinition`'s own field names — a product-specific override of that test's generic limits), deliberately no separate `unit` field (the referenced `TestDefinition` already owns it — a second unit field would be a second source of truth), `requiredForRelease` (distinct from `TestDefinition.criticalTestFlag` — this product's own release gate, not the test's general importance), `market` (blank = every one of the SKU's own `targetMarkets`, same free-text convention `FinishedProduct.targetMarkets` already uses), `effectiveFrom`/`effectiveTo`, `verificationStatus` (reuses `TestDefinition`'s own `TEST_VERIFICATION_STATUSES` vocabulary — the closest real match, no new taxonomy invented). **Domain responsibility, deliberately NOT collapsed**: TestDefinition = what/how to measure (untouched, never duplicated); FinishedProductSpecification = which test applies to which SKU and what limit that SKU needs for release (new); TestResult = the actual measured outcome (untouched). **History semantics**: append-only, the exact `material_prices`/`exchange_rates` convention — a new `effective_from` period is always a new row, so a specification change never silently rewrites what an earlier batch was evaluated against; a quality reviewer can always answer "which specification was in force when this batch was evaluated" from the row's own `effectiveFrom`/`effectiveTo`. **Data Exchange template**: `finished_product_specifications` (natural key `sku_code`+`test_definition_code`+`effective_from`, `duplicatePolicy: "append_history"`) + `commitFinishedProductSpecifications` handler — both code_reference columns required, resolved against `finished_products`/`test_definitions`, never name-matched; `verificationStatus` forced `"imported_unverified"` regardless of file content. **Real UI consumer**: no existing Finished Product workspace exists in the app (confirmed by repository-wide search — genuinely nothing to extend), so the existing generic masterdata-browser pattern (`MaterialsPage.tsx`, already home to inventory/rates/packaging/factory-profile tabs) gained a new read-only "Specifications" tab (`SpecificationsTable`) — flat list across all SKUs with a SKU filter dropdown, displaying sku/test/min/target/max/required/market/effective-dates/verification, with a deterministic expiry indicator; no pass/fail evaluation performed (no existing authoritative evaluator owns that decision for this domain — limits are displayed honestly, never a verdict). **Rust/masterdata**: `finished_product_specifications` collection registered in `masterdata.rs` (`COLLECTIONS` array, append-only=`true`) and `masterdataPolicyAreas.ts`'s `MASTERDATA_COLLECTIONS`/`MASTERDATA_COLLECTION_POLICY_AREAS` (area `rawMaterials`, matching `finished_products`'s own mapping) — role-policy JSON fixtures regenerated via the existing `generate:role-policy-matrix` script, never hand-edited. Collection count 90→91 (every hardcoded-count test/assertion in both Rust and TS updated to match). Acceptance FPS1-FPS15: FPS1-FPS4 proven by a new `dataExchangeCommit.test.ts` test (real SKU+TestDefinition codes, exact target/min/max preserved, no `unit` field on the committed record); FPS5 proven by a new test (`imported_unverified` forced); FPS6/FPS7 proven by the template's own required code_reference column configuration, the same generic mechanism every other template's references use; FPS8 proven by a new `DataExchangeImportDialog.test.tsx` real-CSV-File round-trip; FPS9 proven by a new real-`.xlsx`-workbook round-trip through the actual `buildDataExchangeWorkbook()`/reader; FPS10 proven by the same dialog test's job-lifecycle assertions (shared with FVL-04.012's own H4 proof); FPS11/FPS12 proven by a new `MaterialsPage.specifications.test.tsx` test (real committed data renders in the real Specifications tab; the SKU filter provably excludes another SKU's specification); FPS13 proven by a new append-history test (two validity periods for the same SKU/test both persist, older unmodified); FPS14 confirmed by construction (`TestDefinition` itself untouched, zero diff); FPS15 confirmed by construction (100% existing registry/validation/commit/history pipeline reused). `pnpm --filter @formulab/desktop test`: 1500/1500 (24 new across `dataExchangeCommit.test.ts`, `DataExchangeImportDialog.test.tsx`, `MaterialsPage.specifications.test.tsx`). `pnpm --filter @formulab/shared test`: 1347/1347 (12 new independent-fixture tests, template-count assertions updated 44→45). `cargo test`: 345/345 (masterdata/role_policy count assertions updated 90→91). `typecheck`/`lint`: clean on both packages. |
| FVL-04.006 | Confirm price history import feeds FVL-03.002/003 correctly | FVL-04.002 | YES | COMPLETED (2026-08-19) — re-audit of the already-wired `material_prices` template/`commitMaterialPrices` handler and `engine/cost.ts` confirmed full sufficiency, extending FVL-04.002's own findings with the price-history-specific proof this task requires. Field matrix: material_code/supplier_code (required code_reference, resolved against `raw_materials`/`suppliers`, never name-based), supplier_material_code/manufacturer_name/brand_name/pack_size/country_of_origin/preferred/approved/certificate_available/coa_available/sds_available (folded into `notes` as text — `MaterialPrice` has no dedicated fields for these, confirmed by schema read, so nothing is silently dropped), currency/unit_price/price_unit/minimum_order_quantity/valid_from/valid_until/incoterm — all real, structured `MaterialPrice` fields, all wired. `approved` is never set true by import alone (confirmed: only ever appears as a note-only string). **Append-only/history semantics (§B2)**: `material_prices`' own `duplicatePolicy: "append_history"` and its commit handler (`code: newId("price")` on every call, no update-in-place branch) confirmed to genuinely create a NEW record on every commit — a repeated import can never silently overwrite an earlier price-validity period. **Exact identity (§B3)**: re-confirmed by the FVL-04.002 same-name-different-code test, still passing. **Cost Engine boundary (§B4)**: confirmed by reading `cost.ts::priceFor`/`buildCostSnapshot` — price *selection* (which quote is current for a given `asOf`), landed-cost computation, and FX conversion all live exclusively in `cost.ts`; the commit handler performs none of this, it only writes the fact. Acceptance P1-P8: P1 already proven by FVL-04.002's own create test; P2 proven by a new test (two validity periods for the same material/supplier both persist as genuinely distinct records, `effectiveFrom`/`price` preserved exactly per period); P3 already proven by FVL-04.002's provenance-chain test; P4/P5 already proven by the generic required/optional-field validation tests; P6/P7 proven by construction — `priceFor`/`buildCostSnapshot` already consume real `MaterialPrice[]` records with no importer-specific special-casing anywhere in `cost.ts` (confirmed by grep — no `data-exchange-import` string appears in `cost.ts`); P8 confirmed by audit (no price-selection logic exists in `dataExchangeCommit.ts`). Zero production code changed — 1 new test. `pnpm --filter @formulab/desktop test`: 104/104. **HARDENING (2026-08-19, independent re-audit):** the prior closure proved append-history behavior and called `priceFor`/`findRate` directly on committed records — real but narrow. New `apps/desktop/src/lib/dataExchangeCostAcceptance.test.ts` (13 tests) closes the full chain: every fixture is committed through the real `commitDataExchangeRows()` first, then the committed (never hand-built) records are fed into the real `costFormula()`. P1 (current valid price selected, exact KES total asserted from the engine's own return value) / P2 (an expired price is not selected when a current one exists) / P3 (future-dated price not selected before effectiveFrom) / P4 (a line pinned to a specific supplier via `FormulationLine.supplierCode` gets that supplier's own real price) / P5 (`missingReason: "no_price"`, the engine's own real state) / P6 (two price periods both persist, older one unmodified) / P7 (exact materialCode/supplierCode identity survives) / P8 (re-asserted operationally: committing an older price after a newer one does not reorder or reject it — selection is entirely `priceFor`/`costFormula`'s job) — all proven, all pass. `pnpm --filter @formulab/desktop test`: 1480/1480 (13 new). |
| FVL-04.007 | Confirm inventory import feeds FVL-03.004 correctly | FVL-04.002 | NO | COMPLETED (2026-08-19) — audit found the canonical `InventoryRecord` schema and its live `inventory` masterdata collection already existed (consumed by the real `evaluateMaterialAvailability()`, `engine/inventoryAvailability.ts`), but had **no Data Exchange template at all** — a genuine Case C gap (missing template for an existing canonical entity), not a completeness nicety: the pre-existing in-workspace importer's own `rates`/`inventory` tab config (`MaterialsPage.tsx`) confirmed `INVENTORY_FIELDS` exists there already for the in-workspace path, so this session's new `inventory_records` Data Exchange template is additive to that, not a duplicate — same relationship `raw_materials`/`suppliers` already have with their own in-workspace importer path. Fixed with the smallest correct extension: new `INVENTORY_COLUMNS`/`inventory_records` template (natural key `inventory_code` — the record's own real identity, matching the in-workspace importer's own required `code` field, never derived from material_code+warehouse+lot) and a new `commitInventoryRecords` handler, both following the exact existing `material_prices`/`material_documents` construction pattern. **§C1 carry-forward re-audit (explicit instruction)**: re-checked `MaterialsPage.tsx`, `AdvancedOptimizerPanel.tsx`, `SubstitutionPanel.tsx` — all three still contained the inline `quantity − reservedQuantity` arithmetic the FVL-03.004 closure log had explicitly disclosed and left unfixed ("the three pre-existing call sites are untouched, out of scope"). Confirmed this was a REAL single-authority violation, not cosmetic: none of the three applied `quarantined`/`released`/`expiresAt` filtering, so a quarantined or expired lot's quantity was silently counted as usable stock in the Optimizer, Substitution ranking, and the Materials inventory display/summary — directly contradicting canonical `evaluateMaterialAvailability()` semantics. **Fixed this session** by switching all three (6 call sites total: `AdvancedOptimizerPanel.tsx` buildProblem; `SubstitutionPanel.tsx` scored-candidate map, `toOptimizationMaterial`, system-substitution pool map; `MaterialsPage.tsx`'s per-material `stock` summary map and the per-lot `InventoryTable` "Available" column) to call the existing `evaluateMaterialAvailability()` directly — no new helper created, per explicit instruction. `inventoryAvailability.ts`'s own doc comment (which had claimed the three sites were permanently out of scope) corrected to reflect this. Inventory field matrix: material_code (required code_reference → raw_materials) / warehouse / lot / supplier_lot / quantity (required) / unit / reserved_quantity / manufactured_at / expires_at / coa_status / quarantined / released / unit_cost / currency / notes — all present, all wired, all real `InventoryRecord` fields, no invented ones. **Availability boundary (§C3)**: the commit handler writes only raw facts (`quantity`/`reservedQuantity`/`quarantined`/`released`/`expiresAt`/`coaStatus`); it never computes usable/available/infeasible state — that remains exclusively `evaluateMaterialAvailability()`'s job, confirmed by construction (no derived-availability field exists anywhere in `commitInventoryRecords`). Acceptance I1-I9: I1/I2/I3/I4 proven by a new commit test (exact quantity/reserved/quarantine/release/expiry preserved); I5 satisfied by construction (`evaluateMaterialAvailability`'s own `hasRecords: false` branch, unchanged, still the "genuinely unknown, never zero" signal); I6/I7/I9 proven by a new test feeding real committed records straight into the real `evaluateMaterialAvailability()` (a released, non-quarantined lot's net-of-reservation quantity counts; a quarantined lot's full quantity is excluded, not partially subtracted) and by a full `DataExchangeImportDialog` round-trip test (real CSV file → parse → preview → commit → `evaluateMaterialAvailability()` on the committed record); I8 proven by the UI-fix itself plus regression (`AdvancedOptimizerPanel.test.tsx`/`SubstitutionPanel.test.tsx`, 110/110, unchanged pre-existing assertions still pass against the new canonical-function-backed implementation). `pnpm --filter @formulab/desktop test`: 1452/1452 full suite (10 new across `dataExchangeCommit.test.ts`/`DataExchangeImportDialog.test.tsx`, template-count assertions in `DataExchangePage.test.tsx`/`dataExchangeRegistry.test.ts` updated 41→43 template cards, a real count change, not a weakened test). `typecheck`/`lint`: clean on both packages. **HARDENING (2026-08-19, independent re-audit — real bug found and fixed):** a genuine unit-contract bug was found by tracing `stock`/`availableStockKg`/`stockAvailableKg` downstream into `runtime/formulation/advanced_optimizer.py`, which treats `materials[].stock` as a LITERAL kg number (`cap_kg = min(cap_by_pct, stock)`, no unit ever travels with it) — but `AdvancedOptimizerPanel.tsx`'s `stock` field (and the two structurally-identical fields in `SubstitutionPanel.tsx`, one of which feeds the SAME Python optimizer via `buildSystemSubstitutionProblem`) assigned `evaluateMaterialAvailability(...).usableQuantity` with NO check that `InventoryRecord.unit` was actually "kg" — a material stocked in grams or litres would have silently produced a physical-stock cap wrong by orders of magnitude. Fixed in all three files: `stock`/`availableStockKg`/`stockAvailableKg` are now only ever populated when `availability.unit?.toLowerCase() === "kg"`; any other unit reports unknown (`undefined`) rather than a guessed/mislabeled number — no new unit-conversion table invented, per the hardening brief's own instruction. Proven by two new `AdvancedOptimizerPanel.test.tsx` tests that render the real component, select a candidate, click "Run optimization", and inspect the real problem handed to the solver bridge: a kg lot reports real stock (90, after reservation); a gram lot (100000g, i.e. 100kg) reports `stock: undefined`, never a fabricated 100000. MaterialsPage's own "Available" column/summary was re-audited and found NOT mislabeled (its i18n label is generic "Available", no kg claim) — left as-is, no fix needed there. `pnpm --filter @formulab/desktop test`: 1480/1480 (2 new). |
| FVL-04.008 | Confirm exchange-rate import feeds FVL-03.003 correctly | FVL-04.006 | NO | COMPLETED (2026-08-19) — audit found the canonical `ExchangeRate` schema (`packages/shared/src/schemas/costing.ts`) and its live `exchange_rates` masterdata collection already existed, consumed by the real Cost Engine's own `findRate()` (`engine/cost.ts`) — but had no Data Exchange template. Also confirmed the pre-existing in-workspace importer's own "rates" tab config (`MaterialsPage.tsx` `TAB_CONFIG.rates`) ships with an **empty `fields: []` array** — i.e. bulk exchange-rate import had no working path anywhere in the app before this session, a genuine, confirmed Case C gap. Fixed with the smallest correct extension: new `EXCHANGE_RATE_COLUMNS`/`exchange_rates` template (natural key `base_currency`+`quote_currency`+`effective_from`, `duplicatePolicy: "append_history"`, mirroring `material_prices`' own append-only pattern exactly, since `costing.ts`'s own module doc states rates are "records a person entered or imported, each carrying its date and source" — never overwritten) and a new `commitExchangeRates` handler. **FX authority (§D2)**: the commit handler performs zero rate selection or conversion — `code: newId("rate")`, `entryMethod: "imported"` (honestly true), `verification: "not_verified"` (forced, never taken from the file) are the only handler-side decisions; `findRate()` remains the sole selection/conversion authority, confirmed unchanged by grep and by a new test calling the real `findRate()` directly on committed records. No 1:1 fallback exists anywhere in the commit path (confirmed — the handler writes the file's own `rate` value verbatim, never defaults or infers one). Field matrix: base_currency/quote_currency (required enum, real `CURRENCIES`) / rate (required decimal) / effective_from (required date) / source (required string — matches the schema's own "never blank" rule) / notes — all present, all wired, no invented fields (`entryMethod`/`verification`/`code`/`recordedAt` are correctly handler-set, never file columns). Acceptance FX1-FX8: FX1/FX2 proven by a new commit test (exact pair/rate/source/effective_from preserved, entryMethod/verification forced); FX3/FX4 proven by new `dataExchangeValidation.test.ts` tests (unrecognized currency and missing `source` both classify `invalid` at preview, never reach commit); FX5/FX6 proven by a new test and a full `DataExchangeImportDialog` round-trip test feeding a real committed rate into the real `findRate()`, confirming it resolves with the exact committed rate/code; FX7 proven by a new test (`findRate([], "USD", "TRY", asOf)` returns `undefined`, same real function, no fabricated 1:1); FX8 confirmed by audit (no FX math exists in `dataExchangeCommit.ts` outside the `rate: r.rate` passthrough). `pnpm --filter @formulab/desktop test`: 1452/1452. `pnpm --filter @formulab/shared test`: 1327/1327 (4 new validation tests + template-count updates, a real registered-template-count change). **HARDENING (2026-08-19, independent re-audit):** the prior closure proved `findRate()` on a committed rate — real but narrow, never a full mixed-currency formula cost. `dataExchangeCostAcceptance.test.ts` (shared with .006's own hardening file) closes it: FX1-FX4 (a USD `MaterialPrice` + an imported USD/KES rate, both real committed Data Exchange records, produce a real engine-computed KES total — 10kg × 10 USD/kg × 130 KES/USD = 13000 KES, asserted from `costFormula()`'s own return, never hand-multiplied in the test); FX5 (the same USD price with no imported rate yields the real `missingReason: "no_exchange_rate"`); FX6 (a future-dated imported rate is not selected before its effectiveFrom); FX7 (the real engine selects the most-recent effective imported rate — a later 140 KES/USD rate is picked over an earlier 130 once its own effectiveFrom passes); FX8 (a same-currency cost needs no imported rate at all — `exchangeRateCodes` stays empty, confirming no 1:1 fallback is ever fabricated for a genuinely different pair). `pnpm --filter @formulab/desktop test`: 1480/1480 (5 new). |
| FVL-04.009 | Confirm process-parameter import path relevant to Manufacturing Procedure | — | NO | COMPLETED (2026-08-19) — audit found `processParameterSchema` (`packages/shared/src/schemas/dataExchange.ts`) IS the codebase's one canonical manufacturing-process-step structure (per-formula-version step: temperature/mixing-speed min-target-max, addition rate, hold time, critical-parameter flag, operator instruction) — confirmed by repository-wide grep that no separate pre-existing "Manufacturing Procedure engine" or process model exists anywhere else to preserve a distinction against; `DATA_EXCHANGE_CENTER.md`'s own documentation confirms `processParameterSchema` was one of the five net-new domain schemas the Data Exchange templates themselves introduced. Per this task's own E1 instruction ("do not invent a generic process model... preserve existing distinctions"), since only one such structure genuinely exists, the existing `process_parameters` template/`ProcessParameter` schema/`commitProcessParameters` handler (already registered, already wired) is confirmed the correct, sole, sufficient canonical path — zero production gap. Parent linkage (§E2/PR2) confirmed by construction: `formula_code`/`formula_version` are a required `code_reference` resolved against the existing `formula_bom` template (never a free-text/fabricated parent), and `step_number` completes the natural key (`formula_code`+`formula_version`+`step_number`) — a step's own real identity, generated as `${formula_code}-v${formula_version}-step${step_number}`. Field matrix: step_name/phase/equipment_type/temperature_min-target-max/mixing_speed_min-target-max/mixing_time_minutes/addition_rate/hold_time_minutes/critical_parameter/instruction/notes — all present, all wired, all optional except the identity triple (missing values stay missing, `nn()` never substitutes zero). One disclosed, non-blocking finding (same class as the FVL-04.003/.004 material_documents viewer gap): no dedicated Manufacturing Procedure display UI currently reads the `process_parameters` collection to show a per-formula process step list — a UI/UX completeness gap, not an import-path gap, explicitly out of this task's own "confirm the import path" scope. Acceptance PR1-PR8: PR1/PR5/PR6 already proven by the pre-existing create/update test (create-then-update on the same formula/version/step); PR2/PR3/PR4 proven by a new test (exact temperature/mixing-speed values and `critical_parameter` survive, keyed to the real identity); PR7 satisfied by construction (the generic `listRecords("process_parameters")` masterdata bridge, already proven functional elsewhere, is the same retrieval path any future Manufacturing Procedure UI would use); PR8 confirmed by audit (no second manufacturing/process engine created or touched). Zero production code changed — audit + 1 new test. `pnpm --filter @formulab/desktop test`: 104/104. **HARDENING (2026-08-19, independent re-audit — genuine gap closed, storage-only proof rejected):** the prior closure proved storage only and explicitly admitted no consumer existed — correctly identified by this session's own audit as insufficient. A real Manufacturing Procedure consumer was built: new `apps/desktop/src/components/formula/ProcessParametersPanel.tsx`, wired as a new "Process" tab in `FormulasPage.tsx` (the real saved-formula workspace, `active.code`/`baseVersion.versionNumber` passed as props), reading `listRecords("process_parameters")`, filtering by the exact `formulaCode`/`formulaVersion` identity, sorting by `stepNumber`, rendering step/name/phase/equipment/temperature-range/mixing-range/hold-time/critical-flag/instruction. Explicit precedence established and documented in the panel's own doc comment: a generated session card's `card.manufacturing` (`FormulationResultPage.tsx`'s `ManufacturingProcedureTab`) is a separate session/evidence-derived PROPOSAL that never reads `process_parameters`; this panel is the canonical/imported SAVED-formulation-version process record, read-only by design (written only through Data Exchange import or a future dedicated editor). New `ProcessParametersPanel.test.tsx` (3 tests) proves the full chain: a real CSV row commits through `commitDataExchangeRows`, the real panel renders the exact committed step/temperature/mixing/hold-time/critical/instruction values; a second test proves another formula's or another version's steps are never mixed into the same view; a third confirms the parent-reference column is a required code_reference (invalid references already refused by the existing generic mechanism). i18n: `process.*`/`builder.tabProcess` keys added to all 8 locale files (English content, Turkish translated, matching the project's established convention), `parity.test.ts` — 23/23 still passing. `pnpm --filter @formulab/desktop test`: 1480/1480 (3 new). |
| FVL-04.010 | Regulatory rule/evidence content import: verify `not_verified` seed status is preserved on import, never silently upgraded | — | YES | COMPLETED (2026-08-19) — high-integrity audit of `regulatory_rules`/`dossier_requirements`/`dossier_evidence` (all already registered, already wired) confirmed every verification-state guarantee holds, by direct code read, not merely by doc claim. **§F1 verification-state rule**: `commitRegulatoryRules` forces `verificationStatus: "not_verified"`, `status: "draft"`, `humanReviewStatus: "review_required"` on every commit, with an explicit code comment ("Never taken from the file — an import always resets verification") — even though the row record type is untyped (`Record<string,string>`) and could theoretically carry a smuggled `verification_status: "verified"` key, the handler never reads that key at all, confirmed by a new test that deliberately smuggles `verification_status`/`verified_by`/`verified_at` onto the row and asserts the committed record still shows `not_verified`/`draft`/`review_required`. `commitDossierEvidence` forces `status: "draft"` identically, with its own `linkStatus: "proposed"` (never `"accepted"`) for any auto-suggested requirement link — a human must explicitly accept it via `acceptEvidenceLink`, confirmed unreachable from any import path. **§F2 evidence-is-not-verdict**: confirmed absolute — `regulatory_evidence_items` records carry no compliance/verdict field at all; a proposed evidence-requirement link requires a real human actor (`requireHumanActor`) to accept, which no import code path can perform. The authoritative `evaluateRegulatory()`/`regulatoryClassification.ts` remains completely untouched by this session (confirmed by diff — zero changes to either file). **§F3 market/jurisdiction**: `jurisdiction` is a required `enum` column sourced from the real `REGULATORY_JURISDICTIONS` catalog (not a hardcoded list), validated at preview time before the commit handler ever runs (confirmed safe by the handler's own code comment); a KE rule's `jurisdiction: "KE"` is passed through verbatim, never generalized to EAC or any other market. `rule_type` is independently re-validated inside the commit handler itself (not just at preview) with an explicit thrown error for any unrecognized value — confirmed by the pre-existing test still passing. Acceptance R1-R9: R1 already proven by the pre-existing "creates a rule" test; R2/R3 proven by a new test (verification-smuggling attempt ignored); R4 proven by a new test (exact jurisdiction survives); R5 already proven by construction (all `*_code` fields pass through unchanged); R6 already proven by the generic `reference_missing` validation path, reused; R7 proven by the existing dossier-evidence "always draft" tests, re-verified passing; R8 confirmed by diff (zero changes to `regulatoryRules.ts`/`regulatoryClassification.ts`); R9 confirmed by audit (no second rule/evidence collection or registry created). Zero production code changed — audit + 2 new tests. `pnpm --filter @formulab/desktop test`: 104/104. **HARDENING (2026-08-19, independent re-audit — strengthened, not rewritten):** the prior closure's invariants were confirmed correct at the commit-handler unit level; this session adds the highest-lifecycle-level proof the brief specifically demanded. New `DataExchangeImportDialog.test.tsx` test drives a REAL `File` object through the actual dialog: parse → preview → explicit "Commit import" click → real commit, where the CSV itself carries `verification_status=verified,verified_by=Someone,verified_at=<real ISO datetime>` — the committed canonical record still shows `verificationStatus: "not_verified"`, `status: "draft"`, `humanReviewStatus: "review_required"`, and the real `jurisdiction: "KE"` survives exactly. This closes the gap between "the commit handler ignores this field" (already proven) and "the real, full import pipeline a human actually uses ignores this field" (now proven, not merely inferred). `evaluateRegulatory`/`regulatoryClassification.ts` re-confirmed untouched by `git diff` this session. `pnpm --filter @formulab/desktop test`: 1480/1480 (1 new). |
| FVL-04.011 | Extend the existing template registry only where a genuinely necessary field mapping is proven missing (no new import framework) | FVL-04.001–010 | NO | COMPLETED (2026-08-19) — final gap register across FVL-04.001-.010, classified per the task's own A-G scheme. **Class B (field extension)**: none this block (FVL-04.001's three concentration-range columns were already closed last session). **Class C (genuinely missing template for an existing canonical entity)**: exactly two — `inventory_records` (FVL-04.007) and `exchange_rates` (FVL-04.008), both added this session, both reusing 100% existing registry types/column types/validation/natural-key conventions/reference resolution/preview/commit dispatch/import history — zero new framework code. **Class A (existing template already sufficient)**: `raw_materials` (.001, post-fix), `suppliers`/`material_prices` (.002/.006), `material_documents` (.003/.004), `test_definitions` (.005), `process_parameters` (.009), `regulatory_rules`/`dossier_requirements`/`dossier_evidence` (.010). **Class D/E/F/G (intentionally NOT added, re-confirmed this session, no new evidence found to reopen any of them)**: no `material_suppliers` bulk template (still not required by any FVL-03 consumer — re-checked, no .005-.010 acceptance needed it either); no binary document ingestion (deliberate security boundary, `DATA_EXCHANGE_SECURITY.md`); no source-URL/MIME columns on `material_documents` (not proven necessary); no source-schema mapping/alias mapping/connector/crosswalk (FVL-04.013+ territory, untouched); no Manufacturing Procedure viewer UI (UI/UX gap, not a Data Exchange gap); no per-material document viewer UI (same class). **Registry quality (§G2)**: both new templates use the existing `col()`/`template()` helpers, existing `code_reference`/`enum`/`decimal`/`date`/`boolean` column types, existing `create_or_update`/`append_history` duplicate-policy vocabulary, existing generic `resolveReference` mechanism, existing preview/commit/history pipeline — confirmed by code read, not by claim. **Template count (§G3/TR8)**: registered template count genuinely grew 41→43; every test/doc that hardcoded the prior count was found and updated to the real number (`dataExchangeRegistry.test.ts` ×2, `DataExchangePage.test.tsx`, `DATA_EXCHANGE_CENTER.md`, `DATA_EXCHANGE_TEMPLATE_REGISTRY.md`, `DATA_EXCHANGE_TEMPLATE_CATALOG.md`) — each change traced to the real new registrations, never a blind find-replace. Acceptance TR1-TR9: TR1 this row itself; TR2 satisfied (every .001-.010 onboarding need maps to an existing-or-extended template, per the register above); TR3/TR4 confirmed (2 templates added, both Class C, zero parallel frameworks); TR5 confirmed (catalog/registry docs updated to match runtime, this session); TR6/TR7 confirmed (both new templates have real validation via existing generic column-type rules and a real wired commit handler, proven by tests); TR8 confirmed (count consistent everywhere, tests updated); TR9 confirmed by scope discipline — no FVL-04.013+ connector/mapping/crosswalk code exists anywhere in this session's diff. **HARDENING (2026-08-19, independent re-audit — the .011 closure was premature; rebuilt after .005/.007/.009 hardening, and the MaterialSupplier decision was reversed with new evidence):** the prior .011 gap register was built before .005 (Specification Domain Matrix), .007 (unit-contract bug), and .009 (real consumer) were genuinely proven — it is rebuilt now, after all of A-F hardening is complete. **MaterialSupplier, mandatory reassessment (§G1):** the prior "not required by FVL-03" conclusion was correct for FVL-03 but incomplete for FVL-04 as a whole — the tracker's own FVL-04.016 task description explicitly anticipates a source row fanning into `RawMaterial + Supplier + MaterialSupplier + MaterialPrice + InventoryRecord`, and a real enterprise vendor-qualification list legitimately has no price row at all. That is new evidence, not an old assumption, and it makes this a genuine Class-C gap (an existing canonical entity — `MaterialSupplier`/`material_suppliers`, already a live masterdata collection — with zero Data Exchange path). **Decision: ADD.** New `material_suppliers` template (`MATERIAL_SUPPLIER_COLUMNS`: material_code/supplier_code — both required code_reference — supplier_trade_name/supplier_material_code/preferred/qualified/notes, 100% real `MaterialSupplier` schema fields, no invented ones) + new `commitMaterialSuppliers` handler, `code` deterministic from `material_code::supplier_code` so a repeated import updates the same link; `qualified` never set true by import alone (a quality decision, preserved on update). Proven by 3 new `dataExchangeCommit.test.ts` tests (create with real codes and no price field on the record at all; update-in-place on repeat import, `qualified` preserved; reference-column configuration) plus the new cross-template chain test below (.012). **Final gap register**, Class C (genuinely missing, now closed): `inventory_records` (.007), `exchange_rates` (.008), `material_suppliers` (.011, this session). Everything else re-confirmed Class A/D/E/F/G with no new evidence to reopen any of them — no separate TDS/SDS template, no binary document ingestion, no source-schema/mapping/crosswalk/generic-DB/API connector, no duplicate Cost/Inventory/Regulatory engine. **Template count 43→44** — every hardcoded-count test/doc found and corrected again (`dataExchangeRegistry.test.ts` ×2, `DataExchangePage.test.tsx`, `DATA_EXCHANGE_CENTER.md`, `DATA_EXCHANGE_TEMPLATE_REGISTRY.md`, `DATA_EXCHANGE_TEMPLATE_CATALOG.md` — new dedicated section added). `pnpm --filter @formulab/desktop test`: 1480/1480 (3 new). `pnpm --filter @formulab/shared test`: 1341/1341. **HARDENING (2026-08-19, Part A follow-up):** template count grows once more, 44→45, with the new `finished_product_specifications` template (FVL-04.005's own real gap, closed only after explicit user approval — not reopened speculatively). Classified Class C (genuinely missing template for a newly-created canonical entity) — the ONE new schema this entire FVL-04 block has ever introduced beyond the five already documented in `DATA_EXCHANGE_CENTER.md`'s own header. No other registry changes made this session. `dataExchangeRegistry.test.ts` ×2, `DataExchangePage.test.tsx` updated 44→45. |
| FVL-04.012 | Real sample file acceptance test per confirmed/extended template, using disposable fixtures | FVL-04.011 | YES | COMPLETED (2026-08-19) — closure acceptance for the FVL-04.001-.011 canonical/template-based onboarding block. **Positive coverage**: a new parametrized `dataExchangeValidation.test.ts` suite pushes every confirmed/extended template's own real `exampleRows[0]` (raw_materials, suppliers, material_prices, material_documents, test_definitions, inventory_records, exchange_rates, process_parameters, regulatory_rules, dossier_requirements, dossier_evidence) through a real CSV string and the real `parse → validate → preview` path (`previewDataExchangeImportCsv`), each asserted `valid_create` with no fatal error — the full H1 lifecycle as far as pure unit-test infrastructure permits. Two full higher-level integration tests (`DataExchangeImportDialog.test.tsx`) exercise the two genuinely NEW templates end-to-end through the real dialog component with a real `File` object: upload → parse → preview → explicit "Commit import" click → real `upsertRecords` call — for `inventory_records` and `exchange_rates`, the two surfaces with no prior test coverage at all. **Negative coverage (§H3)**: an inventory row with an unresolvable `material_code` and one missing the required `quantity` both classify `reference_missing`/`invalid` at preview (new `dataExchangeValidation.test.ts` tests, plus a full dialog-level test proving the real "Commit import" button stays disabled); an exchange-rate row with an unrecognized currency and one missing the required `source` both classify `invalid`; a TDS/SDS row with an unresolvable `materialCode` was already proven `reference_missing` in FVL-04.003/.004; a regulatory rule with an unrecognized `rule_type` was already proven `failed` in the pre-existing suite. **Round-trip/consumer acceptance (§H4)**: RawMaterial→candidate pool already proven in FVL-04.001 (`test_master_materials_adapter.py`, untouched, still authoritative); MaterialPrice+FX→Cost Engine proven by two new tests calling the real `findRate()`/consuming real committed `MaterialPrice`/`ExchangeRate` records; Inventory→`evaluateMaterialAvailability()` proven by three new tests (unit test + dialog-level round-trip) feeding real committed records into the real function and confirming quarantined lots are excluded while released lots count net of reservation; Specifications→`test_definitions` consumer link proven by a new test asserting `lab_results`' own `test_code` column already resolves against it; Process Parameters→the real `listRecords("process_parameters")` retrieval path confirmed functional (no fabricated business outcome — the disclosed UI-viewer gap from .009 is honestly restated, not hidden); Regulatory content→remains correctly verification-gated, proven in .010; Material Documents→canonical collection retrievable, already proven in .003/.004. **Repeatability (§H5)**: `material_prices`/`exchange_rates` append-history behavior re-verified this session (two validity periods each, both persist); `inventory_records`/`test_definitions`/`process_parameters`/`regulatory_rules` create-or-update behavior re-verified; no incremental-conflict-resolution architecture built (correctly deferred to FVL-04.023). **Final regression**: `pnpm --filter @formulab/shared test` — 1327/1327 (67 files). `pnpm --filter @formulab/desktop test` — 1452/1452 (152 files). `pnpm --filter @formulab/shared typecheck` / `pnpm --filter @formulab/desktop typecheck` / `lint` — clean. `python scripts/validate_v1_tracker.py` — OK, 171 tasks, no drift. `git diff --check` — clean (LF/CRLF warnings only). Zero Python/Rust files touched this session — no `pytest`/`cargo` re-run required. No FVL-04.013+ connector/mapping/crosswalk work, no second Data Exchange system, no second document/document-binary system, no supplier or literature crawler, no FVL-04.026 artifact naming. Do NOT start FVL-04.013 — explicit boundary honored. **HARDENING (2026-08-19, independent re-audit — rebuilt with independent fixtures, not just exampleRows):** the prior closure's own acceptance (`template.exampleRows[0]` fed back through the same template's validator) was correctly identified as insufficient — the fixture and the thing validating it shared one source. Kept as smoke coverage (per the hardening brief's own instruction) and genuinely strengthened: **(H1) independent fixtures** — new `dataExchangeValidation.test.ts` describe block with 15 hand-authored rows (never copied from `exampleRows`) covering raw_materials/suppliers/material_suppliers/material_documents(specification)/inventory_records/exchange_rates/process_parameters/regulatory_rules, plus 6 independent negative fixtures (missing natural key, unresolvable supplier reference, non-numeric quantity, unresolvable formula reference, unrecognized jurisdiction, unrecognized document type). **(H2) real reference chain** — new `dataExchangeCommit.test.ts` test commits supplier → material → material_supplier → price → TDS → SDS → specification document → inventory sequentially through the real lifecycle and asserts every downstream record carries the exact same real code the supplier/material rows declared — never a name-based join. **(H3) full dialog lifecycle** — expanded from 2 templates (inventory/exchange-rate only) to also cover `material_suppliers` and `regulatory_rules` (verification-smuggling) through the real `DataExchangeImportDialog` (File → parse → preview → explicit commit). **(H4) import history** — new test proves the real job lifecycle: `awaiting_confirmation` recorded at preview with real templateCode/fileName/fileType/counts, `completed` recorded after commit with real `createdRows`/`committedBy`, a real `data_exchange_import_row_results` row linked by `jobId`, and confirms the raw cell content of a row is never persisted into the job record itself (only file metadata). **(H5) CSV and XLSX** — new tests build a REAL `.xlsx` workbook via the existing `buildDataExchangeWorkbook()` (not CSV) for `material_suppliers` and `raw_materials`, uploaded as a real `.xlsx` File and committed through the real `readWorkbookRows()` reader. **(H6) downstream consumers** — RawMaterial→candidate pool (proven in .001, `test_master_materials_adapter.py` untouched); MaterialPrice+ExchangeRate→`costFormula()` (.006/.008 hardening, 18 tests); Inventory→`evaluateMaterialAvailability()` (.007 hardening); Specification/TestDefinition→`lab_results`' real consumer link (.005); process_parameters→`ProcessParametersPanel` (.009 hardening, genuine UI consumer); Regulatory→verification-gated, `evaluateRegulatory` untouched (.010 hardening); MaterialSupplier→retrievable independent of MaterialPrice (.011, proven by the chain test's own assertion that the link record carries no price field at all). **(H7) negative files** — 6 new independent negative fixtures above, plus the pre-existing TDS/SDS/regulatory-rule-type/inventory/exchange-rate negative coverage, all still passing. **(H8) re-import semantics** — append-history (material_prices, exchange_rates) and create-or-update (inventory_records, material_suppliers, process_parameters, regulatory_rules, test_definitions) all re-verified against CURRENT behavior only; no FVL-04.023 incremental-conflict engine built. No real data mutated — every fixture uses a mocked masterdata bridge, never `.FormuLab/runs.db`/`%APPDATA%\com.formulab.app`. `pnpm --filter @formulab/desktop test`: 1480/1480 (152→154 files, +26 new this hardening session across `dataExchangeCommit.test.ts`/`AdvancedOptimizerPanel.test.tsx`/`ProcessParametersPanel.test.tsx`/`DataExchangeImportDialog.test.tsx`/`dataExchangeCostAcceptance.test.ts`). `pnpm --filter @formulab/shared test`: 1341/1341 (67 files, +14 new: independent-fixture matrix + count updates). `typecheck`/`lint`: clean on both packages. `python scripts/validate_v1_tracker.py`: OK, 171 tasks, no drift. `git diff --check`: clean (LF/CRLF warnings only). Zero Python/Rust files touched — no `pytest`/`cargo` re-run required. **HARDENING (2026-08-19, Part A follow-up — the new template gets real sample acceptance, not left storage-only):** `finished_product_specifications` (FVL-04.005) receives the exact same H1-H7 acceptance the rest of the block already has — independent fixtures (`dataExchangeValidation.test.ts`: 1 positive + 5 negative — unknown SKU reference, unknown TestDefinition reference, non-numeric limit, non-ISO date, missing natural key), real CSV round-trip and real `.xlsx` round-trip through the actual dialog/reader, and the real UI consumer (`MaterialsPage`'s Specifications tab) confirmed to display the committed record. This session's Rust change (`masterdata.rs`'s `COLLECTIONS` registration + `cargo check`/`cargo test`) is also newly in scope for .012's own testing requirement since it's the first FVL-04.005-.012 session to touch Rust — `cargo test`: 345/345. `pnpm --filter @formulab/desktop test`: 1500/1500. `pnpm --filter @formulab/shared test`: 1347/1347. `typecheck`/`lint`: clean on both packages. `python scripts/validate_v1_tracker.py`: OK, 171 tasks, no drift. `git diff --check`: clean (LF/CRLF warnings only). |
| FVL-04.013 | External Source Connector Contract — define the common read-only connector contract for external/customer systems: connector/profile ID, source system ID/name/version, source entity/dataset, source record identity, extraction mode, schema metadata, raw source values, source timestamps, extraction timestamp, source lineage, connector version, error/result contract. No canonical FormuLab business rules in the connector contract. | FVL-04.001, FVL-04.002, FVL-04.003, FVL-04.004, FVL-04.005, FVL-04.006, FVL-04.007, FVL-04.008, FVL-04.009, FVL-04.010 | YES | COMPLETED (2026-08-19) — new `packages/shared/src/schemas/connector.ts` defines the common read-only connector contract: `ConnectorIdentity` (connectorId/connectorType/connectorVersion/sourceSystemId/sourceSystemName), `SourceRecordIdentity`, `ExtractionMetadata`, `SourceLineage`, `StagedSourceRecord` (raw source field names/values only, never pre-transformed to canonical field names — that stays FVL-04.016's own job), `ConnectorError` (structured stage/code/sourceEntity/sourceRecordId/message/retryable/detail, no secrets), `ConnectorStats`/`ConnectorResult`, and the `SourceConnector` interface itself (`identity`/`discoverEntities`/`extract` only — deliberately no write/update/delete/patch/put/executeMutation method anywhere in its type). Read-only-by-omission proven two ways in new `connector.test.ts` (10 tests, C13-1 through C13-10): mock FILE/DATABASE/REST_API connectors all satisfy the identical interface; a source-text regex scan of the interface body asserts no forbidden write-shaped method name ever appears (C13-7); a second regex scan proves no plaintext secret field is ever staged (C13-8); structured retryable/non-retryable `ConnectorError` shapes and full extraction determinism (same source+version+config -> identical normalized output, timestamps excluded) proven directly (C13-9/C13-10). No canonical FormuLab business rule exists anywhere in this file — pure identity/metadata/result typing. `pnpm --filter @formulab/shared test`: 10/10 new, full suite 1423/1423. `typecheck`: clean. See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`. **HARDENING (2026-08-19, Session 6 — independent review corrections):** the prior closure's claim that `ConnectorResult` "exposes source identity/type/size/hash" was aspirational, not real — no such fields existed. Fixed: new `SourceResourceMetadata` (`kind`/`resourceName`/`mediaType`/`byteSize`/`contentFingerprint`/`sourceSchemaVersion`) attached via `ConnectorResult.sourceResource`, `contentFingerprint` explicitly documented as the same non-cryptographic FNV-1a fingerprint used elsewhere, never mislabeled a "hash"/"SHA256". `SourceRecordIdentity` gained `idSource: "configured"/"ordinal"`, making the three-tier identity model (staging ordinal / explicit external ID / canonical FormuLab ID) a real type, not just prose. C13-8's secret-exclusion test was weak (proved only that an unsupplied secret was absent) — a new hardening test supplies a REAL fake credential object and proves it is excluded from staged fields/lineage/errors/`sourceResource`. A mocked retryable `ConnectorError` is now proven alongside the pre-existing non-retryable one. `pnpm --filter @formulab/shared test`: 1467/1467 (73 files). **HARDENING (2026-08-19, Session 7 — second independent review, real gaps found and fixed):** parse-failure paths (CSV/JSON/XML/XLSX) previously interpolated the raw underlying library exception's own `.message` into `ConnectorError.message` — a genuine leak vector (a path, a connection string, whatever the library's own error text happened to contain). Fixed: every failure now returns a stable, hand-written message; `detail` carries only the exception's own constructor name, never its content — proven with an EXECUTABLE test (not source-text inspection) that throws an adapter error containing a real credential and a real local path and asserts neither appears anywhere in the serialized `ConnectorResult`. `explicit_primary_key` removed from `ExternalIdEvidence` — a dead enum value nothing ever set; `metadata_primary_key` (which DOES have a real input path) retained. `pnpm --filter @formulab/shared test`: full suite green (73 files). **HARDENING (Session 8, 2026-08-19/20 — final reference & version-chain integrity correction, no change needed to this file specifically):** re-audited as part of the session's registry-wide `code_reference` consistency sweep (`dataExchangeRegistry.consistency.test.ts`, 57 tests) — `connector.ts`'s own contract types needed no change; the real bugs closed this session were entirely in `dataExchangeValidation.ts`'s resolver logic and `dataExchangeExisting.ts`'s `buildReferenceResolver()`, covered under .014/.017's own rows below. See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`'s Session 8 section for the full four-part correction. |
| FVL-04.014 | Generic File Connector — scope for generic external-source file ingestion (CSV, XLSX, JSON, XML where current libraries already permit) that reads the customer's own source structure, does not require FormuLab column names, and feeds source staging/schema discovery, ultimately entering the existing Data Exchange lifecycle. | FVL-04.013 | YES | COMPLETED (2026-08-19) — new `packages/shared/src/engine/fileConnector.ts` + `xmlParser.ts` + `connectorFingerprint.ts` implement the generic CSV/XLSX/JSON/XML file connector, none requiring FormuLab canonical column names. CSV reuses the existing `parseCsv` (`importer.ts`) verbatim — no duplicate delimiter/quote/encoding logic. JSON (`flattenJson`/`findRecordArray`) supports bare arrays and `{items:[...]}` shapes with reversible dot/bracket path notation, never destructively flattened. XML is a hand-rolled recursive-descent parser (`xmlParser.ts`) with XXE safety by construction — a `DOCTYPE_OR_ENTITY` regex rejects any `<!DOCTYPE`/`<!ENTITY` sequence before parsing ever begins (`UnsafeXmlError`), a stronger guarantee than configuring a general-purpose library's flags since the vulnerable code path simply does not exist; `detectRepeatedElements` finds record boundaries by structural sibling repetition only, never a tag-name guess. XLSX multi-sheet support added via new `readWorkbookAllSheets()` in `apps/desktop/src/lib/xlsx.ts`, each sheet its own source entity, never auto-merged (`xlsx.test.ts` new 2-sheet-different-schema test, 15/15 passing). Independent fixtures with deliberately NON-FormuLab headers proven in new `fileConnector.test.ts` (13 tests): arbitrary CSV headers/nulls/decimal-text preserved verbatim, malformed CSV/JSON/XML all fail structured, nested JSON paths preserved, explicit `recordTag` override, DOCTYPE injection rejected before any parsing occurs. `ConnectorResult` exposes source identity/type/size/hash/entity/records/warnings/errors, no local absolute paths. `pnpm --filter @formulab/shared test`: 13/13 new (`fileConnector.test.ts`), full suite 1423/1423. `typecheck`: clean. See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`. **HARDENING (2026-08-19, Session 6 — independent review corrections, real gaps found and fixed):** (1) XLSX was staged through a disconnected special path (`readWorkbookAllSheets` fed manually into `stageRows` by callers) with no common connector abstraction and no real file metadata — fixed with new `stageFile()`, the ONE abstraction CSV/XLSX/JSON/XML all funnel through, XLSX via an injected `readWorkbook` adapter (`readWorkbookAllSheets` is the real production adapter, proven wired end-to-end in `xlsx.test.ts` with a genuine ExcelJS-written buffer). (2) corrupt XLSX previously threw a raw, unstructured ExcelJS exception — fixed: `stageFile()` catches it and returns a structured `corrupt_xlsx` connector error (`stage: "parse"`, `retryable: false`), proven with BOTH a mocked reader and the REAL `readWorkbookAllSheets` fed a genuinely corrupt buffer. (3) a missing configured source ID silently fell back to an ordinal identity with no way to require otherwise — new `StageOptions.requireExplicitId` makes that a structured `missing_source_id` error instead; `SourceRecordIdentity.idSource` (`"configured"/"ordinal"`) now makes the distinction a real type, never conflated with a persistent crosswalk-eligible identity. `pnpm --filter @formulab/shared test`: 24/24 in `fileConnector.test.ts` (11 new). `pnpm --filter @formulab/desktop test`: 2 new tests in `xlsx.test.ts` proving the real end-to-end wiring. **HARDENING (2026-08-19, Session 7 — second independent review, real gaps found and fixed):** the XLSX `contentFingerprint` previously fingerprinted the SELECTED SHEET's own parsed rows, not the source file — selecting a different sheet of the same file produced a different fingerprint, and `resourceName` had the sheet folded into it (`"file.xlsx#Materials"`). Fixed: new `fingerprintBytes()` (`connectorFingerprint.ts`) fingerprints the raw workbook bytes directly — same file, different selected sheet, now proven to produce the IDENTICAL fingerprint; new `SourceResourceMetadata.subResourceName` carries the sheet name separately, `resourceName` stays the plain filename. `byteSize` was previously caller-supplied on `FileConnectorInput` — removed entirely; `stageFile()` now always derives it internally (XLSX: actual `ArrayBuffer.byteLength`; text formats: real UTF-8 byte length via `TextEncoder`, proven with a multibyte "İstanbul" fixture distinguishing it from JS `string.length`). New `createFileConnector()` is a real `SourceConnector` implementation (`identity`/`discoverEntities`/`extract`, `discoverEntities()` returns real XLSX sheet names) — the common contract previously existed only as an unimplemented interface next to a standalone `stageFile()` function. `pnpm --filter @formulab/shared test`: 36/36 in `fileConnector.test.ts` (12 new). `pnpm --filter @formulab/desktop test`: `xlsx.test.ts` re-verified with corrected `subResourceName` assertion. **HARDENING (Session 8, 2026-08-19/20 — discriminated file-input contract, Part 6):** `FileConnectorInput`/`FileConnectorSource` were a single interface with `text?`/`bytes?` both optional regardless of `fileKind` — `stageFile()`/`createFileConnector()` silently fell back to `input.text ?? ""` / `input.bytes ?? new ArrayBuffer(0)` for a caller that got the shape wrong. Replaced with a real discriminated union: `TextFileConnectorInput` (`fileKind` csv/json/xml, `text: string`) and `XlsxFileConnectorInput` (`fileKind: "xlsx"`, `bytes: ArrayBuffer`), unioned as `FileConnectorInput` (same split for `FileConnectorSource`); both silent fallbacks removed. New compile-time acceptance in `fileConnector.test.ts` proves the four valid combinations type-check and four invalid ones (CSV+bytes-only, XLSX+text-only, XLSX-without-bytes, JSON-without-text) are rejected by `tsc` via `@ts-expect-error`, each directive itself failing typecheck (TS2578) if it ever stopped being necessary. `pnpm --filter @formulab/shared test`: 37/37 in `fileConnector.test.ts` (1 new). `typecheck`: clean. |
| FVL-04.015 | Source Schema Discovery — deterministic discovery/inspection of incoming source schemas: field/column names, primitive data types, nullable/missing patterns, date formats, decimal conventions, units when explicitly represented, probable primary/external IDs, relationship/reference columns, sheet/table/entity structure. Discovery may propose classifications but must never silently create authoritative canonical mappings. | FVL-04.013, FVL-04.014 | YES | COMPLETED (2026-08-19) — new `packages/shared/src/engine/schemaDiscovery.ts` performs deterministic (never AI) profiling: `classifyValue` never forces a mixed-type column to one type (`['1','2','N/A']` stays mixed); null/missing patterns distinguish absent/empty/null/source-token from real `0`/`false`/`'0'` values; `discoverDateFormat` only resolves DD/MM vs MM/DD when the sample data itself contains disambiguating evidence (a day value >12), otherwise reports `dateAmbiguous: true` rather than guessing a locale; `discoverDecimalConvention` resolves dot/comma only on unambiguous evidence, reporting `"ambiguous"` when a 3-digit trailing group after one separator is inherently unresolvable between US-grouping and EU-decimal; `discoverUnitHint` only fires on a dedicated unit column or a recognized header suffix (`Viscosity_cP`), never inferring a unit from a bare word like "Quantity"; `discoverRelationshipHints` flags `_id`/`Id`-suffixed fields as hints only, explicitly never a validated relationship; `entityFingerprintInput`/`discoverSourceSchema` produce a stable structural fingerprint independent of extraction time (field paths + observed types only, sorted entities). New `schemaDiscovery.test.ts` (18 tests, SD1-SD15 plus extras) proves mixed types, null/missing distinctions, unambiguous ISO dates, DD/MM vs MM/DD staying ambiguous without evidence, dot/comma/grouped decimal discovery, explicit-unit-column-vs-unresolved-field, unique-external-ID-vs-rejected-non-unique-name, XLSX multi-sheet distinctness, nested JSON paths, XML attribute/element description, and fingerprint stability across two runs of the same schema. One typecheck fix (`discoverField`'s value-array param widened to accept `undefined`) and one test-fixture bug (unquoted CSV commas inside decimal-comma values silently mis-split by the real CSV delimiter sniffer — fixed by quoting the fixture values) resolved during implementation. `pnpm --filter @formulab/shared test`: 18/18 new, full suite 1423/1423. `typecheck`: clean. See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`. **HARDENING (2026-08-19, Session 6 — independent review corrections, a real identity-authority gap fixed):** the prior `EXTERNAL_ID_STATUSES` (`"candidate"/"unresolved"`) let a merely-unique field — including a unique DISPLAY NAME like `MaterialName` — read as identity "candidate", indistinguishable from a genuinely configured ID. Replaced with `EXTERNAL_ID_EVIDENCE` (`explicit_primary_key`/`configured_external_id`/`metadata_primary_key`/`unique_candidate`/`unresolved`): a unique field now earns only the honest `unique_candidate` OBSERVATION; only an explicitly configured `idField` (carried into discovery via new `configuredIdField`) earns `configured_external_id`; `metadataPrimaryKeyFields` represents future DATABASE/REST-declared-PK evidence without implementing FVL-04.021/.022. New `discoverUnitColumnHints()` adds real dedicated unit-column discovery — `Quantity`/`UOM` (shared-column convention, only applied when exactly one numeric field exists, else left unresolved) and `Viscosity`/`ViscosityUnit` (unambiguous per-field suffix convention) — both structural, neither guessed from a bare field name. New `observedNullTokens` reports candidate null tokens (`N/A`/`NULL`/`-`/...) for a mapping profile to configure, never silently nulled by discovery itself; real `0`/`false`/`"0"` values proven never included. Fingerprint hardened to also cover unit hints and CONFIGURATION-driven identity role, while explicitly excluding sample-driven observations (`unique_candidate`, null ratio) that must not flip the fingerprint batch to batch — proven by a new test that two batches with different null ratios/values produce the identical fingerprint, and a new test that adding a unit-column pairing DOES change it. New `SourceSchema.sourceProvidedSchemaVersion` preserves a source-declared version separately from the computed fingerprint. `pnpm --filter @formulab/shared test`: 31/31 in `schemaDiscovery.test.ts` (13 new). **HARDENING (2026-08-19, Session 7 — second independent review, a real fingerprint-stability gap fixed):** `observedTypes` was still part of the fingerprint input — SAMPLE-derived, not declared structure: a `Quantity` column reading numeric values in one batch and text in the next (same headers, same declared structure) produced two DIFFERENT fingerprints. Removed `observedTypes` from the fingerprint input entirely; a new regression test proves the fingerprint is now identical across two such batches, while `SourceFieldSchema.observedTypes` itself still honestly reports the different per-batch profile — fingerprint stability and honest reporting are now deliberately separate concerns. New `nullTokenCandidates` config lets a caller EXTEND (never replace) the default null-token recognizer with customer-specific tokens (`"NO DATA"`, `"NOT RECORDED"`, `"~"`), still only ever reported, never silently nulled. `explicit_primary_key` removed from `ExternalIdEvidence` (dead, never set); `metadata_primary_key` retained (real input path). `pnpm --filter @formulab/shared test`: 33/33 in `schemaDiscovery.test.ts` (2 new this session). **HARDENING (Session 8, 2026-08-19/20 — no change needed):** re-audited as part of this session's four-part correction; `schemaDiscovery.ts` was not touched — the reference-resolution and mapping-profile-chain bugs closed this session live entirely downstream of discovery, in `dataExchangeValidation.ts`/`dataExchangeExisting.ts`/`mappingProfile.ts`. |
| FVL-04.016 | Mapping Profile Model — reusable, versioned customer mapping profiles recording source system, source entity, destination canonical entity, source field, destination field, transformation references, required/optional status, default only where explicitly allowed, relationship mapping, mapping version, created/updated timestamps, human approval state, mapping provenance. A mapping can fan one source entity into multiple canonical FormuLab record types (e.g. one external raw-material row → RawMaterial + Supplier + MaterialSupplier + MaterialPrice + InventoryRecord) — a simple one-column rename is not assumed sufficient. | FVL-04.015 | YES | COMPLETED (2026-08-19) — new `mappingProfileSchema` (zod, `packages/shared/src/schemas/connector.ts`, matching the established persisted-masterdata-schema convention) + new `packages/shared/src/engine/mappingProfile.ts` implement reusable, versioned mapping profiles supporting one source row fanning into multiple canonical records — proven end-to-end: one CHT_LIMS materials row -> `raw_materials` + `suppliers` + `material_suppliers` + `material_prices` + `inventory_records` candidates (MAP2, and the FVL-04.018 end-to-end fixture below). Mapping profiles are pure configuration — `fieldMappings`/`constantMappings` reference the closed `TRANSFORMATION_OPS` enum only, no eval/JS/Python snippet support anywhere (TR16's own source-text grep of `mappingProfile.ts`/`transformation.ts` for `dataExchangeCommit`/`upsertRecords` references — none found). `validateMappingProfile()` resolves every `targetTemplate`/`targetField` through the REAL existing `getDataExchangeTemplate()` registry — never a duplicate catalog — and fails validation on a nonexistent target field/template (MAP3), a schema fingerprint mismatch (blocks an incompatible profile by default, MAP7), and missing-required-target-field coverage (MAP4). Profile evolution is version-based: `profileVersion` is a new row, never a silent rewrite of a prior version's own behavior (MAP8 — v1 remains immutable/readable after v2 exists). `applyMappingProfile()`'s `MappingResult` always carries full source lineage, profileId/version, target template, candidate row, transformation trace, unresolved references, warnings/errors — and never commits (no `dataExchangeCommit`/`upsertRecords` import anywhere in the file). New `mappingProfile.test.ts` (10 tests, MAP1-MAP10 + TR15/TR16) proves all of the above, including MAP10/TR15 feeding a real mapped candidate through the REAL `previewDataExchangeImport()` and asserting `valid_create`. New `mapping_profiles` masterdata collection registered (Rust `COLLECTIONS`, TS `MASTERDATA_COLLECTIONS`/policy areas, role-policy matrix regenerated) — mutable (a still-`draft` profile can be edited before it's ever applied; a new version is still a new row by application-layer discipline, not storage-layer immutability). `pnpm --filter @formulab/shared test`: 10/10 new, full suite 1423/1423. `cargo test masterdata`: 23/23. `typecheck`: clean both packages. See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`. **HARDENING (2026-08-19, Session 6 — independent review corrections, a real immutability gap fixed):** the prior closure's "mutable, application-layer discipline only" was proven insufficient — nothing actually stopped a second write from silently rewriting an existing version's own mappings, and `MappingProfile` had no `code`/`id` field at all, so a real desktop write would have failed with "a mapping_profiles record has no `code` or `id`" (never caught because tests fully mocked the masterdata bridge). Fixed: `mappingProfileSchema` gained `code` (the real immutable storage identity, `"${profileId}::v${profileVersion}"`, via new `mappingProfileCode()`), always re-derived defensively in `connectorPersistence.ts`'s `saveMappingProfile()`; `mapping_profiles` re-registered **append-only=true** in `masterdata.rs` (was `false`), so the storage layer itself now rejects a second write reusing an existing version's `code` — proven by a new `connectorPersistence.test.ts` test simulating the real Rust rejection, and two new Rust tests. `validateMappingProfile()` gained `validateTransformationConfig()` (real per-op config-shape validation — e.g. `parse_decimal` requires `decimalSeparator`, `convert_unit` requires two dimensionally-compatible recognized units — BEFORE any row is mapped) and fan-out natural-key coverage validation (`missing_target_natural_key_field`), closing the gap where a fanned-out target with no identity mapping would only fail at commit time. `pnpm --filter @formulab/shared test`: 14/14 in `mappingProfile.test.ts` (4 new). `pnpm --filter @formulab/desktop test`: 7/7 in `connectorPersistence.test.ts` (3 new). `cargo test masterdata`: 25/25 (2 new). **HARDENING (2026-08-19, Session 7 — second independent review, a real lifecycle contradiction fixed):** Session 6's own append-only fix introduced a genuine contradiction — a persisted `active` v1 can never be rewritten to `"superseded"` (storage forbids it), yet the schema implied that mutation was expected. Fixed: a version's own stored `status` is now understood as its status AT CREATION, never rewritten; whether a version is CURRENTLY superseded is a DERIVED fact via new pure `effectiveMappingProfileStatus(profile, allVersions)` (superseded iff a newer version in the same `profileId` family exists), never stored. The ambiguous `supersedesProfileId` (could not distinguish "replaces v1" from "replaces v2" once three-plus versions exist) replaced with `supersedesProfileCode`, naming the EXACT immutable prior version. New `validateMappingProfileSupersession()` rejects self-supersession, a nonexistent target, cross-family supersession, and a duplicate version code — wired into `saveMappingProfile()` before storage. `validateTransformationConfig()` deepened: `parse_decimal` now restricted to explicitly supported separators; `map_boolean`/`map_enum` now validate member/value types (closing a real runtime-throw risk in `transformation.ts` itself, see .018's own row); `resolve_crosswalk` now requires an explicit `sourceEntity` or `sameEntity:true`, and a configured `fallbackCanonicalField` is validated against the discovered source schema. New Rust `apply_upsert()` (extracted from the async Tauri command so the real merge/rejection logic is directly unit-testable against disposable in-memory rows, never real `data/master`) proves the exact behavior, not just the collection's own metadata flag. `pnpm --filter @formulab/shared test`: 20/20 in `mappingProfile.test.ts` (6 new). `pnpm --filter @formulab/desktop test`: 10/10 in `connectorPersistence.test.ts` (3 new). `cargo test masterdata`: 28/28 (3 new). **HARDENING (Session 8, 2026-08-19/20 — exact immutable version chain, Part 5):** Session 7's `effectiveMappingProfileStatus()` marked a version superseded whenever ANY higher-numbered version existed in the family — including an unlinked draft that never actually named it as predecessor, too loose. Fixed: a version is now effectively superseded only when some OTHER persisted version explicitly names its exact `code` via its own `supersedesProfileCode` AND that successor's own `status` is `"active"` — a draft successor never deactivates its predecessor. `validateMappingProfileSupersession()` now also enforces an exact linear chain (no gaps, no branching): `profileVersion === 1` requires no `supersedesProfileCode`; every `profileVersion > 1` must equal `max(existing) + 1` for its family AND its `supersedesProfileCode` must equal the current latest persisted version's exact code. Storage model audited before implementation per the session's own instruction: `mapping_profiles` is already append-only with `status` fixed at creation and no separate activation-pointer collection exists anywhere — the model this correction assumes (Option A) was already the codebase's real model; no storage or Rust change was needed. New MP1-MP12 in `mappingProfile.test.ts` prove no-predecessor v1 valid, v2-with-no-supersedes rejected, genuine v2->v1 valid, v3->v1-while-v2-missing rejected as a gap, v3->v1-while-v2-exists rejected as wrong-predecessor, draft-successor-does-not-deactivate, active-successor-does-deactivate, full v1/v2/v3 chain correctness, cross-family/duplicate-version still rejected, no mutation of prior rows, no rewrite of stored status. `pnpm --filter @formulab/shared test`: 32/32 in `mappingProfile.test.ts` (12 new). No Rust file touched — `cargo test masterdata` not re-run (nothing to verify). |
| FVL-04.017 | External ID Crosswalk Registry — persistent crosswalk from external source identity (sourceSystem/sourceEntity/sourceRecordId) to canonical FormuLab identity (canonicalEntity/canonicalRecordId), e.g. CHT_LIMS/MATERIAL/883729 → RawMaterial/RM-00291. Stable external IDs preferred over display-name matching; aliases/names are never authoritative identity; requires mapping provenance, mapping version, conflict detection, human confirmation when unresolved/ambiguous, reuse on later imports. No name-only silent matching. | FVL-04.016 | YES | COMPLETED (2026-08-19) — new `externalIdCrosswalkSchema` (zod, `packages/shared/src/schemas/connector.ts`) + new `packages/shared/src/engine/crosswalk.ts` persist the deterministic sourceSystem+sourceEntity+sourceRecordId -> canonicalEntity+canonicalRecordId mapping (e.g. CHT_LIMS/MATERIAL/883729 -> RawMaterial/RM-00291, XW1). `resolveCrosswalk()` matches the exact tuple + `status === "active"` only — no fuzzy or partial matching exists anywhere in the function. `upsertCrosswalk()` re-confirms (bumps `lastSeenAt`, preserves `firstSeenAt`) when the same tuple resolves to the same target, but returns an explicit `CrosswalkConflict` object with the existing array left completely unchanged when the same tuple would resolve to a DIFFERENT target (XW4) — conflicts are never silently overwritten. XW5/XW6 prove display-name equality creates NO automatic crosswalk and a source record without a configured external ID stays unresolved rather than being name-matched — there is no code path in `crosswalk.ts` that reads or compares any name/label field at all. XW2/XW3 prove the same source record ID resolves to the same canonical identity on a second import while a DIFFERENT source system with the identical record ID stays fully distinct (the tuple includes `sourceSystemId`). XW7/XW8 prove persistence round-trip and mapping-profile-version/source-lineage preservation. XW9 (no automatic deletion) satisfied by construction — no delete/remove function exists anywhere in `crosswalk.ts`. Desktop-side `apps/desktop/src/lib/connectorPersistence.ts` (`persistCrosswalkEntry`/`loadCrosswalks`) is the only place the pure resolution logic touches the existing masterdata bridge (`listRecords`/`upsertRecords`) — matching the established pure-engine/desktop-persistence split every prior FVL module uses. New `external_id_crosswalks` masterdata collection registered (Rust `COLLECTIONS` 91->93 total across .016/.017, TS `MASTERDATA_COLLECTIONS`/policy areas, role-policy matrix regenerated, every hardcoded collection-count assertion in both Rust and TS updated). New `crosswalk.test.ts` (8 tests, XW1-XW9) + `connectorPersistence.test.ts` (4 tests) all pass. `pnpm --filter @formulab/shared test`: 8/8 new, full suite 1423/1423. `pnpm --filter @formulab/desktop test`: 4/4 new, full suite 1511/1511. `cargo test masterdata`: 23/23. `typecheck`/`lint`: clean both packages. See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`. **HARDENING (2026-08-19, Session 6 — independent review, one real inconsistency fixed, everything else re-confirmed correct):** an independent audit found `CROSSWALK_STATUSES = ["active", "conflict"]` declared a `"conflict"` persisted status that NOTHING in the codebase ever actually set — `upsertCrosswalk()` always returns a conflict as a separate, unpersisted `CrosswalkConflict` object, leaving the existing `active` record completely untouched. A dead enum value with no real persisted semantic is worse than none; narrowed to `CROSSWALK_STATUSES = ["active"]`, with the actual chosen behavior now documented explicitly in the schema's own doc comment: the canonical active crosswalk is never silently overwritten, a conflicting write is never persisted, the conflict surfaces to the caller for human review. `resolveCrosswalk()`'s exact-tuple-including-`canonicalEntity` matching was re-verified correct (no risk of resolving Supplier against a RawMaterial tuple) — no code change needed there. No-name-matching, no-auto-delete, and persistence round-trip were all re-confirmed correct by re-running XW1-XW9 unchanged. `pnpm --filter @formulab/shared test`: 8/8 in `crosswalk.test.ts`, unchanged and still passing. **HARDENING (2026-08-19, Session 7 — second independent review, a real API-enforcement gap fixed):** a second audit found the "never persist a crosswalk from a staging-only ordinal identity" guarantee was previously enforced only by TEST-USAGE CONVENTION — nothing in `persistCrosswalkEntry()`'s own signature prevented calling it with an ordinal `sourceRecordId`. Fixed: `persistCrosswalkEntry()` now REQUIRES `sourceIdentity: { sourceRecordId, idSource }` as part of its own call signature; when `idSource === "ordinal"`, persistence is refused (`ordinal_identity_not_crosswalk_eligible`) before `upsertCrosswalk()` or any storage call ever runs — structurally impossible to call this function with an ordinal identity and have it actually persist, proven by a new test asserting neither `listRecords` nor `upsertRecords` is ever invoked for that path. `crosswalk.ts` itself needed no change — re-confirmed correct again. `pnpm --filter @formulab/desktop test`: 10/10 in `connectorPersistence.test.ts` (3 new this session, shared with .016's own row). **HARDENING (Session 8, 2026-08-19/20 — reference resolution made field-aware, Part 1/2):** `buildReferenceResolver()` previously took `Iterable<string>` (template codes only) and checked a key against the target's own composite `naturalKeys` set regardless of which single field the referencing column actually needed — a live false negative for every real reference into a composite-natural-key template (`packaging_bom`, `label_content`, `doe_factors_responses`). Rewritten to take `{referenceTemplate, referenceField}[]` requirements, indexing arbitrary exported fields from each template's own `rows` (reusing the same loader output, never a second per-template reference registry), caching the underlying record load per template and the resolved value set per `(template, field)` pair. Blanket self-reference bypass (`column.referenceTemplate === template.templateCode ? true : ...`) removed from the validator — a self-reference now resolves through this exact same field-aware path. See .013's row above and `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`'s Session 8 section for the full correction, including the registry-wide consistency test and SELF1-4/REF1-11 acceptance. `pnpm --filter @formulab/desktop test`: 54/54 in `dataExchangeExisting.test.ts` (8 new). |
| FVL-04.018 | Transformation / Unit / Enum Mapping — configurable transformations between customer and FormuLab representations: decimal locale conversion, date parsing, whitespace normalization, explicit text casing where safe, enum/value crosswalk, unit conversion through an EXISTING canonical unit-conversion capability where one exists, boolean representation mapping, relationship resolution. No duplicate business engines — reuse an existing FormuLab domain conversion/validation function wherever one already exists. Missing/ambiguous transformations fail validation or remain unresolved, never guessed. | FVL-04.016 | YES | COMPLETED (2026-08-19) — new `packages/shared/src/engine/transformation.ts` implements the declarative transformation step set (trim/empty_to_null/lowercase/uppercase/safe_code_case/parse_decimal/parse_date/map_enum/map_boolean/convert_unit/resolve_crosswalk/constant/copy/split/join) as configuration-only operations, never a general scripting language. Decimal locale (`parseExplicitDecimal`) and date parsing (`parseExplicitDate`) both require an explicit configured format/separator from the mapping profile and never guess from host locale — an ambiguous or unconfigured convention produces a structured `decimal_convention_not_configured`/`date_format_not_configured` error, never a guessed value (TR2/TR4). This is deliberately a SEPARATE function from the pre-existing heuristic `parseHumanDecimal()` (`decimal.ts`, still used unchanged by Data Exchange's own CSV validation) — genuinely different semantics (explicit-config vs. auto-detecting heuristic), not a duplicate. Enum mapping (`map_enum`) and boolean mapping (`map_boolean`) are explicit/configured only — an unrecognized source value is never fuzzy-matched to the nearest enum (TR8/TR9). Unit conversion (`convert_unit`) is dimension-gated: `MASS_UNITS`/`VOLUME_UNITS` tables, `unitDimension()` only allows same-dimension conversion (g<->kg, mL<->L proven, TR10); a cross-dimension conversion (L->kg) always errors `incompatible_unit_conversion`, with no density-based conversion authority anywhere in this file — density-based costing conversion remains solely `cost.ts`'s own existing per-line logic, untouched (TR11). Relationship resolution (`resolve_crosswalk`) calls the real FVL-04.017 `crosswalk.ts` `resolveCrosswalk()` through an injected `TransformationContext`. Every mapped field's trace records source field/path, raw value, operations run, and result (`applyTransformationPipeline`). No `dataExchangeCommit`/`upsertRecords` reference exists anywhere in `transformation.ts` or `mappingProfile.ts`, confirmed by a source-text grep test (TR16) — transformation output is always a canonical CANDIDATE, the existing Data Exchange validator remains the final authority. New `transformation.test.ts` (17 tests, TR1-TR14 + null propagation) all pass. `pnpm --filter @formulab/shared test`: 17/17 new, full suite 1423/1423. `typecheck`: clean. **End-to-end acceptance closing FVL-04.013-.018 together:** new `apps/desktop/src/lib/connectorEndToEnd.test.ts` (6 tests) proves the full chain for real. Fixture 1 (`CHT_LIMS`, customer-material-master-shaped headers `Chemical_ID`/`Chemical_Name`/`Vendor_ID`/`Vendor_Name`/`Vendor_Product_Code`/`Use_Min`/`Use_Max`/`Price_USD`/`Stock_Grams`/`Active_Flag`) stages via `stageCsvFile` -> `discoverSourceSchema` -> a real `MappingProfile` fanning into `raw_materials`/`suppliers`/`material_suppliers`/`material_prices`/`inventory_records` -> `applyMappingProfile()` -> the supplier candidate committed through the REAL `commitDataExchangeRows()` -> `persistCrosswalkEntry()` records the crosswalk only AFTER that real commit -> a second mapping pass resolves `material_prices.supplier_code` through that real crosswalk, proven genuinely load-bearing by a negative run: re-mapping with no crosswalk available produces an explicit `crosswalk_unresolved` error rather than silently falling back to the raw source ID -> all four remaining candidates committed through the real Data Exchange lifecycle, with unit conversion (250000g -> 250kg) and boolean mapping (`Y` -> `released: true`) both verified on the real committed candidate. Fixture 2 (`ACME_ERP`, structurally different headers `ItemNo`/`Description`/`VendorNo`/`VendorItem`/`CurrencyCode`/`UnitCost`/`StockQty`/`StockUOM`) proves a different schema fingerprint, a different mapping profile, the identical connector/discovery/mapping engine code, and a dedicated source-text grep test confirming no `sourceSystem === "..."` conditional exists anywhere in `mappingProfile.ts`/`fileConnector.ts`/`schemaDiscovery.ts`/`crosswalk.ts`/`transformation.ts` — vendor-specific behavior lives only in profile DATA. Structured failure acceptance: malformed CSV/JSON and unsafe XML (DOCTYPE) all fail with a structured stage-tagged error; a schema fingerprint mismatch and a crosswalk conflict both produce explicit structured states; an invalid canonical candidate never reaches `upsertRecords` (asserted never called for an invalid preview row) — no partial canonical commit. Security audit re-confirmed by construction across all six tasks: connectors read-only (no write method exists, C13-7), no arbitrary SQL/code execution surface, XML external entities disabled by construction, no plaintext secret ever staged (C13-8), crosswalk conflicts never silently resolved (XW4), no name-based silent matching anywhere (XW5/XW6). No admin/dev viewer UI was built — deliberately deferred per the brief's own "primary deliverables are architecture/models/engines/tests" framing; this decision is recorded here, not silently omitted. New `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md` covers the full pipeline, security boundaries, the Data Exchange boundary, and the FVL-04.024 future-bridge boundary. `pnpm --filter @formulab/desktop test`: 6/6 new (`connectorEndToEnd.test.ts`), full suite 1511/1511. `pnpm --filter @formulab/shared test`: full suite 1423/1423 (73 files). `typecheck`/`lint`: clean both packages. `cargo check`/`cargo test masterdata`: clean, 23/23. `python scripts/validate_v1_tracker.py`: OK, 171 tasks, no drift. **HARDENING (2026-08-19, Session 6 — independent review corrections, the largest correction in the whole FVL-04.013-.018 block):** an independent repository-level review found real gaps this closure had missed. **(1) Duplicate unit-conversion tables**: `MASS_UNITS`/`VOLUME_UNITS` lived only inside `transformation.ts` with no genuine single authority — a repository-wide audit (grep for `MASS_UNITS`/`VOLUME_UNITS`/`convertUnit`/density/unit conversion across `packages/shared/src`) confirmed no pre-existing generic authority existed (`cost.ts`'s own inline conversions are deliberately different, density-specific business logic, correctly left untouched). Fixed: new `packages/shared/src/engine/unitConversion.ts` is now the ONE generic mass/volume conversion authority; `transformation.ts`'s own local tables deleted, `convert_unit` now delegates to it. **(2) Relationship-resolution precedence was implicit, not real**: `resolve_crosswalk`'s `canonicalEntity` was previously supplied only by the CALLER's own context wiring (a test literal), never by the transformation step's own configuration — genuinely ambiguous which canonical entity a mapping intended. Fixed: `canonicalEntity` is now a REQUIRED field of `resolve_crosswalk`'s own config (validated at profile-validation time too, D3); the full required precedence is now implemented — (1) crosswalk, (2) an explicit canonical code named by a new `fallbackCanonicalField` on the SAME source record, (3) unresolved — never a name match, proven by new tier-2/tier-3 tests. **(3) Impossible dates were silently accepted**: `parseExplicitDate` only checked `day<=31`/`month<=12`, so `31/02/2026`, `29/02/2025` (non-leap), and `31/04/2026` all previously "parsed" as valid dates. Fixed: new `isValidCalendarDate()` (real days-in-month table, leap-year rule) rejects all three; `29/02/2024` (a genuine leap year) still correctly accepts. **(4) Malformed decimal grouping was silently digit-stripped**: `"1,23,4"` (invalid grouping) and `"1.2.3"` (two decimal separators) previously parsed as `1234`/passed the regex incorrectly. Fixed: `parseExplicitDecimal` now validates real thousands-grouping structure (groups of exactly 3 digits) and rejects more than one decimal-separator occurrence — both now correctly refused as `ambiguous_or_invalid_decimal`. **(5) The end-to-end closure tests used an unconditional `resolveReference` stub** (`() => true`) everywhere, bypassing the real Data Exchange reference-existence check entirely — a `material_prices` row referencing a nonexistent supplier would have "passed" only because the check was disabled. Fixed: `connectorEndToEnd.test.ts` rewritten around a real `ReferenceStore` built ONLY from actually-committed `DataExchangeRowResult.naturalKey` values, wired as the genuine `resolveReference` callback everywhere; a new negative case proves an unregistered code is genuinely refused (`reference_missing`). ACME_ERP now performs at least one real explicit commit (previously preview-only). A new "Structured failure matrix" describe block gives FAIL1-FAIL20 each an explicit test or a direct pointer to the specific existing test covering it, closing Section 6 of the original brief completely. `pnpm --filter @formulab/shared test`: 30/30 in `transformation.test.ts` (13 new), full suite 1467/1467 (73 files). `pnpm --filter @formulab/desktop test`: 23/23 in `connectorEndToEnd.test.ts` (17 new/rewritten), full suite 1533/1533 (158 files). `typecheck`/`lint`: clean both packages. `cargo test masterdata`: 25/25 (2 new). `python scripts/validate_v1_tracker.py`: OK, 171 tasks, no drift. `git diff --check`: clean (LF/CRLF warnings only). **HARDENING (2026-08-19, Session 7 — second independent review, the single most important gap in the whole FVL-04.013-.018 block, plus real transformation-safety fixes):** a second independent review found that Session 6's own `ReferenceStore` E2E proof, while genuinely improved over the prior no-resolver-at-all state, still only proved a TEST path — the REAL production import screen, `apps/desktop/src/components/dataExchange/DataExchangeImportDialog.tsx`, was found to call `previewDataExchangeImport()` with NO `resolveReference` at all. **Real production imports never validated `code_reference` existence.** Fixed by reusing the EXISTING per-template existing-record authority (`dataExchangeExisting.ts`'s own `loadExisting`/`loadExistingFormulaBom`, already used for create-vs-update classification) through a new `buildReferenceResolver(referenceTemplates)`, wired into the dialog's own preview call — no new reference engine, no material/supplier-specific branch, generic and registry-driven. This immediately surfaced 5 pre-existing `DataExchangeImportDialog.test.tsx` fixtures that had never actually seeded their referenced parent records (inventory referencing a material never created; material_suppliers referencing a material/supplier never created; three finished_product_specifications fixtures referencing a SKU/TestDefinition never created) — they only ever "passed" because production validated nothing at all; fixed by seeding the referenced parents, matching what a real user would need to import first. `connectorEndToEnd.test.ts` rebuilt around the SAME `buildReferenceResolver()` (never a parallel test-only implementation), backed by a realistic in-memory masterdata store that only grows through real `commitDataExchangeRows()` calls. **Real transformation runtime-throw risk fixed**: `map_boolean`'s runtime previously cast `config.trueValues`/`falseValues` to `string[]` without checking the shape — a malformed non-array value would have thrown `TypeError: ...some is not a function` if it ever bypassed profile validation; fixed with explicit `Array.isArray`/per-member `typeof` checks at BOTH runtime and profile-validation time, proven by a new sweep test calling `applyTransformation()` directly with garbage config across every op and asserting none throws. `map_enum` gained the equivalent value-type check; `parse_decimal`'s `decimalSeparator`/`groupSeparator` restricted to explicitly supported values (`.`/`,` for decimal; a small supported set for grouping) rather than an arbitrary non-empty string. `resolve_crosswalk` now requires an explicit `sourceEntity` or `sameEntity:true` (no more accidental fallback to `ctx.currentEntity`), and a configured `fallbackCanonicalField` is validated against the discovered source schema at profile-validation time. `connectorEndToEnd.test.ts`'s FAIL1-FAIL20 matrix now asserts explicit no-mutation (the realistic in-memory store genuinely received no write) for every relevant scenario, not merely a returned error code; FAIL20 is now an EXECUTABLE secret-exclusion assertion, not a source-text inspection. `pnpm --filter @formulab/shared test`: 40/40 in `transformation.test.ts` (10 new), full suite green. `pnpm --filter @formulab/desktop test`: `connectorEndToEnd.test.ts` rebuilt to 24 tests; `DataExchangeImportDialog.test.tsx` 14/14 after the 5-fixture fix; full suite green. `typecheck`/`lint`: clean both packages. `cargo test masterdata`: 28/28 (3 new: `apply_upsert_*`, the real merge/rejection logic extracted into a directly-unit-testable pure function). `python scripts/validate_v1_tracker.py`: OK, 171 tasks, no drift. **HARDENING (Session 8, 2026-08-19/20 — final correction closing FVL-04.013-.018, four parts):** a narrow final-correction session found Session 7's own closure language ("all `code_reference` fields are fully validated") was true for existence-checking in general but silently used the wrong key for every composite-target reference — corrected here. **(1)** `resolveReference` contract widened to `(referenceTemplate, referenceField, key)`; new `resolveColumnReferenceField()` (`dataExchangeValidation.ts`) is the single authority for which field a column resolves against (explicit `referenceField`, else the target's own single natural-key field, else a deterministic `configError` — never a guess); `buildReferenceResolver()` rewritten field-aware. Registry audit: all ~94 real `code_reference` columns already carry explicit `referenceField` — the bug was resolver logic, not registry data — now locked in by a new `dataExchangeRegistry.consistency.test.ts` (57 tests). **(2)** the blanket self-reference bypass removed from the validator; self-reference now resolves through the same field-aware path, with same-file forward references explicitly, deliberately unsupported (SELF1-4). **(3)** `effectiveMappingProfileStatus()`/`validateMappingProfileSupersession()` corrected to an exact linear version chain (no gaps, no branching) with active-successor-only effective supersession (MP1-MP12, see .016's own row). **(4)** `FileConnectorInput`/`FileConnectorSource` strengthened into discriminated unions, removing silent empty-content fallbacks (see .014's own row). Closure-level acceptance rebuilt around the new contract: REF1-REF11 in `connectorEndToEnd.test.ts` (single-key AND composite-key targets, including two genuine REQUIRED-reference-into-composite-target hard-block proofs — `artwork_register.label_code`->`label_content`, `doe_observations.response_code`->`doe_factors_responses` — and one honest non-blocking composite proof, `finished_products.packaging_sku_code`->`packaging_bom`, since no REQUIRED reference into `packaging_bom` exists anywhere in the registry); four new dialog-level acceptance tests (A-D) in `DataExchangeImportDialog.test.tsx` against the real production dialog, using `artwork_register` (the one real template with both a REQUIRED composite-key reference and a self-reference column). Full re-verification: `pnpm --filter @formulab/shared test`: 1575/1575 across 74 files. `pnpm --filter @formulab/desktop test`: 1555/1555 across 158 files (`connectorEndToEnd.test.ts` 30/30, `DataExchangeImportDialog.test.tsx` 18/18). `typecheck`/`lint`: clean both packages. No Rust file touched this session — `cargo check`/`cargo test masterdata` not re-run. `python scripts/validate_v1_tracker.py`: OK, 171 tasks, no drift. `git diff --check`: clean (LF/CRLF warnings only). See `docs/FVL04_EXTERNAL_SOURCE_CONNECTOR_ARCHITECTURE.md`'s Session 8 section for full detail. FVL-04.013-.018 FINAL-HARDENED AND COMPLETED. FVL-04 EXTERNAL CONNECTOR FOUNDATION — FINAL CLOSURE VERIFIED. FVL-04 = 18/26 COMPLETED. NEXT: FVL-04.019 — NOT STARTED. |
| FVL-04.019 | Formula / Recipe Relationship Import — customer recipe/formula migration into existing Formulation/FormulationVersion/FormulationLine: preserve source formula ID and source version ID if present, resolve materials through the external-ID crosswalk, keep unresolved materials explicit, preserve exact source percentages/quantities, validate units and relationship integrity, validate mass/composition structure using existing FormuLab validation, never invent missing concentration, never silently identify a material from a trade name, retain original source lineage. | FVL-04.017, FVL-04.018 | YES | COMPLETED (2026-08-20) — audit found the existing `formula_bom` Data Exchange template (registered since an earlier session) already covers most of this task: real reference validation on `material_code` (REQUIRED, `raw_materials`), exact decimal percent/quantity passthrough, `formula_code`/`formula_version` preserved directly as the canonical `Formulation.code`/`FormulationVersion.versionNumber` (blank `formula_version` auto-appends the next version rather than fabricating one), no trade-name matching anywhere (exact code match only). One real gap found and fixed: `commitFormulaBom` (`apps/desktop/src/lib/dataExchangeCommit.ts`) built the saved version through the bare `newVersion()` helper, which never computes `totalsSnapshot`/`validationSnapshot` — an imported formula silently skipped the SAME mass/composition-structure validation (`validateFormula()`, `engine/formula.ts`) every hand-authored version gets. Fixed by switching to the single-authority `createVersion()` (`engine/versioning.ts`), the exact function the real Formula Builder save path already uses — findings are attached, never blocking, matching the builder's own non-blocking discipline. New end-to-end test proves a customer recipe's material reference resolves through the REAL External ID Crosswalk (`resolve_crosswalk`, not a raw copy of the customer's own ID) before reaching `formula_bom.material_code`, fans two lines into one real `FormulationVersion`, and that version's `totalsSnapshot.totalPercent`/`validationSnapshot.errorCount` are genuinely computed (100.0000%/0 errors), not silently blank. Two disclosed, deliberately non-blocking scope decisions: (1) `quantity_unit` has no dedicated known-unit check on this template — it is not a required field, and unit normalization is already the mapping/transformation layer's own job (`convert_unit`) upstream of Data Exchange, not duplicated here; (2) "retain original source lineage" is satisfied by the EXISTING Data Exchange import-job history (`data_exchange_import_jobs`/`data_exchange_import_row_results`) plus the connector's own `MappingResult.trace`/`sourceLineage` — deliberately NOT a second lineage field duplicated onto `Formulation`/`FormulationVersion`, consistent with FVL-04.024's own "no second import-history model" requirement. `pnpm --filter @formulab/desktop test`: 1556/1556 across 158 files (2 new: 1 in `dataExchangeCommit.test.ts`, 1 in `connectorEndToEnd.test.ts`). `typecheck`/`lint`: clean. `python scripts/validate_v1_tracker.py`: OK. No `packages/shared` file touched this task. **Session 10/11 correction**: an independent audit found `formula_bom`'s registry `quantity`/`quantity_unit` columns were read from source files but silently dropped (`FormulationLine` had no field to receive them) — fixed with additive optional `quantity`/`quantityUnit` fields, wired through `commitFormulaBom`. New acceptance A1-A10 (two distinct versions, deterministic line order, phase, quantity/unit, q.s., 99.7%/103% findings preserved exactly, and A10: an EXPLICIT source-supplied `formula_version=1` then `=2` preserved exactly, never merely "next generated after existing", remaining independently addressable/immutable). A genuinely RELATIONAL two-entity source (`FormulaHeader`+`FormulaLine`, real `DatabaseAdapter`, real FK) proven separately: header/lines staged independently, the real FK relationship verified directly, deterministic order despite scrambled read order, `resolve_crosswalk`-based material identity, and an unresolved-material formula blocking with zero partial commit (`connectorEndToEnd.test.ts`). **Session 12 correction**: that relational acceptance still relied on a test-local `filter()`/manual merge — no real, reusable, config-driven relationship-assembly path existed. Closed with `packages/shared/src/engine/relationalAssembly.ts` (`assembleRelationalRecords()`/`wrapAssembledSource()`, generic, no per-customer branch) and a real production-path test: `DatabaseAdapter` -> two independently extracted entities -> the generic join -> the UNCHANGED `prepareConnectorImport()`/`confirmConnectorImport()` -> a genuine `Formulation`+`FormulationVersion`, proving deterministic order, missing-header/missing-material blocking with zero partial commit, and explicit-version immutability.  **FVL-04 close-out correction (2026-08-20):** the relational FormulaHeader+FormulaLine production path now assembles through the generic, config-driven / (), proven by "FVL-04.019 Section 1" in  — never a test-local /manual merge.  **FVL-04 close-out correction (2026-08-20):** the relational FormulaHeader+FormulaLine production path now assembles through the generic, config-driven `assembleRelationalRecords()`/`wrapAssembledSource()` (`packages/shared/src/engine/relationalAssembly.ts`), proven by "FVL-04.019 Section 1" in `customerMigrationFixture.test.ts` — never a test-local `filter()`/manual merge. |
| FVL-04.020 | Laboratory / Test Result Relationship Import — migration of customer laboratory/history records into existing LaboratoryTrial/TestResult and applicable existing stability/DOE linkage: external trial ID, external formula/version reference, sample/test references, measurement metadata, timestamps, method/device where available, units, raw values, source attachments/references where available, unresolved-linkage detection, provenance. Not a second laboratory platform — FVL-06 remains responsible for the structured measured-response expansion and instrument-specific performance model; this is the enterprise migration/mapping bridge into the existing records only. | FVL-04.017, FVL-04.018 | YES | COMPLETED (2026-08-20) — audit found the existing `lab_results` Data Exchange template already covers most of this task: `trial_code`/`test_code` (REQUIRED, `test_code` registry-validated against `test_definitions`; `trial_code` has no `referenceTemplate` since `laboratory_trials` is deliberately not itself Data-Exchange-importable — a trial can only be created through the real Laboratory workspace, matching the task's own "migration bridge into EXISTING records" framing — its existence is checked at commit time via `findByCode`, the SAME established pattern every other non-importable-parent reference in the registry already uses, e.g. `doe_observations.study_code`), replicate grouping, raw numeric/text values preserved exactly, `passFail` always forced `not_evaluated` on import (never trusts an imported verdict). Two real gaps found and fixed: (1) the template's own `instrument` column was read from the file but silently dropped — never reached the saved `TestResult` at all, since the schema itself had no field for it; added `instrument?: string` to `testResultSchema` (additive, optional, no migration needed — masterdata collections are untyped JSON on the Rust side, confirmed no Rust struct mirrors `TestResult`) and wired it through `commitLabResults`. (2) `project_code`/`formula_version` were accepted by the template but never used for anything — added a cross-check: when provided, they must match the RESOLVED trial's own real link (`LaboratoryTrial.projectId`/`sourceFormulaVersionId` — that transitive link already exists and is never duplicated onto `TestResult`), refusing the commit with a clear message on a genuine mismatch — real "unresolved-linkage detection" for a wrong `trial_code` silently accepted for the wrong formula, rather than silently attaching a result to an unrelated trial. Two disclosed non-blocking scope decisions, matching FVL-04.019's own: `unit` has no dedicated known-unit check on this template (unit normalization stays the upstream mapping/transformation layer's job); "provenance"/source lineage is satisfied by the EXISTING Data Exchange import-job history, never a second lineage model. `pnpm --filter @formulab/shared test`: 1575/1575 (74 files, `testResultSchema` change). `pnpm --filter @formulab/desktop test`: 1559/1559 (158 files, 3 new). `typecheck`/`lint`: clean both packages. `python scripts/validate_v1_tracker.py`: OK. No Rust file touched. **Session 10/11 correction**: an independent audit found lab-result migration required a `LaboratoryTrial` to already exist manually, permanently blocking end-to-end LIMS migration — fixed with `findOrCreateTrial()`, reusing the real `snapshotFormulaForTrial()`/masterdata bridge `TrialsPanel.tsx` itself uses; auto-creates ONLY when `project_code` resolves to a real `Formulation`+version, otherwise the original honest error. New acceptance B2-B8 (auto-creation, multi-sample/multi-test/replicate 1/2/3, numeric/text/missing values, full unit/instrument/timestamp/analyst metadata, exact `test_code` resolution, no partial commit on a missing TestDefinition). The full REST LIMS -> SourceConnector -> Schema Discovery -> Mapping Profile -> Data Exchange -> trial resolution -> commit path is proven end-to-end against a real local HTTP server in `customerMigrationFixture.test.ts` (MIG14-21). **Session 12 correction**: re-verified against Section 3's list of required LIMS acceptance items — all were already genuinely proven (Session 10 Part B + the fixture's own MIG14-21); no new gap found, no new work needed here this session.  **FVL-04 close-out correction (2026-08-20):** no change required to this task's own scope; the crosswalk-preflight and CANONICAL_MISSING fixes below (FVL-04.023/.024 rows) apply uniformly to any  identity this task's import path uses.  **FVL-04 close-out correction (2026-08-20):** no change required to this task's own scope; the crosswalk-preflight and CANONICAL_MISSING fixes below (FVL-04.023/.024 rows) apply uniformly to any `create_or_update` identity this task's import path uses. |
| FVL-04.021 | Generic Database Read Connector — generic READ-ONLY database connector capability (SQL Server, PostgreSQL, MySQL/MariaDB, Oracle where practical, SQLite, ODBC-compatible sources where supported) — generic connectivity, not customer-specific business logic. Read-only by architecture, configurable queries/views, no writes back to the customer database, credentials/secrets handled securely, extraction goes to source staging then mapping then the existing Data Exchange. No SAP/Dynamics/etc. business logic built into this task. | FVL-04.013, FVL-04.015 | NO | COMPLETED (2026-08-20) — new `packages/shared/src/engine/databaseConnector.ts` implements a real generic `SourceConnector` for databases, the same shape `createFileConnector()` already implements for files, and every staged row funnels through the SAME `stageRows()` staging path CSV/XLSX/JSON/XML already use (`fileConnector.ts`) — generalized to accept its own `connectorType` (defaulting to `"FILE"`, zero behavior change for every existing caller) so `ConnectorResult.connector.connectorType` genuinely reports `"DATABASE"` rather than a leftover `"FILE"`. Read-only enforced two ways: (1) `DatabaseConnectorDeps` exposes exactly one capability, `executeQuery` — no write method exists on the contract, matching `SourceConnector` itself; (2) `assertReadOnlyQuery()` structurally refuses a query whose own leading statement keyword is write-shaped (INSERT/UPDATE/DELETE/MERGE/DROP/ALTER/CREATE/TRUNCATE/EXEC/EXECUTE/CALL/GRANT/REVOKE) BEFORE it is ever handed to the injected adapter — comments stripped first so a write keyword hidden in one can't smuggle a real statement past the check, and proven NOT a naive substring search (a `SELECT ... WHERE description LIKE '%update%'` correctly passes). Credentials are never handled by this layer: `DatabaseQuerySpec.connectionRef` is an opaque string, no host/port/username/password field exists anywhere on the contract — the real driver-backed adapter (SQL Server/PostgreSQL/MySQL/MariaDB/Oracle/SQLite/ODBC) is desktop-only, injected via `executeQuery`, and is explicitly NOT built this task, the same "engine layer now, real adapter wired by a later desktop-integration task" boundary `fileConnector.ts`'s own `readWorkbook` injection point already established for XLSX before `apps/desktop/src/lib/xlsx.ts`'s real ExcelJS adapter existed — disclosed, not silently assumed. Sanitized failure paths (never leaks a raw adapter exception, proven with a fixture containing a real fake credential string) and a genuinely-empty-vs-malformed result distinction (zero columns is a structured error; zero rows with real columns is not) both proven directly. New `databaseConnector.test.ts` (15 tests). `pnpm --filter @formulab/shared test`: 1590/1590 across 75 files (15 new). `pnpm --filter @formulab/shared typecheck`/`pnpm --filter @formulab/desktop typecheck`: clean. No `packages/shared` file's existing behavior changed for any pre-existing caller. **Session 10/11 correction — the "no real database driver wired" disclosure above is now FALSE and superseded**: the primary public model was rewritten to remove arbitrary SQL entirely — `DatabaseAdapter` (`listSchemas`/`listTables`/`describeEntity`/`readPage`, structurally no write method) is the sole contract; the old keyword-refusal free-form-SQL path survives only as a clearly separate, differently-named "expert" boundary (`stageDatabaseExpertQuery`), unreachable from a mapping profile. A genuinely working disposable adapter (`sqliteTestAdapter.ts`, real sql.js/WASM SQLite) proves it end-to-end: composite-PK deterministic identity, real paging, SQL-injection-safe filters, and — Session 11 hardening — deterministic ORDER BY on every paged read (real PK order, composite PK respected in ordinal order, `rowid` fallback when no PK exists; the prior LIMIT/OFFSET-with-no-ORDER-BY could duplicate/skip/reorder rows across pages) plus a real DB-derived ordinal-fallback identity proven, through the real crosswalk persistence path, to never be crosswalk-eligible (DB1-DB20). **Session 12 correction**: `CANONICAL_MISSING`'s own existence check now decodes `prior.targetRecordId` itself as the live-lookup key (`priorTargetStillExists()`, `create_or_update` templates only, honestly unresolved for append-only ones) instead of inferring existence from the CURRENT candidate's own (possibly-drifted) natural key — three new regression tests cover the three real buckets.  **FVL-04 close-out correction (2026-08-20):** REST timeout cancellation is now unconditional in  (a real  is always constructed and attached before a request is issued, failing closed if one genuinely cannot be built) rather than an opt-in the production adapter-creation path never actually supplied — proven by REST-CANCEL-1/2/3/4/6 in .  **FVL-04 close-out correction (2026-08-20):** REST timeout cancellation is now unconditional in `createHttpFetchAdapter()` (a real `AbortController` is always constructed and attached before a request is issued, failing closed if one genuinely cannot be built) rather than an opt-in the production adapter-creation path never actually supplied — proven by REST-CANCEL-1/2/3/4/6 in `httpFetchAdapter.test.ts`. |
| FVL-04.022 | REST API Connector Contract — REST-based external extraction using the same connector contract: API endpoint/entity configuration, pagination, authentication references, incremental cursors/timestamps where exposed by the source, error/retry semantics, source identity, raw-response lineage. Auth mechanisms limited to what implementation later proves necessary (e.g. API key, Basic, OAuth2/client credentials). No vendor-specific API implementation in the tracker unless separately approved later. | FVL-04.013, FVL-04.015 | NO | COMPLETED (2026-08-20) — new `packages/shared/src/engine/restApiConnector.ts` implements a real generic `SourceConnector` for REST APIs, the same shape `createFileConnector()`/`createDatabaseConnector()` already implement. `RestConnectorSource.endpoints` is caller-configured `entity -> path` only, never a hardcoded vendor integration. Pagination follows each page's own `nextCursor` (whatever convention the real API uses — a query param, a header, an offset — is entirely the injected `fetchPage` adapter's own concern, never guessed or string-concatenated by this layer), capped by a `maxPages` safety limit (default 500) so a misbehaving/malicious API cannot page this connector forever — records fetched before the cap are kept, a real `pagination_limit_reached` warning is attached, nothing beyond the cap is fabricated. Every page's JSON body reuses the SAME `stageJsonFile()` flattening/staging logic the generic FILE connector's own JSON support already has (generalized, like `stageRows()` in the same task, to accept its own `connectorType` — zero behavior change for existing callers) — real gap found and fixed while wiring this: staging each page independently gave every un-configured-`idField` record an ordinal identity relative to its OWN page, so page 2's first record would silently COLLIDE with page 1's first record on the same ordinal `sourceRecordId`; fixed with a post-merge renumbering pass across the WHOLE batch when no `idField` is configured (a real configured external ID is never touched by it), proven by a dedicated test. Auth is deliberately not a request-shaping concern in this shared-package layer: `connectionRef` is an opaque reference resolved and applied entirely server-side by the desktop-only `fetchPage` adapter (API key/Basic/OAuth2 client-credentials — whichever a later implementation actually needs) — this module never issues an HTTP request itself and never sees a raw credential, the same "engine now, real adapter wired later" boundary FVL-04.021's own `executeQuery` and FVL-04.014's own `readWorkbook` already established. Sanitized failure paths (never leaks a raw adapter exception, proven with a fixture containing a real fake bearer token) proven directly. New `restApiConnector.test.ts` (11 tests). `pnpm --filter @formulab/shared test`: 1601/1601 across 76 files (11 new). `typecheck`: clean both packages. **Session 10/11 correction — the "no real HTTP client wired" disclosure above is now FALSE and superseded**: a real GET-only `fetch()`-backed adapter (`httpFetchAdapter.ts`) exists — method hardcoded, no configuration path to POST/PUT/PATCH/DELETE; declarative page/offset/cursor pagination; explicit `recordArrayPath`; `sanitizeUrl()` redacts query values before any URL reaches a structured error; `HttpStatusError`/`retryableForStatus()` give real HTTP status semantics. Session 11 hardening closed three remaining gaps: (A) a configurable client-side request timeout (`Promise.race()`-based — deliberately not `AbortController`/`fetch`'s own `signal`, since a signal built in one JS realm mishandles across realms, a real regression this session found and fixed), proven against a real server route that never responds; (B) audited and proven (not reimplemented) that an explicit DOTTED `idField` (e.g. `"external.id"`) already resolves a nested identity correctly via the existing JSON-flattening staging logic; (C) audited the auth boundary — `RestConnectorSource`/`DatabaseConnectorSource` carry only an opaque `connectionRef`, headers are always caller-supplied at call time, no credential field exists on any persisted connector/profile schema. REST1-REST23 all real-HTTP-server-backed. **Session 12 correction**: the Promise.race() timeout bounded caller wait but never cancelled the underlying fetch. Fixed with an opt-in `createAbortController` factory (never constructed internally/by default — a real, reproduced cross-realm jsdom/undici `AbortSignal` incompatibility, confirmed via `TypeError: RequestInit: Expected signal ... to be an instance of AbortSignal`, ruled out constructing it internally). REST-TIMEOUT-2 proves genuine server-side connection termination (the real server's own "close" event) when a caller opts in; REST-TIMEOUT-3 proves normal requests are unaffected either way.  **FVL-04 close-out correction (2026-08-20):** append_history/new_revision crosswalk reconciliation no longer has an append-only blind spot —  () resolves prior-target existence generically from the real / for every duplicatePolicy, and the crosswalk preflight now uses Import History's own  as the reconciliation reference for append-only templates — proven by the new XW-APPEND suite in . A real fingerprint-comparison bug was also found and fixed in the same pass: candidate-side fingerprints were computed from raw pre-validation values, causing a false CANONICAL_LOCAL_CONFLICT on any reimport of a byte-identical decimal/currency/percentage field formatted differently in the source.  **FVL-04 close-out correction (2026-08-20):** append_history/new_revision crosswalk reconciliation no longer has an append-only blind spot — `priorTargetExists()` (`dataExchangeExisting.ts`) resolves prior-target existence generically from the real `targetCollection`/`targetRecordId` for every duplicatePolicy, and the crosswalk preflight now uses Import History's own `prior.targetRecordId` as the reconciliation reference for append-only templates — proven by the new XW-APPEND suite in `connectorImportBridge.test.ts`. A real fingerprint-comparison bug was also found and fixed in the same pass: candidate-side fingerprints were computed from raw pre-validation values, causing a false CANONICAL_LOCAL_CONFLICT on any reimport of a byte-identical decimal/currency/percentage field formatted differently in the source. |
| FVL-04.023 | Incremental Re-import / Conflict Handling — repeatable enterprise re-import behavior: same external record maps to the same canonical identity via the crosswalk; detect new vs updated vs unchanged source rows; detect mapping conflicts; detect deleted/missing source records without silently deleting canonical history; no duplicate canonical records on repeated import; dry-run/preview before commit; preserve import batch/job lineage; deterministic conflict outcomes; human decision required for ambiguous/destructive changes. | FVL-04.017, FVL-04.024 | YES | COMPLETED (2026-08-20) — audit found most of this task already exists and is reused, never reimplemented: "same external record -> same canonical identity" and "detect mapping conflicts" are the EXISTING External ID Crosswalk's own job (`resolveCrosswalk()`/`upsertCrosswalk()`, XW1-XW9 — a conflicting write is refused, never silently overwritten); "new vs updated vs unchanged" and "no duplicate canonical records" are the EXISTING Data Exchange preview's own natural-key-driven `valid_create`/`valid_update`/`unchanged` classification; "dry-run/preview before commit" and "preserve import batch/job lineage" are the EXISTING `data_exchange_import_jobs`/`data_exchange_import_row_results` import-history model. The one genuinely missing piece — detecting a source record that existed in a PRIOR import batch for a template but is ABSENT from the CURRENT one, "without silently deleting canonical history" — closed with a new pure `detectMissingFromSource()` (`packages/shared/src/engine/dataExchangeIncremental.ts`) comparing the current batch's natural keys against a NEW `loadPriorCommittedRows()` (`dataExchangeExisting.ts`) that reads the EXISTING import-history model for the MOST RECENT completed job of the exact same template — never a second batch-tracking store. Deliberately non-destructive: never deletes/archives a canonical record itself, only surfaces a structured finding; wired into `DataExchangeImportDialog.tsx` as a purely informational banner (never affects `canCommit`) listing which previously-imported natural keys didn't reappear — real "human decision required" per the task's own wording, since nothing is ever auto-archived. New `COMMITTABLE_ROW_STATES` constant is the one real authority for which row states genuinely reached canonical storage, now also replacing the dialog's own previously-duplicated local literal. New tests: `dataExchangeIncremental.test.ts` (8, the pure comparison — dedup within one batch, no-natural-key rows never flagged, a re-submitted-but-otherwise-failing row never conflated with genuine absence), `dataExchangeExisting.test.ts` (5, `loadPriorCommittedRows` — most-recent-job-only selection never mixes an older job's rows in, cross-template isolation, committable-state filtering, incomplete jobs never used as baseline), `DataExchangeImportDialog.test.tsx` (2, the real banner rendering and non-blocking commit through the actual dialog). i18n: `missingFromSource`/`missingFromSourceItem` added to all 8 locales (English text in 6, real Turkish translation, matching the established convention). `pnpm --filter @formulab/shared test`: 1609/1609 across 77 files (8 new). `pnpm --filter @formulab/desktop test`: 1569/1569 across 158 files (7 new). `typecheck`/`lint`: clean both packages. `python scripts/validate_v1_tracker.py`: OK. No Rust file touched. **Session 10/11 correction**: `CHANGED`/`CANONICAL_LOCAL_CONFLICT`/`CANONICAL_MISSING`/`MAPPING_PROFILE_CHANGED`/`CROSSWALK_CONFLICT` were computed by Session 9 but a genuine wiring bug made `CANONICAL_LOCAL_CONFLICT` structurally unreachable as its own distinct state (it compared the freshly re-mapped SOURCE candidate against itself, never the LIVE canonical record) — fixed with `loadLiveCandidateFields()`. Session 11 then closed the real remaining gap: these unsafe states were CLASSIFIED but never ENFORCED — a row in one of them could still silently enter the normal committable path. Now `isRowCommittable()` is the one deterministic commit-eligibility authority (considers preview validity AND conflict-safety together), and every one of the five unsafe states blocks the WHOLE batch (same atomic preflight as invalid/reference_missing) — zero silent overwrite, explicit human resolution required. `CROSSWALK_CONFLICT` is now preflighted BEFORE any canonical write, reusing the existing crosswalk authority read-only. `CANONICAL_MISSING` now uses the real live lookup, not preview-state inference. **Session 12 correction**: `CROSSWALK_CONFLICT` preflight still required `prior?.targetRecordId` (Import History), missing the real case of an active crosswalk with zero prior Data Exchange history at all. Now resolved directly from the real crosswalk store, independent of Import History, via a new `canonicalIdentityFor()` (create_or_update templates only). `crosswalkTargets` is now decided ONLY at `prepareConnectorImport()` time and carried inside the returned plan — `confirmConnectorImport()` no longer accepts an independent crosswalk-target argument at all, eliminating the prepare/confirm mismatch risk structurally rather than by runtime comparison. `confirmConnectorImport()` also now revalidates, immediately before committing, the exact live state (canonical fingerprint, crosswalk binding) each row's prepared classification depended on — a plan that went stale since review (edited/deleted canonical record, rebound crosswalk) is refused outright (TOCTOU-1 through TOCTOU-4). New XW-PREFLIGHT/XW-CONFIG/TOCTOU/CANONICAL_MISSING-bucket tests (`connectorImportBridge.test.ts`) and a real happy-path crosswalk-lifecycle test with source identity genuinely distinct from canonical identity (`customerMigrationFixture.test.ts`, "Section 10").  **FVL-04 close-out correction (2026-08-20):**  () now decodes existence directly from the prior commit's own real / (, ) for every duplicatePolicy, not only  — closing the previously-honest-but-unresolved append-only gap; proven by CANONICAL_MISSING semantics bucket 4 in .  **FVL-04 close-out correction (2026-08-20):** `priorTargetStillExists()` (`connectorImportBridge.ts`) now decodes existence directly from the prior commit's own real `targetCollection`/`targetRecordId` (`priorTargetExists()`, `dataExchangeExisting.ts`) for every duplicatePolicy, not only `create_or_update` — closing the previously-honest-but-unresolved append-only gap; proven by CANONICAL_MISSING semantics bucket 4 in `connectorImportBridge.test.ts`. |
| FVL-04.024 | Connector → Existing Data Exchange Bridge — the critical architectural integration task: connector output must enter the EXISTING Data Exchange lifecycle exactly (connector extraction → source staging → mapping/transformation → canonical import candidate → existing Data Exchange preview → existing validator → human review → existing explicit commit → existing import history). Prove there is NO second commit/import authority and no second import-history model — reuse the existing one. | FVL-04.014, FVL-04.015, FVL-04.016, FVL-04.017, FVL-04.018, FVL-04.019, FVL-04.020, FVL-04.021, FVL-04.022 | YES | COMPLETED (2026-08-20) — built before FVL-04.023 despite the ordinal numbering, since .023 itself depends on .024 (documented in the handoff, not silently reordered). This task was largely already proven true by construction across FVL-04.013-.022 (every fixture in `connectorEndToEnd.test.ts` already stages through a real connector, maps through `applyMappingProfile()`, and commits through the REAL `previewDataExchangeImport()`/`commitDataExchangeRows()` — never a parallel path) — the genuine, new-this-task gap was that the two connector types added THIS session (DATABASE, REST_API) had only ever been proven at the STAGING level (their own unit tests), never end-to-end through mapping/Data Exchange/commit the way FILE already was. Closed with two new `connectorEndToEnd.test.ts` tests: a DATABASE-sourced row (`stageDatabaseQuery`, mocked `executeQuery`) and a REST_API-sourced row (`stageRestEntity`, mocked `fetchPage`) both flow through the IDENTICAL `discoverSourceSchema` -> `MappingProfile` -> `applyMappingProfile()` -> `previewDataExchangeImport()` -> `commitDataExchangeRows()` chain FILE already uses, reaching real canonical `materials`/`suppliers` records — proving neither new connector type is a dead end that bypasses the existing lifecycle. A third new test extends the file's own established "no `sourceSystem === '...'` conditional" source-text audit to `databaseConnector.ts`/`restApiConnector.ts`, plus a new check that neither file imports `dataExchangeCommit`/`upsertRecords`/the masterdata bridge directly — structural proof neither connector module could BECOME a second write authority even by accident, on top of the repeated MAP9/TR16-style single-commit-authority audits already established in prior sessions. `pnpm --filter @formulab/desktop test`: 1562/1562 across 158 files (`connectorEndToEnd.test.ts` 34/34, 3 new). `typecheck`/`lint`: clean. `python scripts/validate_v1_tracker.py`: OK. No `packages/shared`/Rust file touched this task. **Session 10/11 correction**: Session 9's "end-to-end" proof was still tests manually chaining the pipeline — no real production orchestration module existed. Session 10 built the actual bridge (`connectorImportBridge.ts`): `prepareConnectorImport()`/`confirmConnectorImport()`, registry-driven dependency ordering with cycle detection, a disclosed non-transactional residual boundary (never a fake rollback claim), crosswalk persisted only after successful commit, and a real desktop entry point (`ConnectorBridgeImportDialog.tsx`, wired into the existing Data Exchange screen). Session 11 hardening closed three real defects an independent audit found: (A) Import History jobs for a DATABASE/REST import falsely claimed `fileType:"csv"`/`fileSize:0`/`sha256:extractionRunId` — `fileType` gains an honest `"connector"` value, `fileSize`/`sha256` become optional, and `extractionRunId`/`connectorVersion`/`sourceEntity`/`sourceSchemaFingerprint`/`mappingProfileVersion` get their own truthful optional fields; (B) `withBatchOverlay()`'s own doc comment claimed only an earlier-committing template could satisfy a same-batch forward reference, but the implementation checked the FULL unfiltered batch — fixed to track `earlierTemplates` explicitly; (C) a genuine RUNTIME commit-layer failure (never a synthetic injected outcome) is now proven end-to-end: the failing row's outcome is truthfully reported, zero crosswalk persisted for it, and later templates in the same batch are never even attempted. **Session 12 correction**: that runtime-failure row used an ordinal (not configured) source identity, so the zero-crosswalk-persisted assertion was a trivial consequence of `persistCrosswalkEntry()`'s own ordinal refusal, never a real proof of the failure path itself — strengthened to use a genuinely CONFIGURED identity with a crosswalk target actually reviewed at prepare. `SOURCE_MISSING` findings and bridge warnings are now also rendered in `ConnectorBridgeImportDialog.tsx` (previously computed by the engine but never surfaced anywhere in this UI), reusing the EXISTING `dataExchange.import.missingFromSource(Item)` i18n keys/rendering convention, purely informational, never blocking or destructive.  **FVL-04 close-out correction (2026-08-20):** the original MIG1-MIG35 acceptance matrix, supplied directly by the user as authoritative, is now restored verbatim in 's own top-of-file comment, with every item mapped to an exact named executable test (in this file or, where a more specific existing suite already proves it, by exact cross-reference). MIG9/MIG15/MIG18/MIG25/MIG33/MIG34/MIG35 — previously weak, N/A, or prose-only — now each have a real new executable test; MIG14/MIG24/MIG28 were strengthened in place with direct assertions.  **FVL-04 close-out correction (2026-08-20):** the original MIG1-MIG35 acceptance matrix, supplied directly by the user as authoritative, is now restored verbatim in `customerMigrationFixture.test.ts`'s own top-of-file comment, with every item mapped to an exact named executable test (in this file or, where a more specific existing suite already proves it, by exact cross-reference). MIG9/MIG15/MIG18/MIG25/MIG33/MIG34/MIG35 — previously weak, N/A, or prose-only — now each have a real new executable test; MIG14/MIG24/MIG28 were strengthened in place with direct assertions. |
| FVL-04.025 | Customer Migration Acceptance Fixture — future acceptance task (not its implementation now): a realistic enterprise customer fixture with intentionally non-FormuLab schemas covering materials, suppliers, prices, inventory, formulations/recipes, formulation versions, laboratory trials/test results, with deliberately different column names, IDs, decimal/date conventions, units, enum values, relationships. Must prove schema discovery, a saved mapping profile, the external-ID crosswalk, transformations, unresolved-data handling, repeat import without duplication, preview/validation, human explicit commit, lineage, final canonical records. Disposable fixtures only. | FVL-04.012, FVL-04.019, FVL-04.020, FVL-04.023, FVL-04.024 | YES | COMPLETED (2026-08-20) — new disposable synthetic fixture "GLOBAL_MFG" (`connectorEndToEnd.test.ts`, 4 tests) proves every explicitly-required item together, in one coherent customer scenario, all through REAL production engines (never a parallel test-only implementation): schema discovery over deliberately different column names (`MatlNr`/`VendNr`/`RecipeNr`/`TrialRef` etc., never FormuLab's own vocabulary), a genuinely SAVED mapping profile (persisted through the real `saveMappingProfile()`/append-only storage — the other fixtures in this file only ever build an in-memory `MappingProfile` object, a real gap this task closed), the external-ID crosswalk (`persistCrosswalkEntry`/`resolveCrosswalk`, a customer material ID resolved into the real canonical code before reaching `formula_bom`), real `parse_decimal`/`parse_date` transformations proving deliberately different conventions (European decimal comma `"3,20"` -> `"3.2"`, `DD/MM/YYYY` `"15/01/2026"` -> `"2026-01-15"`, never guessed), unresolved-data handling (a material with no crosswalk entry stays genuinely unmapped — `candidate.row.material_code` is `undefined`, never the raw customer ID smuggled through, and surfaces in `MappingResult.unresolved`), repeat-import-without-duplication (the identical batch committed twice updates the SAME canonical records in place — store length proven unchanged, not merely a preview-state assertion, since this test harness's own helper doesn't wire `existingNaturalKeys` the way the real production dialog already does — that distinction is disclosed here rather than silently glossed over), preview/validation and human-explicit-commit (every candidate goes through the real `previewDataExchangeImport()`/`commitDataExchangeRows()`, never a shortcut), lineage (every staged record carries a real `SourceLineage`; the formula/recipe test additionally proves a real `FormulationVersion` with genuine `totalsSnapshot` per FVL-04.019's own fix), and final canonical records across `materials`/`suppliers`/`material_prices`/a real `Formulation`+`FormulationVersion`/`test_results`. Domain coverage: materials, suppliers, prices, formulations/recipes+versions, and laboratory results are all exercised; `inventory_records` is NOT included in this specific fixture (already exhaustively covered by fixture 1's own inventory scenario elsewhere in this file, not duplicated here) — disclosed, not silently omitted. Deliberately-varied unit/enum-value conventions were not specifically exercised in this fixture beyond decimal/date (a narrower scope than the task's full wish-list; the mechanisms for both — `convert_unit`/`map_enum` — are already proven generically in `transformation.test.ts`, not re-proven per-customer here). `pnpm --filter @formulab/desktop test`: 1573/1573 across 158 files (`connectorEndToEnd.test.ts` 38/38, 4 new). `typecheck`/`lint`: clean. `python scripts/validate_v1_tracker.py`: OK. No `packages/shared`/Rust file touched. **Session 10/11 correction — Session 9's "closes the queued brief" claim above was disputed by an independent audit and did not hold**: the GLOBAL_MFG fixture never used a real DB/REST-backed source, never included inventory, never exercised a real mapping-profile version chain, and never proved a second migration's incremental/conflict states. Replaced with a genuinely real, end-to-end, TWO-MIGRATION fixture (`apps/desktop/src/lib/customerMigrationFixture.test.ts`, MIG1-MIG37, 9 tests): a real SQLite-backed ERP (`sqliteTestAdapter` — single+composite PK, FK, decimals, dates, booleans, price history, inventory), a legacy formulation JSON file (two formulas, scrambled line order, phase, a real 99.7% mass-balance finding, one unresolved-material negative), and a real REST-backed LIMS (real local `node:http` server + `httpFetchAdapter` — pagination, explicit IDs, replicates, numeric/text/missing values, a real 429 followed by a real operator retry). Real `convert_unit` (g->kg) and `map_boolean` transformations; a real v1->v2 mapping-profile chain proving `MAPPING_PROFILE_CHANGED`. The second migration proves NEW/UNCHANGED/CHANGED/SOURCE_MISSING/CANONICAL_LOCAL_CONFLICT/CANONICAL_MISSING/CROSSWALK_CONFLICT/MAPPING_PROFILE_CHANGED against real fixture data — never synthetic objects — all through the real production bridge, never manually chained. **This genuinely closes the FVL-04.019-.025 brief.** **Session 12 correction**: added MIG36/MIG37 (`CANONICAL_MISSING`/`CROSSWALK_CONFLICT` against the fixture's own real committed data), a real happy-path crosswalk-lifecycle test with source identity genuinely distinct from canonical identity ("Section 10"), the relational-production-path acceptance ("FVL-04.019 Section 1"), and — honestly — recovered that the "MIG1-MIG35" numbering itself is NOT a recoverable original scheme: the ORIGINAL Session 9 closure commit (`f9a2aa1`) contains zero "MIG" references, and no committed doc anywhere ever transcribed an exact original numbered list. The labels are Session 10's own invented tracking labels; the file now documents this truthfully and instead maps every required CATEGORY to its real, named, executable proof — see the file's own top-of-file comment. `pnpm --filter @formulab/shared test`: 1692/1692 across 81 files. `pnpm --filter @formulab/desktop test`: 1639/1639 across 161 files. `typecheck`/`lint`: clean both packages. `python scripts/validate_v1_tracker.py`: OK.  **FVL-04 close-out correction (2026-08-20):** see FVL-04.019/.023/.024/.025 rows above for this session's corrections; the production bridge UI () was re-audited against the 10-point safety checklist and found already compliant (explicit commit required, blocking issues disable commit, SOURCE_MISSING informational-only, warnings visible, exact-name mapping never guesses, TOCTOU automatic inside confirm, no DB/REST connection UI invented) — no change needed.  **FVL-04 close-out correction (2026-08-20):** see FVL-04.019/.023/.024/.025 rows above for this session's corrections; the production bridge UI (`ConnectorBridgeImportDialog.tsx`) was re-audited against the 10-point safety checklist and found already compliant (explicit commit required, blocking issues disable commit, SOURCE_MISSING informational-only, warnings visible, exact-name mapping never guesses, TOCTOU automatic inside confirm, no DB/REST connection UI invented) — no change needed. |
| FVL-04.026 | Human-Readable Literature & Formulation Artifact Naming Convention — ONE deterministic, cross-platform-safe naming convention for (A) downloaded literature/source documents and (B) saved/exported formulation artifacts, replacing opaque names like `DOI-209899898789.pdf` with a human-browsable filename while keeping stable identifiers for uniqueness/provenance. Literature: a display title separate from the physical filename (`<First Author> (<Year>) — <Short Human-Readable Title>`, e.g. "Sharma (2024) — Herbal Anti-Dandruff Shampoo Formulation and Evaluation") and a deterministic filename `LIT_<Year>_<FirstAuthor>_<ShortTitle>_<StableSourceId>.<ext>` (year/author fall back to `UnknownYear`/`UnknownAuthor`; DOI slashes and filesystem-illegal characters sanitized deterministically; original extension preserved; no illegal Windows characters; no trailing dot/space; reasonable max length; collision-safe deterministic suffix). Formulation: a display title `<Product Family> — <Formula Name> — <Formula Code> — V<Version>` and a deterministic export filename `FORM_<ProductFamily>_<ShortFormulaName>_<FormulaCode>_V<Version>_<ArtifactType>.<ext>` (e.g. "FORM_Shampoo_Anti-Dandruff_FML-0042_V03_Formula.xlsx") — never renaming canonical internal database IDs. The ORIGINAL source filename, URL/source, DOI/source ID, acquisition timestamp, and content hash (if already available) are always preserved as provenance metadata, never destroyed by display renaming — reuse existing provenance/storage models, no duplicate document registry. | FVL-01, FVL-03.011 | NO | |

---

## FVL-05 — Historical Experiment Dataset Builder

A derived, versioned dataset builder over existing source-of-truth records.
Not a duplicate knowledge base. No mutation of source records.

**Existing dependency / baseline capability**: Formula versions, Laboratory
Trials, TestResult, Stability Studies, DOE studies/runs, corrective
actions, cost snapshots — all already implemented per
`IMPLEMENTATION_STATUS.md`.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-05.001 | Define dataset schema version + feature schema version (explicit, incrementable) | FVL-02, FVL-03 | YES | |
| FVL-05.002 | Row/entity lineage model: every dataset row cites its exact source record IDs | FVL-05.001 | YES | |
| FVL-05.003 | Extractor: formula version + exact composition + materials + material properties + product family | FVL-05.002 | YES | |
| FVL-05.004 | Extractor: process plan + actual process observations (from Manufacturing Procedure / real trial records) | FVL-05.002 | YES | |
| FVL-05.005 | Extractor: LaboratoryTrial + TestResult | FVL-05.002 | YES | |
| FVL-05.006 | Extractor: stability studies/results | FVL-05.002 | NO | |
| FVL-05.007 | Extractor: DOE studies/runs/observations | FVL-05.002 | NO | |
| FVL-05.008 | Extractor: corrective actions when relevant, cost snapshots, packaging/context, environmental/test conditions | FVL-05.002 | NO | |
| FVL-05.009 | Normalization: units, categorical, numeric — missing values stay missing, no missing-to-zero unless mathematically/domain-explicitly valid | FVL-05.003–008 | YES | |
| FVL-05.010 | Exact target-variable definitions (per product family / measured response) | FVL-05.009 | YES | |
| FVL-05.011 | Dataset hash/fingerprint + reproducible rebuild from source records | FVL-05.009 | YES | |
| FVL-05.012 | Train/validation/test partition rules with leakage prevention (no same-formula-version rows split across partitions) | FVL-05.010 | YES | |
| FVL-05.013 | Immutable historical linkage — dataset build never mutates a source record | FVL-05.003–008 | YES | |
| FVL-05.014 | Regression tests: rebuild determinism, missing-value handling, leakage checks, lineage round-trip | FVL-05.011, FVL-05.012 | YES | |

---

## FVL-06 — Instrument & Performance Result Ingestion

Extends the existing Laboratory/TestResult/Data Exchange architecture. Not
a second lab platform.

**Existing dependency / baseline capability**: Laboratory Trials + Stability
Studies (spec §9), Data Exchange import/commit layer.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-06.001 | Define structured measured-response schema (pH, viscosity, foam, density, active matter, and only product-family-specific responses with real documented definitions) | FVL-04 | YES | |
| FVL-06.002 | Spectrophotometric/detergent-performance metric schema (device, method, metric, unit, substrate/fabric, stain type, pretreatment, wash/test conditions, control/reference, replicate) | FVL-06.001 | YES | |
| FVL-06.003 | Raw measurement + normalized-performance field (only when a deterministic, documented normalization exists — otherwise leave raw-only) | FVL-06.002 | YES | |
| FVL-06.004 | Raw-file attachment/provenance linkage | FVL-06.002 | NO | |
| FVL-06.005 | Linkage fields: `formulaVersionId`, `trialId`, `sampleId`, `testDefinitionId`, timestamps | FVL-06.001 | YES | |
| FVL-06.006 | CSV/Excel instrument import routed through existing Data Exchange (no new import path) | FVL-06.001–005 | YES | |
| FVL-06.007 | Validation rules for instrument imports (unit consistency, required linkage fields present) | FVL-06.006 | YES | |
| FVL-06.008 | Import history / commit behavior reuses existing Data Exchange job lifecycle | FVL-06.006 | NO | |
| FVL-06.009 | Feed instrument/performance data into FVL-05's dataset extractors | FVL-06.001–005, FVL-05.005 | YES | |
| FVL-06.010 | Real sample instrument-export acceptance test (disposable fixture) | FVL-06.006 | YES | |

---

## FVL-07 — Predictive Performance Engine

Supervised ML, **not** an LLM. Learns from real historical formulation +
experiment data (FVL-05/FVL-06) to predict measurable performance.

**Existing dependency / baseline capability**: none — this is genuinely new,
built strictly on top of FVL-05's dataset builder.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-07.001 | Eligibility: minimum dataset size + minimum target observations per product family/target | FVL-05 | YES | |
| FVL-07.002 | Eligibility: missing-data rules, applicability-domain requirements | FVL-07.001 | YES | |
| FVL-07.003 | Eligibility: explicit `INSUFFICIENT_DATA` outcome (never trains/serves below threshold) | FVL-07.001, FVL-07.002 | YES | |
| FVL-07.004 | Feature generation: composition + material/property features, with provenance | FVL-05.003 | YES | |
| FVL-07.005 | Feature generation: process + test-context + product-family features | FVL-05.004, FVL-06 | YES | |
| FVL-07.006 | Feature generation: optional interaction features, only when reproducible | FVL-07.004, FVL-07.005 | NO | |
| FVL-07.007 | Baseline models first (simple deterministic/statistical baseline before any ML candidate) | FVL-07.004–006 | YES | |
| FVL-07.008 | Candidate PREDICTION-MODEL comparison for the performance target (deterministic/statistical baseline vs. ML), train/validation/test split, cross-validation where appropriate, recorded random seeds — this is model selection for FVL-07's prediction target only, never a second deterministic ingredient-selection/candidate-pool engine parallel to `engine.py` | FVL-07.007 | YES | |
| FVL-07.009 | Hyperparameter recording — no fabricated result | FVL-07.008 | YES | |
| FVL-07.010 | Evaluation: appropriate metrics (RMSE/MAE/R² only where mathematically suitable), held-out performance vs. baseline, failure thresholds, acceptance policy | FVL-07.008 | YES | |
| FVL-07.011 | Model registry: ID/version, dataset hash, feature-schema version, training date, algorithm, params, metrics, target definition, applicability domain, uncertainty method, artifact checksum, status | FVL-07.009, FVL-07.010 | YES | |
| FVL-07.012 | Prediction API: exact formula/version lineage in, predicted target + uncertainty/CI where supported | FVL-07.011 | YES | |
| FVL-07.013 | Applicability-domain warning / out-of-domain refusal | FVL-07.012 | YES | |
| FVL-07.014 | No regulatory/safety override by prediction (hard rule, tested) | FVL-07.012 | YES | |
| FVL-07.015 | Per-product-family, per-target model training — never one universal model claiming to predict everything | FVL-07.001, FVL-07.011 | YES | |
| FVL-07.016 | Regression + acceptance tests: eligibility gate, baseline-vs-candidate comparison, registry round-trip, out-of-domain refusal | FVL-07.003, FVL-07.011, FVL-07.013 | YES | |

---

## FVL-08 — Performance Ranking + Existing Optimizer Integration

Rank feasible alternatives using FVL-07's predictions. No single opaque AI
score.

**Existing dependency / baseline capability**: Advanced Optimizer, Cost
Engine — reused, not duplicated.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-08.001 | Ranking data model: predicted performance, uncertainty, cost, material availability, compatibility, safety, regulatory, manufacturing feasibility, evidence completeness — kept separately visible, never collapsed into one score | FVL-07.012, FVL-03 | YES | |
| FVL-08.002 | Hard rule: Safety/Regulatory FAIL cannot be outweighed by predicted performance | FVL-08.001 | YES | |
| FVL-08.003 | Missing prediction never silently becomes zero; out-of-domain prediction never looks equivalent to a validated one | FVL-08.001, FVL-07.013 | YES | |
| FVL-08.004 | Cost dimension sourced from the existing Cost Engine (FVL-03.003), never recomputed independently | FVL-08.001 | YES | |
| FVL-08.005 | Optimization pass uses the existing Advanced Optimizer where applicable (FVL-03.005), not a new solver — single-authority rule applies here exactly as elsewhere even though this row is non-blocking; do not build a second solver merely because this task itself isn't gating | FVL-08.001 | NO | |
| FVL-08.006 | UI: result screen exposes why one alternative ranks differently (per-dimension breakdown, not a single number) | FVL-08.001 | YES | |
| FVL-08.007 | Persistence + audit trail for every ranking input | FVL-08.001 | YES | |
| FVL-08.008 | Regression + acceptance test: a Safety-FAIL alternative never outranks a passing one regardless of predicted performance | FVL-08.002 | YES | |

---

## FVL-09 — Active Learning / Next Best Experiment

Uncertainty-aware experiment recommendation over the existing DOE +
Laboratory architecture. Does not replace the DOE engine.

**Existing dependency / baseline capability**: DOE engine (spec §5),
Laboratory Trials.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-09.001 | Candidate experiment space definition over the existing formula/DOE model | FVL-07, FVL-02 | YES | |
| FVL-09.002 | Feasibility filtering (materials, manufacturing) | FVL-09.001, FVL-03 | YES | |
| FVL-09.003 | Safety/regulatory filtering (hard exclusion, never overridden) | FVL-09.001 | YES | |
| FVL-09.004 | Uncertainty + defensible acquisition criterion (expected information gain or equivalent, named and documented) | FVL-07.012 | YES | |
| FVL-09.005 | Diversity + estimated experiment cost where real cost data exists | FVL-09.004, FVL-03.003 | NO | |
| FVL-09.006 | DOE integration: recommendation ranking + rationale surfaced through the existing DOE UI/workflow | FVL-09.004, FVL-09.002, FVL-09.003 | YES | |
| FVL-09.007 | Human selection required — no automatic lab execution | FVL-09.006 | YES | |
| FVL-09.008 | Creation/linkage of a real LaboratoryTrial/DOE run from an accepted recommendation | FVL-09.007 | YES | |
| FVL-09.009 | Completed-experiment ingestion triggers model retraining (versioned, per FVL-07.011) | FVL-09.008, FVL-07.011 | YES | |
| FVL-09.010 | Regression test: an unperformed recommendation is never presented/labeled as validated | FVL-09.007 | YES | |

---

## FVL-10 — Closed Laboratory Feedback Loop

Close and trace the full lineage: Generated Formula → Saved Version →
Laboratory Trial → Actual Process Data → Test Results → Stability Results →
DOE Observation → Historical Dataset → Model Training → Prediction → Active
Learning Recommendation → Next Trial → New Data → Retraining.

**Existing dependency / baseline capability**: every individual stage above
already exists as a FormuLab record type; this package closes the
traceable chain between them.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-10.001 | ID/provenance model spanning every transition in the chain (extends `traceability.py`'s existing convention) | FVL-03.011, FVL-05.002 | YES | |
| FVL-10.002 | Immutable references — a later stage cites, never rewrites, an earlier one | FVL-10.001 | YES | |
| FVL-10.003 | Version relationships across the chain (formula version ↔ trial ↔ result ↔ dataset row ↔ model ↔ prediction ↔ recommendation) | FVL-10.001 | YES | |
| FVL-10.004 | Corrective-action-derived draft linkage | FVL-10.001 | NO | |
| FVL-10.005 | DOE-derived draft linkage | FVL-09.008 | NO | |
| FVL-10.006 | Phase-14-generated draft linkage (formula version ↔ its own generation session) | FVL-10.001 | YES | |
| FVL-10.007 | Prediction source-model linkage (which exact model version produced a shown prediction) | FVL-07.012, FVL-10.001 | YES | |
| FVL-10.008 | Retraining lineage (which new data triggered which model version) | FVL-09.009 | YES | |
| FVL-10.009 | UI navigation across the full chain + audit trail view | FVL-10.003 | YES | |
| FVL-10.010 | Failure/retry behavior + human gates at each transition; hard rule: no model may rewrite a laboratory result | FVL-10.002 | YES | |

---

## FVL-11 — Final Integrated Acceptance & FormuLab v1 Closure

Closure only — no new feature development.

**Existing dependency / baseline capability**: every prior FVL package.

| Task ID | Title | Depends on | Blocking | Status |
|---|---|---|---|---|
| FVL-11.001 | Acceptance matrix design across several genuinely different product families | FVL-02–FVL-10 | YES | |
| FVL-11.002 | Full-chain acceptance run 1: request → literature → full-text gate → evidence → 3–7 alternatives → materials/masterdata → supplier → cost → optimization → compatibility → safety → regulatory → manufacturing → report | FVL-11.001 | YES | |
| FVL-11.003 | Full-chain acceptance run 2 (second product family) covering the same chain plus lab trial → measured performance → stability → DOE → historical dataset → predictive model → alternative ranking → active learning → next experiment → retraining → traceability | FVL-11.001 | YES | |
| FVL-11.004 | Backward compatibility: historical sessions (including pre-dynamic-count V1/V2/V3 sessions) still open correctly | FVL-02.019 | YES | |
| FVL-11.005 | Old vs. new formulation UI decision — made only after the new UI is proven stable across FVL-11.002/003 | FVL-11.002, FVL-11.003 | YES | |
| FVL-11.006 | Authorization/role gates + approval gates re-verified end to end; no AI auto-approval anywhere in the chain | FVL-11.002 | YES | |
| FVL-11.007 | Zero-LLM formulation regression re-confirmed (permanent guard test still passes) | FVL-11.002 | YES | |
| FVL-11.008 | Data integrity + backup/restore re-verified where relevant to new record types (dataset, model registry, predictions, recommendations) | FVL-10 | YES | |
| FVL-11.009 | Full regression suite: `runtime/pipeline` pytest, Rust workspace, frontend Vitest, tsc, ESLint, `git diff --check` | FVL-11.002, FVL-11.003 | YES | |
| FVL-11.010 | Installer build: fresh `formulab.exe`, MSI/NSIS bundles | FVL-11.009 | YES | |
| FVL-11.011 | `C:\Users\sekip\Desktop\FormuLab.lnk` verification against the fresh build | FVL-11.010 | YES | |
| FVL-11.012 | GitHub branch state confirmed (local HEAD == `origin/feature/laboratory-stability`, no outstanding blocking issue) | FVL-11.010 | YES | |
| FVL-11.013 | `docs/handoffs/FORMULAB_V1_CURRENT.md` updated to closure state | FVL-11.012 | YES | |
| FVL-11.014 | Explicit **FormuLab v1 COMPLETE** declaration recorded, only after every task above is COMPLETED | FVL-11.001–013 | YES | |

---

## Validation

Run `python scripts/validate_v1_tracker.py` before every commit that
touches this file or `FORMULAB_V1_FINAL_SCOPE.md`. It checks: unique task
IDs, only allowed status literals, every dependency references a real task
ID, all eleven `FVL-01`..`FVL-11` packages present and no `FVL-12`+, and
every task belongs to exactly one package. See that script's own header for
usage.
